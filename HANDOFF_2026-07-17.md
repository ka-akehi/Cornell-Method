# Cornell Method Notebook 引き継ぎメモ

作成日: 2026-07-17（JST）

## この handoff の目的

2026-07-16 前半までの仕様整理・MVP QA・次の UI-001 を記録していた旧 handoff に、その後の「紙面を中心にした学習ノート」への設計転換、概念 PNG の確定、概念画像と現行 DOM の差分監査、UI-PAPER-011〜016 の実装結果、静的検証結果、ブラウザ目視 QA の未実施理由を統合する。次回の Manager / Worker は、raw log や `.next` 生成物を読み直さず、この handoff と `Next Read` に示す最小ファイルから再開する。

## 1. 現在の全体状態と未コミット方針

- 現在の実装は、従来のフォーム／カード中心の画面から、深いフォレスト色のアプリクロームと暖色の一枚紙面を主役にする紙面中心 UI へ移行した状態である。
- UI-PAPER-011〜016 の対象実装は完了通知済みで、コード上は共通 chrome の状態表示、fluid paper shell、罫線、本文 Markdown Preview、作成・編集・閲覧・復習の紙面構造、edit 状態の重複表示抑制まで反映されている。
- 視覚的な受け入れはまだ完了していない。ブラウザ目視 QA が未実施のため、紙面 UI 全体を PASS と扱わない。
- 現行 MVP の route、API、Prisma / SQLite、保存・削除・復習の判断は `doc/implementation/MVP_CONTRACT.md` が正本であり、紙面 UI の変更はデータモデルや API 契約を変更しない。
- 作業開始時点で既存の未コミット変更があった。主なものは紙面 UI のコード、画面設計書、概念画像／補助 HTML、UI Worker summary であり、これらはユーザーの作業として保持する。
- まだ commit は作成していない。既存の未コミット変更を戻さず、発注者の指示なしに `commit`、`reset`、`checkout` を実行しない。
- この handoff task で意図する変更は `HANDOFF_2026-07-17.md` の追加、`AGENTS.md` の最新 handoff ポインタ更新、`HANDOFF_2026-07-16.md` の削除だけである。コード、設定、依存関係、DB、テスト、画像、生成物、summary、queue の running/done/failed は変更しない。

## 2. このチャットの経緯

### 2.1 初期の UI 問題提起

発注者から、次の問題が明示された。

- 基本情報がノート本文と同じ画面で目立ち、ノートを書く邪魔になっている。
- PC で開いているのにノート記載欄が狭い。
- 現在の画面が「ノート」という感じになっていない。

当初のフォーム中心の見た目を微調整するだけでは、紙面を中心にする目的と本文の読み書き幅を同時に満たしにくいと判断した。

### 2.2 紙面中心への方向転換

設計方針を「管理フォームを主役にする画面」から「タイトル、Cue、本文、Summary が連続する学習紙面を主役にする画面」へ変更した。基本情報は削除せず、タイトル直下のコンパクトなメタ情報帯と概要の開閉領域へ圧縮する。本文は PC で最も広い列に置き、Cue / 本文は約 30% / 70% を維持する。

### 2.3 概念画像の確定と旧 PNG の整理

実際の画面イメージの視覚的な正本を、次の概念 PNG に確定した。

`doc/screens/assets/mockups/mvp-paper-note-canvas-concept.png`

旧来の編集／復習別 PNG export は現行の参照画像から外し、不要な旧 PNG を削除した。概念 PNG は残し、`mvp-paper-note-canvas-mock.html` は表示状態や操作順を確認する補助資料として扱う。どちらも本番 runtime QA の証跡ではない。

### 2.4 背景・ヘッダー・紙面幅の差分修正

概念画像と実画面の比較で、次の差分を順に指摘・修正した。

- フォレスト色のアプリ背景と暖色の紙面背景が route 間で揃っていない。
- ヘッダーの brand / nav の配置が崩れ、紙面内の情報と共通 chrome の境界が曖昧になっている。
- 新規作成画面だけ背景や紙面の扱いが異なる。
- 紙面が狭い上限に閉じ込められ、PC の本文領域を十分に使えていない。
- 紙面の外周、ページ背景、縦罫線・横罫線、本文 Preview の扱いが概念画像と異なる。

先行する紙面 UI task で共通 shell、ヘッダー安定化、新規ページの紙面化、背景統一、紙面幅と page marker の調整を進めた。その後の静的監査で、見た目だけでなく DOM の状態差分、詳細画面の local overflow、edit のタイトル／状態重複も再確認した。

### 2.5 概念画像と現行画面の差分監査

`summary/20260716/2355-paper-note-canvas-diff-audit-20260716-summary.md` と設計書の監査では、概念 PNG（1672 × 941）を上から下へ分解し、現行の create / edit / view / review の DOM、class、表示状態と照合した。

主な監査結果は次のとおり。

- 既に存在するもの: フォレスト系背景、暖色 paper shell、create/edit の本文 textarea → Preview、review の本文初期マスク、Cue リスト、Summary の領域、明示保存。
- 不足または部分一致だったもの: chrome 右上の状態 badge、画像相当のコンパクトなメタ帯、ruled surface の横罫線、detail の Cornell local scroll、paper-native Preview、Summary / footer の一体化。
- detail edit には外側のタイトルと embedded `NoteEditor` の title input が重複していた。
- 概念画像の `編集中` と Preview / Summary が見える状態は create/edit の視覚正本として解釈した。復習画面については画像をそのまま複製せず、MVP 契約どおり本文確認後に Summary を開く順序を維持する。
- Cue リストと 1 本の Markdown 本文、タグ、概要、`nextReviewDate`、`reviewedAt` は紙面化のために分割・統合しない。

## 3. 視覚的な正本と設計基準

### 3.1 視覚的な正本

`doc/screens/assets/mockups/mvp-paper-note-canvas-concept.png`

この画像を背景色だけでなく、紙面の構造、情報階層、操作位置の基準とする。最低限、次の関係を保つ。

- 深いフォレストグリーンの app chrome。左に `C` マークとアプリ名、一覧・新規作成などのナビ、右上に画面状態 badge。
- chrome の下に、十分な外周 gutter と控えめな影を持つ暖色アイボリーの一枚紙面。
- 紙面上部に一つだけ置く大きなタイトル。create / edit は同じ位置の title input、view / review は `h1` とする。
- タイトル直下のコンパクトなメタ情報帯。学習日、学習元、タグを操作可能なまま置き、狭幅では field 単位で自然に折り返す。
- Cornell 本体は Cue 約 30% / 本文約 70%。Cue は左、本文は右、中央に縦罫線を置き、両列に薄い横罫線を引く。本文を主役にし、細かな floating card を主役にしない。
- 編集時の本文列は textarea の下に Markdown Preview を縦に置く。Preview は本文列と同じ紙面の表示領域として扱い、GFM、`rehype-sanitize`、表示専用 checkbox を維持する。
- Cornell の下に全幅 Summary を置く。保存・キャンセル・次回復習日などの操作は紙面フッターにまとめ、広い幅では保存を右下の primary action とする。

画像の pixel-perfect 再現は目的にしない。紙面幅は `viewport - outer gutter` の fluid rule、本文の可読幅、375 / 768 / 1280 / 1440px の到達性を優先する。概念画像と standalone HTML、静的 class の存在だけでは runtime PASS を宣言しない。

### 3.2 モード間の固定骨格

タイトル → メタ情報 → 概要 → Cornell（Cue / 本文）→ Summary → footer の順序は create / edit / view / review で共有する。モード差分は入力可否、本文と Summary の開示状態、復習操作に限定する。

- create: title / metadata / overview / Cue / body textarea → Preview / Summary textarea → Preview / next review date / cancel / save。
- edit: create と同じ紙面位置で既存値を編集し、`PATCH` の明示保存を行う。
- view: 本文と Summary を Markdown 表示する。編集・復習・削除・一覧へ戻る導線を維持する。
- review: Cue と概要を手がかりにし、本文を初期マスクする。本文を表示した後に Summary を開き、復習済み API を呼ぶ。本文表示状態と Summary 開示状態は保存しない。

## 4. 設計監査の結果と実装順序

監査で確定した実装順は次のとおり。

`UI-PAPER-011 → UI-PAPER-012 → UI-PAPER-013 → UI-PAPER-014 → UI-PAPER-015 → UI-PAPER-016`

設計上は 014 と 015 を 013 完了後に並行可能としていたが、edit の shell / chrome 状態の受け渡しが共通するため、実際には完了内容を照合しながら順に進めた。次の runtime QA はこれらの完了だけでは代替できない。

## 5. UI-PAPER-011〜016 の task 履歴

| task | 主な対象 | 実装内容 | 依存 | 状態 |
| --- | --- | --- | --- | --- |
| `UI-PAPER-011` | `src/app/_components/app-chrome.tsx`, `src/app/layout.tsx` | 共通 header の brand / nav を維持しつつ、`create` / `edit` / `view` / `review` を `新規作成` / `編集中` / `閲覧中` / `復習中` として右上 badge に表示する state slot を追加。path の既定 state と component からの state override を context で扱い、badge を canonical な状態通知にした。 | なし | done |
| `UI-PAPER-012` | `src/app/globals.css` | フォレスト chrome、暖色 paper token、fluid paper width、outer gutter、inner padding、控えめな影、紙面と Cornell の repeating horizontal rule、中央 vertical rule、focus / error 表示、detail の Cornell local overflow、狭幅の wrap を共通 CSS 化した。 | 011 | done |
| `UI-PAPER-013` | `src/shared/markdown/markdown-field.tsx` | shared `MarkdownPreview` を paper-native な淡色 surface / empty state に更新。`remark-gfm`、`rehype-sanitize`、表示専用 checkbox の `readOnly` / `preventDefault`、label、`aria-describedby`、`stacked` / `desktop-split` 互換を維持した。 | 012 | done |
| `UI-PAPER-014` | `src/app/notes/_components/note-editor.tsx` | create / edit の一枚紙面を title → metadata → overview disclosure → Cue → body textarea / Preview → Summary → footer の順に整理。Cue の追加・削除、タグ候補・最大 12 件、日付制約、validation、POST / PATCH、明示保存、cancel、保存中 disabled は維持した。 | 012, 013 | done |
| `UI-PAPER-015` | `src/app/notes/_components/note-detail-modes.tsx` | view / review の紙面 shell を揃え、detail の Cornell と 30/70、本文列、全幅 Summary、復習 footer、削除確認、戻る導線を紙面順序へ再配置。review では本文を初期非表示にし、本文確認後だけ Summary を開ける body → Summary の段階的開示を現行コードへ反映した。 | 012, 013, 014 | done |
| `UI-PAPER-016` | `src/app/notes/_components/note-detail-modes.tsx` | edit mode で外側の `AppChromeState` を重ねて登録せず、embedded `NoteEditor` が供給する `編集中` badge だけを使うよう状態注入を整理した。編集時の chrome state の二重表示／cleanup 競合を避ける。 | 015 | done |

根拠として確認した summary は次のとおり。summary の完了状態・対象ファイル・未確認事項を使い、summary の `Next Read` にある `.next` 生成物と raw log は読んでいない。

- `summary/20260716/2355-paper-note-canvas-diff-audit-20260716-summary.md`
- `summary/20260717/0000-design-paper-layout-gap-audit-bed9c8a6-summary.md`
- `summary/20260717/0012-ui-paper-011-common-chrome-state-slot-b6ed94ef-summary.md`
- `summary/20260717/0022-ui-paper-012-paper-shell-ruling-responsive-css-887ce96e-summary.md`
- `summary/20260717/0031-ui-paper-013-paper-native-markdown-preview-40782dc1-summary.md`
- `summary/20260717/0046-ui-paper-014-create-edit-paper-layout-rebuild-657bc2e8-summary.md`
- `summary/20260717/0050-ui-paper-015-detail-review-paper-layout-rebuild-ba907683-summary.md`
- `summary/20260717/0951-ui-paper-016-deduplicate-edit-chrome-state-646a212e-summary.md`

## 6. 現在の主要ファイルと責務

| ファイル | 責務と次回確認ポイント |
| --- | --- |
| `src/app/_components/app-chrome.tsx` | `AppChrome`、global nav、path からの既定 mode、`AppChromeState` context、画面状態 badge を担当。edit / review の override と badge の一重性を確認する。 |
| `src/app/layout.tsx` | `body` を `AppChrome` で包み、metadata と `globals.css` を適用する route 共通入口。ページ固有の紙面構造を重複させない。 |
| `src/app/globals.css` | chrome / paper の design token、全体背景、fluid shell、紙面罫線、Cornell の 30/70、responsive local overflow、focus / error / footer の共通 styling を担当する。 |
| `src/app/notes/_components/note-editor.tsx` | create / edit の client form。title、date、source、tags、overview、Cue list、本文 Markdown、Summary、next review date、明示保存、validation、cancel を担当する。 |
| `src/app/notes/_components/note-detail-modes.tsx` | `/notes/[id]` 内の view / edit / review state、view shell、review の本文／Summary 開示、review API、削除確認、編集への切替を担当する。 |
| `src/shared/markdown/markdown-field.tsx` | 編集用 `MarkdownField` と表示用 `MarkdownPreview` の共通責務。GFM、sanitize、checkbox 表示専用化、Preview surface、empty state、field error を担当する。 |
| `src/app/notes/new/page.tsx` | `/notes/new` の成功経路を `.note-paper-page` で包み、`NoteEditor mode="create"` を描画する。背景や shell を独自に持たない。 |
| `src/app/notes/[id]/page.tsx` | ノート詳細を取得し、not-found branch と成功時の `.note-paper-page` wrapper を担当する。view / edit / review の本体は `NoteDetailModes` に委譲する。 |

## 7. 現行 MVP / Phase 2 境界と既存契約差分

### 7.1 現行 MVP の正本

現行 MVP は、ローカル個人利用で次の流れを明示保存で完了できる範囲である。

- `/notes` の一覧・検索・日付・タグ・復習対象 filter、`/notes/new` の新規作成、`/notes/[id]` の view / edit / review、`/backup` の手動バックアップ。
- `Notebook` にタイトル、学習日、学習元、概要、タグ、1 本の Markdown `body`、Summary、手動管理の `nextReviewDate` / `reviewedAt` を保存する。
- Cue はリストとして保持し、本文との厳密な Card link は持たない。保存は新規 `POST`、編集 `PATCH` の明示保存である。
- 詳細画面の削除は確認後に `DELETE /api/notes/:id` で物理削除する。現行 MVP は削除後の Undo / 復元を保証しない。
- 復習は `/notes/[id]` 内で Cue → 本文確認 → Summary 確認を行い、`POST /api/notes/:id/review` で `reviewedAt` と任意の次回日を更新する。

### 7.2 紙面 UI task とは別に残る MVP 契約差分

- `nextReviewDate + 7日` の初期値: `MVP_CONTRACT.md` は新規ノートで `noteDate + 7日` を初期値とするが、現行 `createInitialNoteEditorForm` は空欄で始まり、UI-PAPER task では修正していない。既存ノートの未設定値を自動補完しない、日付変更で既存設定値を自動移動しない、という契約は別途守る。
- 概要の Markdown preview / sanitize: 契約は閲覧時の概要を Markdown として安全に表示することを求める。現行 `NoteEditor` は概要を textarea、`NoteDetailModes` は通常の text 表示としており、本文／Summary と同じ `MarkdownPreview` を通っていない。紙面 UI task は概要のデータを壊していないが、この差分は未解決である。
- 復習時 Summary の初期開示: UI-PAPER-015 により、現行コードでは `showSummary=false` で開始し、本文を表示した後にのみ Summary を開ける。これは旧 `doc/implementation/IMPLEMENTATION_STATUS.md` の「Summary は常時表示」という記述より新しいコード状態である。次回の DOC / QA でコード、契約、実装状況、証跡を再照合して `IMPLEMENTATION_STATUS.md` を更新する。古い文書を根拠に UI-PAPER-015 の実装を未達と扱わない一方、runtime 未確認を PASS と扱わない。

### 7.3 Phase 2 に送る機能

次の機能は紙面 UI task の完了に含まれない。

- `NotebookDraftState`、3 秒 autosave、差分保存、optimistic lock / 409、再試行バナー、確定保存との競合解決。
- soft delete、5 秒 Undo Snackbar、`SoftDeleteBuffer`、`/api/undo`、期限切れ purge。
- `/tasks/review`、`/api/review-tasks`、1 日後 / 1 週間後の専用復習タスク、review status、未完了タスク badge、自動 spaced repetition。
- `NoteCard` / `CueCard`、本文カード分割、`NoteCueLink`、hidden flag、D&D 並び替え。
- PDF / HTML export、期間 export、Playwright による 1 ノート 1 ページ PDF。
- タグ名称変更・削除、管理 UI、タグ専用 mutation API、右クリック管理。
- `BackupLog`、起動時自動バックアップ、backup log / retry UI、`/api/backups/retry`。

これらは必要になった時点で、MVP 契約、schema、API、画面、テストを分けて task 化する。紙面の見た目が整ったことを理由に Phase 2 のコードやデータモデルを先行追加しない。

## 8. 最終検証と未確認事項

### 8.1 最終静的検証

UI-PAPER-011〜016 の実装後に行われた最終静的検証は次のとおり、すべて成功している。

| コマンド | 結果 |
| --- | --- |
| `npm run lint` | PASS |
| `npm run build` | PASS |
| `git diff --check` | PASS |

この handoff task ではコードや生成物を変更しないため、lint / build の再実行を完了条件にはしない。今回の handoff 更新後にも `git diff --check` を実行し、結果を最終報告する。

### 8.2 ブラウザ目視 QA は未実施

ブラウザ目視 QA は未実施であり、PASS と記録しない。browser runtime の browser list が空で、ローカル開発サーバーも環境制約または既存プロセスのため Manager から目視確認できなかった。したがって、次の事項は未確認である。

- `/notes/new` の 1440px / 375px で、概念 PNG と紙面幅、背景、タイトル、メタ帯、本文 Preview、Summary / 保存 footer を比較すること。
- `/notes/[id]` の view / edit / review で、タイトル重複、状態 badge の重複、Cue 30% / 本文 70%、本文 → Summary の復習開示、横溢れ、操作到達性を確認すること。
- 375 / 768 / 1280 / 1440px の create / edit / view / review を fixture 付きで `doc/testing/TEST_SCENARIOS.md` に記録すること。

既存の `TEST_SCENARIOS.md` にある旧レイアウトの証跡は、今回の紙面 UI の runtime PASS を意味しない。新しい目視結果を取得するまで、未確認は未確認のまま残す。

## 9. 次回の最優先作業と確認観点

1. `AGENTS.md`、この `HANDOFF_2026-07-17.md`、関連 summary を読む。
2. browser が利用可能なら `/notes/new` を 1440px / 375px で開き、視覚的な正本 PNG と比較する。背景、紙面の幅、ヘッダー、タイトル、メタ帯、Cue / 本文の罫線、本文 Preview、全幅 Summary、右下保存を確認する。
3. `/notes/[id]` の view / edit / review を確認する。特に、タイトルの二重表示、状態 badge の二重表示、Cue 30% / 本文 70%、復習時の本文 → Summary の初期開示順、detail の局所横スクロールとページ全体の横溢れを確認する。
4. 差分が見つかったら、対象ファイル単位で小さな UI task に分割して投入する。既存の未コミット変更と重なる場合は、先に Manager へ論点を報告する。
5. runtime 確認ができたら、viewport、mode、fixture、実施日、判定、参照 summary を `doc/testing/TEST_SCENARIOS.md` に追記する。未実施を PASS にしない。
6. runtime 後に `doc/implementation/IMPLEMENTATION_STATUS.md` をコードと契約へ再照合し、UI-PAPER-015 の Summary 開示実装、`nextReviewDate + 7日`、概要 Markdown preview / sanitize の状態を分けて更新する。

## 10. 次回の最小 Read

次の順で読む。`.next` の生成物と raw log は除外する。

1. `AGENTS.md`
2. `HANDOFF_2026-07-17.md`
3. `summary/20260716/2355-paper-note-canvas-diff-audit-20260716-summary.md`
4. `summary/20260717/0951-ui-paper-016-deduplicate-edit-chrome-state-646a212e-summary.md`
5. `doc/implementation/MVP_CONTRACT.md`
6. `src/app/_components/app-chrome.tsx`
7. `src/app/layout.tsx`
8. `src/app/globals.css`
9. `src/app/notes/_components/note-editor.tsx`
10. `src/app/notes/_components/note-detail-modes.tsx`
11. `src/shared/markdown/markdown-field.tsx`
12. 必要に応じて `src/app/notes/new/page.tsx` と `src/app/notes/[id]/page.tsx`

UI-PAPER-011〜015 の個別 summary や `doc/screens/MVP_SCREEN_DESIGN.md` / `doc/screens/MVP_SCREEN_INVENTORY.md` は、差分の再確認が必要な場合だけ追加で読む。summary の `Next Read` に列挙された `.next` は成果物根拠として扱わない。

## 11. 運用上の注意

- 作業前後に `git status --short` を確認し、ユーザーの未コミット変更を戻さない。
- 発注者の指示なしに commit、reset、checkout を行わない。
- 既存の failed task は、失敗理由と依存関係を確認せず推測で再実行しない。
- Worker の `done`、summary の作成、対象ファイルの存在だけで成果物の妥当性を PASS にしない。コード、runtime、証跡を分けて確認する。
- 長い調査結果や raw log を handoff に転載しない。.next、cache、trace、生成済み manifest は次回の根拠として読まない。
- 仕様判断が必要な場合は推測で実装せず、MVP 契約と紙面設計のどちらに関わる論点かを示して発注者へ確認する。
