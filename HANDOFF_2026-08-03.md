# Cornell Method Notebook 引き継ぎ資料

作成日: 2026-08-03（JST）

対象: 2026-08-02〜2026-08-03 の AppChrome／desktop sidebar collapse 作業と、未コミット UI redesign worktree の安全な再開

この資料は、次回の Manager / Worker が raw log を読み直さずに現在地・判断・検証境界・次の作業順序を把握できるようにまとめたものです。raw log、長大な command output、秘密情報は記録しません。

## 1. 現在地

### 1.1 Git / remote / branch

| 項目 | 確認結果 | 種別 |
| --- | --- | --- |
| branch | `develop` | 事実 |
| `HEAD` | `09b760e2e5056c4c904c30fb486c70079ad83173`（`add`） | 事実 |
| `origin/develop` | `09b760e2e5056c4c904c30fb486c70079ad83173`。local `develop` と一致 | 事実 |
| `origin/main` | `9886472e59d48072eaa85258025feebb029da071`（`Exclude operational records from Codex review (#80)`） | 事実 |
| `origin/main` と `HEAD` | 共通祖先は `634a9e7`。`origin/main` は `HEAD` の祖先ではない | 事実 |
| `origin/main` の develop への統合 | 未完了。今回も merge、rebase、push、履歴書き換えは行っていない | 事実 |

現在の基準 ref は上表です。旧 handoff に書かれた branch や commit を現在の状態として扱わないでください。

PR #71 は既存の統合判断対象です。local ref では `b98363e`（`fix: preserve note edit mode across reloads (#71)`）が `develop` 側に存在しますが、`origin/main` にはありません。GitHub 上の PR #71 の live な open／review／CI／merge 状態は今回確認していないため、過去の記録を現在の証拠とみなさず、接続可能な環境で再確認してください。

### 1.2 作業ツリーと保護対象

作業開始時点で、既存の未コミット変更を確認しました。これらはユーザーの作業であり、戻したり整理のために stash／reset／checkout／clean したりしてはいけません。

- tracked 差分は 29 ファイル。AppChrome、App shell／foundation、paper、Canvas、list、backup、detail／editor 周辺の source と関連 contract test が含まれます。
- untracked には `floating-tooltip-mockup.png`、8/2〜8/3 の Worker／Manager summary、追加の responsive／visual contract test が含まれます。
- 差分量は確認時点で約 1,719 insertions / 306 deletions。mock UI redesign の広範な差分であり、まだ branch 統合済みの受け入れ成果とは扱いません。
- 今回の handoff 作業では source、test、CSS、API、DB、依存関係、画像、既存 summary、queue の task state を変更しません。追加する変更は本 handoff と `AGENTS.md` の参照更新、旧 handoff の置換だけです。

### 1.3 Queue

作業開始時の `codex-queue/bin/worker-status.sh --queue all` では、queued は 0、UI / API は実行中 0、Common はこの handoff task 自身が running でした。queue の `running`／`done`／`failed` は直接編集していません。Worker runner が完了時にこの task を terminal state へ移動する前提です。

## 2. このチャットでの意思決定の時系列

以下は、スクリーンショット／実装／設計 task の結果を区別して時系列に整理したものです。

1. **rail 外へ押し出す配置の問題**: 最初の desktop hamburger は absolute 配置と負の `right` offset により persistent rail の外側へ張り出していました。rail と操作部品の関係が不明瞭で、最初の修正では rail 内の in-flow slot へ戻しました。
2. **rail 上部単独配置の問題**: rail 内の最上部に hamburger だけを置くと、brand と結び付かず単独で浮いて見えました。常設 rail を折りたたむ操作として視覚的な説明力も弱いと判断しました。
3. **brand 右側配置による brand 圧迫**: brand header の右側へ hamburger を水平整列する案を実装しましたが、限られた rail 幅で brand の表示領域を圧迫し、mobile 用の hamburger 的意味も残りました。
4. **persistent desktop rail に hamburger は不適切**: desktop rail は常設ナビゲーションであり、hamburger は mobile overlay の開閉を示す意味が強いため、desktop の rail collapse control として使わない方針に決めました。
5. **edge chevron handle の採用**: desktop は rail edge に専用の左右 chevron handle を置き、open 時は rail 右端、collapsed 時は main 左端に同じ button を残す設計を採用しました。collapsed 時は rail の `aside` を native `hidden` にして main の幅を全面的に解放します。
6. **design task と implementation task の完了**: `design-desktop-sidebar-collapse-dom-20260803-e22f044b` は設計成果物を作成して完了し、その設計を source of truth として `implement-desktop-sidebar-edge-handle-20260803-342fab85` が実装を完了しました。Manager 再検証では focused contract 2 件、lint、TypeScript、`git diff --check` が PASS です。Browser runtime の確認だけは未実施です。

関連 summary は次のとおりです。

- `summary/20260802/manager-next-work-organization-after-ui-redesign-20260802.md`
- `summary/20260802/2350-fix-app-chrome-hamburger-position-20260802-b4b31d2f-summary.md`
- `summary/20260803/0000-align-app-chrome-hamburger-with-brand-header-20260802-91b05ce1-summary.md`
- `summary/20260803/0027-design-desktop-sidebar-collapse-dom-20260803-e22f044b-summary.md`
- `summary/20260803/desktop-sidebar-collapse-dom-design-20260803.md`
- `summary/20260803/0043-implement-desktop-sidebar-edge-handle-20260803-342fab85-summary.md`

## 3. 現行 AppChrome の実装概要

### 3.1 DOM と state

`src/app/_components/app-chrome.tsx` の現在の構造は次のとおりです。

```text
app-chrome-shell（is-rail-collapsed）
├─ app-chrome-rail-region（is-collapsed）
│  ├─ button#app-chrome-rail-toggle.app-chrome-rail-handle
│  └─ aside#app-chrome-rail（hidden={!isRailOpen}）
│     └─ brand / global nav / 新規ノート
├─ app-chrome-content（inert={isMobileNavOpen}）
│  ├─ mobile header
│  │  └─ button#app-chrome-mobile-menu-button（mobile hamburger）
│  └─ main#app-main-content
└─ div#app-chrome-mobile-overlay（hidden={!isMobileNavOpen}）
   └─ mobile panel / close button / nav / 新規ノート
```

- `isRailOpen` は desktop rail と desktop edge handle だけを管理し、初期値は `true` です。
- `isMobileNavOpen` は mobile overlay と mobile hamburger だけを管理し、初期値は `false` です。
- desktop handle と mobile menu button は別 ref（`desktopRailHandleRef` / `mobileMenuButtonRef`）を持ちます。
- desktop の rail を閉じても mobile header は表示条件になりません。viewport listener は breakpoint の切替時に mobile overlay を閉じ、rail を open に戻し、必要な場合だけ次の trigger へ focus を移します。
- state の localStorage 永続化は設計範囲に含めず、reload では rail open に戻ります。

### 3.2 desktop edge handle

- `button#app-chrome-rail-toggle` は `aside` の子ではなく `app-chrome-rail-region` 内の兄弟です。
- open 時は `aria-label="サイドバーを折りたたむ"` と左 chevron、collapsed 時は `aria-label="サイドバーを展開する"` と右 chevronを表示します。
- `aria-expanded={isRailOpen}` と `aria-controls="app-chrome-rail"` を持ち、collapse 前後で同じ button が DOM に残ります。
- `aside#app-chrome-rail` は `hidden={!isRailOpen}` と `aria-hidden={!isRailOpen}` を持ちます。collapsed 時に hidden subtree 内へ focus が残らないことが設計上の狙いです。
- CSS は desktop で rail region を sticky な `100svh` とし、open 時の幅を `clamp(13.5rem, 18vw, 15.5rem)`、collapsed 時の region 幅を 0 とします。handle は rail edge から半分張り出す 2.75rem の hit area です。

### 3.3 mobile hamburger

- `button#app-chrome-mobile-menu-button` は mobile header 専用で、`aria-expanded={isMobileNavOpen}` と `aria-controls="app-chrome-mobile-overlay"` を持ちます。
- mobile overlay が開くと dialog semantics、`aria-modal`、panel の label、backdrop、close button、Escape、Tab focus trap、body scroll lock、main content の `inert` が有効になります。
- overlay を閉じたときの focus は mobile menu button へ戻します。desktop rail の collapse／expand 時の focus は同じ edge handle に戻します。
- `@media (min-width: 901px)` では mobile header／overlay／mobile menu button を隠し、`@media (max-width: 900px)` では desktop rail region／edge handle を隠して mobile header／hamburger を表示します。

このように、desktop の rail collapse と mobile の overlay navigation は責務、state、DOM、`aria-controls`、focus 復帰先を共有しません。

## 4. 検証結果と未確認範囲

### 4.1 実施済み

| 確認 | 結果 | 境界 |
| --- | --- | --- |
| `node --test test/notes/app-chrome-contract.test.js test/notes/app-chrome-responsive-contract.test.js` | PASS（2 tests） | source／CSS 契約の確認 |
| `npm run lint` | PASS | Manager 再検証の記録 |
| `npx tsc --noEmit --pretty false --incremental false` | PASS | Manager 再検証の記録 |
| `git diff --check` | PASS | 既存 UI 差分を含む whitespace 検査 |
| 作業前 `git status --short`、branch／HEAD／remote ref | 完了 | 現在地の記録 |
| queue status | 完了 | `worker-status.sh` の snapshot。queue state は変更していない |

この handoff Worker 自身の最終確認では、上記の focused contract と `git diff --check` を再実行します。lint／TypeScript は edge-handle implementation 後の Manager 再検証結果を引き継ぎます。

### 4.2 未確認・未解決

- Browser runtime backend が利用できないため、desktop 1280／1440px の rail open／collapsed の実 screenshot と accessibility tree は未確認です。
- mobile 900px 以下の hamburger、overlay、focus trap、resize による focus 復帰の実ブラウザ挙動も未確認です。
- `floating-tooltip-mockup.png` や既存 screenshot は設計の参考であり、今回の新しい desktop edge handle の受け入れ証拠ではありません。
- source-level contract が PASS でも、実画面での handle の z-index、main content との重なり、focus ring、brand／paper の最終的な見た目は未確定です。
- 広範な mock UI redesign の未コミット差分は採用範囲・統合単位・最終 QA が未確定です。追加 coding、origin/main merge、push は Manager の判断なしに進めません。
- PR #71 の live review／CI／merge 状態と、`origin/main` を `develop` に統合する方針は未確認・未決定です。

## 5. 次回の作業順序

### 最優先

1. まず本 handoff と `git status --short`、`git branch -vv`、`git log --left-right develop...origin/develop`、`git log --left-right origin/main...HEAD`、`git diff --check` を確認し、既存の source／test／summary／画像差分を保護します。
2. 接続可能な環境で PR #71 の live state（state、draft、review、checks、mergedAt）を read-only で確認し、local `develop` と `origin/main` の差分を Manager が比較します。
3. Browser runtime が復旧したら、実装前の追加修正を入れずに次の QA を行います。
   - desktop 901px 以上: rail open、edge handle 押下、collapsed、再展開、keyboard focus、main 幅
   - mobile 900px 以下: rail／handle 非表示、hamburger、overlay、backdrop／close／Escape／Tab、main inert
   - 900px 境界をまたぐ resize: overlay close、rail open、focus の復帰先
   - 1280／1440px の screenshot と accessibility tree
4. QA と未コミット差分の採用範囲が確定した後に、必要な追加 UI 修正、全体 lint／typecheck／build／E2E、または統合 task を別々に切り出します。

### 依存関係と実装前チェック

- PR #71 の判断と `origin/main` の統合は、現在の未コミット UI 差分をどう保護・採用するかの決定に依存します。先に merge／stash／reset／commit／push を推測で実行しません。
- AppChrome を修正する場合は、`summary/20260803/desktop-sidebar-collapse-dom-design-20260803.md` を設計の source of truth とし、`app-chrome-rail-toggle-slot` や desktop header 内の旧 hamburger が復活していないこと、desktop／mobile の state と `aria-controls` が混ざっていないことを確認します。
- browser QA で不具合が見つかった場合も、この handoff の範囲で直接広範な redesign を始めず、設計判断が必要な差分は別の Manager／UI task に分割します。
- PR を作成する場合は、`AGENTS.md` のルールに従い、Worker summary を PR の変更ファイルへ含めず、Issue 修正がある場合は Issue ごとの `Closes #[番号]` を本文に記載します。

## 6. Next Read

次回は raw log ではなく、次の最小ファイルをこの順で読みます。

1. `HANDOFF_2026-08-03.md`
2. `summary/20260802/manager-next-work-organization-after-ui-redesign-20260802.md`
3. `summary/20260803/desktop-sidebar-collapse-dom-design-20260803.md`
4. `summary/20260803/0043-implement-desktop-sidebar-edge-handle-20260803-342fab85-summary.md`
5. `src/app/_components/app-chrome.tsx`
6. `src/app/styles/app-shell.css`
7. `test/notes/app-chrome-contract.test.js` と `test/notes/app-chrome-responsive-contract.test.js`

次の判断が Git／PR 統合に及ぶ場合だけ、以下も追加で読みます。

- `summary/20260802/2313-audit-develop-origin-main-pr71-state-20260802-5cb103b5-summary.md`
- `summary/20260802/2319-audit-current-ui-worktree-after-mock-redesign-20260802-de08f298-summary.md`
- `summary/20260802/2350-fix-app-chrome-hamburger-position-20260802-b4b31d2f-summary.md`
- `summary/20260803/0000-align-app-chrome-hamburger-with-brand-header-20260802-91b05ce1-summary.md`
- `summary/20260803/0027-design-desktop-sidebar-collapse-dom-20260803-e22f044b-summary.md`

## 7. 最小再開コマンド

```sh
git status --short
git branch --show-current
git branch -vv
git log -1 --oneline --decorate
git log --oneline --left-right develop...origin/develop
git log --oneline --left-right origin/main...HEAD
git diff --check
codex-queue/bin/worker-status.sh --queue all
```

focused contract を再確認する場合:

```sh
node --test test/notes/app-chrome-contract.test.js test/notes/app-chrome-responsive-contract.test.js
```

PR #71 の live 状態を確認できる環境でのみ、次を実行します。

```sh
gh pr view 71 --json number,state,isDraft,baseRefName,headRefName,mergedAt,reviewDecision,statusCheckRollup
```
