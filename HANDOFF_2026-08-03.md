# Cornell Method Notebook 引き継ぎ資料

作成日: 2026-08-03（JST）

対象: PR #84 の UI redesign、保守性分割、および Issue #85 / #86 の follow-up

この資料は、次の Manager / Worker が現行の Git・PR・検証状態を起点に作業を再開するための handoff です。現行 MVP の実装・受け入れ判断は [`doc/implementation/MVP_CONTRACT.md`](doc/implementation/MVP_CONTRACT.md) を正本とし、製品全体のロードマップは [`AGENTS.md`](AGENTS.md) に従います。日付は `2026-08-03` のままです。新しい handoff は作成せず、`AGENTS.md` の latest handoff pointer も変更しません。

## 1. 現在地

### 1.1 Git / PR

| 項目 | 現在の事実 | 根拠・備考 |
| --- | --- | --- |
| branch | `agent/polish-notebook-ui-20260803` | `git branch --show-current` |
| base | `develop` | PR #84 の base |
| HEAD | `b5b65f5d959b6c3335726b5f538d58b4765cd740` | `keep collapsed rail handle fully visible` |
| remote head | `origin/agent/polish-notebook-ui-20260803` は HEAD と一致 | `git branch -vv` |
| PR #84 | Open・非 Draft・base `develop`・head は現 branch と同一 | PR の現行状態。Worker の GitHub 再照会はネットワーク接続失敗のため未実施 |
| Issue #87 | この Worker は close していない | Issue / PR の state は変更対象外 |

PR #84 の branch には、UI redesign、保守性分割、Issue #85 / #86 の修正を含む push 済み HEAD が存在します。以後はこの branch と HEAD を current state として再開します。

### 1.2 Worktree と保護対象

handoff 編集前の `git status --short` では、tracked source の差分はありませんでした。既存の untracked 運用記録はユーザーの作業として保持し、削除・変更・ステージしません。

- `summary/` 配下の既存 summary
- `floating-tooltip-mockup.png`

これらは PR #84 の対象外です。この Worker が変更する対象は `HANDOFF_2026-08-03.md` だけです。コード、設定、依存関係、DB、生成物、画像、summary、queue の task state は変更しません。

## 2. PR #84 の採用済み scope

以下は branch 上で commit 済みで、PR #84 の対象です。

- AppChrome の desktop rail と mobile navigation、responsive navigation の整理。
- note paper、Canvas、notes list、backup UI の visual / responsive 整理。
- AppChrome の部品境界と、Canvas toolbar の controls / layout / responsive / history / floating tooltip の保守性分割。
- Issue #85: pathname change 時の mobile navigation close。
- Issue #86: collapsed rail handle の全面表示。

主要 commit は次のとおりです。

| commit | 内容 |
| --- | --- |
| `5583994` | notebook UI と responsive navigation の redesign |
| `579e7a1` | AppChrome / Canvas toolbar の保守性分割 |
| `d104156` | Issue #85 の pathname-change close |
| `b5b65f5` | Issue #86 の collapsed rail handle 修正。現在の HEAD |

既存の CanvasDocumentV1、API、DB、MVP の明示保存・canonical route などの契約を、この UI 作業の採用根拠として変更していません。UI の static contract は runtime acceptance と別の証拠レベルで扱います。

## 3. Issue #85 / #86

### Issue #85 — pathname change 時に mobile navigation を閉じる

- 根本原因: pathname が変わっても `isMobileNavOpen` が残り、mobile overlay が開いた状態を次の route へ持ち越し得ました。
- 修正: `pathname` を依存値とする effect を追加し、`requestAnimationFrame` 内で `setIsMobileNavOpen(false)` を実行します。unmount 時は frame を cancel します。pathname change では通常の close handler を呼ばず、旧 trigger への focus 復帰も行いません。既存の Escape / backdrop / close 操作と body-scroll cleanup は維持しています。
- commit: `d104156` (`fix mobile navigation on pathname changes`)
- 検証: responsive contract test が pathname-change effect と、そこから `closeMobileNav` / mobile trigger focus を呼ばないことを確認しています。

### Issue #86 — collapsed rail handle を全面表示する

- 根本原因: rail が collapsed のとき、2.75rem の edge handle が `translateX(50%)` のままで、rail 側の clipping により hit area の一部が画面外へ出る可能性がありました。
- 修正: open state の edge placement は維持し、collapsed state では `.app-chrome-shell.is-rail-collapsed .app-chrome-rail-handle` を `translateX(100%)` として main-content 側へ移動し、handle 全体を表示します。
- commit: `b5b65f5` (`keep collapsed rail handle fully visible`)
- 検証: responsive contract test が collapsed state の `translateX(100%)` と、既存のサイズ・focus-visible 契約を確認しています。

## 4. 検証結果と境界

### 4.1 実施済み

| 確認 | 結果 | 境界 |
| --- | --- | --- |
| `npm run lint` | PASS | ESLint |
| `npx tsc --noEmit --pretty false --incremental false` | PASS | TypeScript no-emit |
| AppChrome / Canvas 関連 focused contract tests | PASS | source / CSS 契約。実ブラウザ挙動の証明ではない |
| `npm run build` | PASS | production build |
| `git diff --check` | PASS | whitespace 検査 |
| 作業前後の `git status --short` | 確認済み | handoff 以外の tracked source 差分と、保護対象 untracked の内容を変更していない |

### 4.2 未確認事項

in-app Browser は利用不可でした。したがって、次の実ブラウザ QA は未確認です。

- desktop rail の open / collapsed 状態における実際の focus、overlay、handle hit-area、focus ring、clipping。
- mobile navigation の pathname change、overlay、Escape / backdrop / Tab、body scroll、focus 復帰。
- paper / Canvas の各 viewport における実際の overflow、Canvas toolbar の floating tooltip placement、pointer / wheel / touch lifecycle。

static contract test、lint、TypeScript、build が PASS でも、上記の実ブラウザ acceptance を PASS と扱いません。Browser backend が復旧するまで、実ブラウザの focus / overlay / rail hit-area QA は blocked と記録します。

## 5. 次の作業

1. PR #84 の GitHub checks、特に Vercel と P2 / P3 review の状態を確認する。
2. PR 本文へ Issue close references を追加する。対象は `Closes #85`、`Closes #86`、`Closes #87` です。Issue #87 自体の state はこの handoff Worker が変更せず、PR の確認・更新時に Manager が扱います。
3. in-app Browser が利用可能になったら、AppChrome の desktop / mobile、pathname change、focus / overlay / collapsed handle hit-area、および paper / Canvas の runtime QA を実施する。
4. runtime QA で実際に再現した問題だけを、別の最小 task として切り出す。static contract PASS だけを理由に追加 redesign や API / DB 変更を始めない。

PR #84 の GitHub state、checks、本文更新、Issue の close は別々に確認し、Issue #87 を勝手に close しません。

## 6. Next Read

再開時は次の最小順で読みます。

1. `HANDOFF_2026-08-03.md`
2. `summary/20260803/issues-85-87-triage-20260803.md`
3. `doc/implementation/MVP_CONTRACT.md`
4. `src/app/_components/app-chrome.tsx`
5. `src/app/styles/app-shell.css`
6. `src/modules/notes/ui/components/canvas/toolbar.tsx` と関連 toolbar files
7. `test/notes/app-chrome-contract.test.js`
8. `test/notes/app-chrome-responsive-contract.test.js`
9. `test/notes/canvas-scroll-contract.test.js` と Canvas toolbar contract tests

PR の live state を確認できる環境では、まず PR #84 の checks と本文を read-only で確認してから、Manager の判断に従って更新します。

## 7. 最小再開コマンド

```sh
git status --short
git branch --show-current
git rev-parse HEAD
git show --stat --oneline --decorate HEAD
git diff --check
```

focused contract を再確認する場合:

```sh
node --test test/notes/app-chrome-contract.test.js test/notes/app-chrome-responsive-contract.test.js test/notes/canvas-scroll-contract.test.js test/notes/canvas-toolbar-responsive-contract.test.js test/notes/canvas-toolbar-visibility-contract.test.js
```
