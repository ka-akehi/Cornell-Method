# Cornell Method Notebook 引き継ぎ資料

作成日: 2026-07-23（JST）
対象: このチャット開始時点から PR #18 作成までのレビュー・修正・Worker 運用

この資料は、次回の担当者がチャット履歴や Worker の raw log を読み直さず、今回の設計判断・変更範囲・検証状態・残作業を再開できるようにまとめたものです。

## 1. 現在の結論

コードレビューで確認した責務・配置上の問題を、挙動を変えないリファクタリングとして整理しました。DB schema、migration、API 契約、Canvas 保存形式、Phase 2 機能は変更していません。

修正は `main`（開始時点 `bf1bc6f`）から作成した次のブランチに、問題単位で5コミットに分けて反映済みです。

- ブランチ: `agent/refactor-review-boundaries-20260723`
- 実装先端: `0447553`（この資料の docs commit は後続）
- PR: [#18](https://github.com/ka-akehi/Cornell-Method/pull/18)
- PR 状態: Open、Draft ではない
- 作業ツリー: clean
- Worker: queued / running task なし

## 2. 今回の発注者判断・作業方針

チャット中に確認された方針は次のとおりです。

- コードレビューは、過去の履歴や既存の修正量に引きずられず、責務・配置・依存方向を一歩引いて再評価する。
- 修正はリファクタリングの範囲に限定し、オーバーエンジニアリングを避ける。
- DB ファイルだけを修正対象にせず、UI、module boundary、component と logic の責務も確認する。
- prefix で機能 grouping を表す場合は、`components` 配下を機能ディレクトリに分ける。現在は Canvas / detail / editor / list の grouping が採用されている。
- `components` 配下には表示コンポーネントを置き、型・設定・Fabric bridge のような非コンポーネントは適切な `model` / `lib` / `ui/canvas` へ置く。
- `index.ts` や中間 facade は、公開境界として必要な場合だけ残す。単に別ファイルを再 export するだけの冗長 facade は削除する。
- Manager はタスクの切り分けと Worker への投入に専念し、Worker は一つの目的の task だけを実行する。
- 問題単位で1コミットとし、軽微な修正をまとめる場合でもこのブランチでは最大5コミットまでとする。
- PR は draft ではなく open 状態で作成する。

## 3. チャット開始からの経緯

### 3.1 レビュー観点の明確化

発注者から、レビューの進捗確認、コードの責務・ファイル配置・ロジック切り出しの再評価、DB 以外のファイルも含めたレビュー、命名と component 責務の確認を依頼されました。

特に次の論点が起点になりました。

- `src/shared/canvas/canvas-document.ts` のような中間ファイルが本当に必要か。
- `components` 配下に component ではない TypeScript ファイルを置く理由はあるか。
- `note-canvas`、`note-detail`、`note-editor` の prefix を使うなら機能ディレクトリに分けるべきではないか。
- `src/modules/backup/contracts/index.ts`、`src/modules/backup/remote/index.ts` の命名は適切か。
- `src/modules/backup/ui/components/backup-page.tsx` にロジックが入り過ぎていないか。
- `dto`、`remote` などの中間的な名前はディレクトリの役割で判断できるなら不要ではないか。

この方針を踏まえ、リポジトリ内の `.agents/skills/cornell-code-review` を使って、Next.js App Router、React、TypeScript、Canvas、backup、API contract、Prisma/SQLite、Target Architecture の観点でレビューしました。

### 3.2 今回の修正対象の選定

レビューで確認した問題のうち、挙動変更なしで責務境界を明確にでき、過剰設計にならないものを Worker task に切り分けました。

一方、次のような機械的・広範囲な変更は今回見送りました。

- remote の operation ファイル名を一律に変更すること。
- shared Canvas のファイル名を大量に変更すること。
- 未使用と思われる互換 facade を一括削除すること。
- `draft`、autosave、soft delete / Undo、review task、Card / D&D、PDF export など Phase 2 機能を追加すること。
- DB schema や migration を追加すること。

## 4. Worker task と結果

### 4.1 完了した task

| Task | 区分 | 結果 |
| --- | --- | --- |
| `move-notes-response-dtos-to-contracts-20260723.task.md` | Common | 完了。response DTO を `contracts` に移動。初回 Worker 起動は Codex 初期化失敗だったが、再実行で完了。 |
| `fix-backup-list-request-race-20260723.task.md` | UI | 完了。古い非同期レスポンスが最新状態を上書きしないよう guard を追加。 |
| `move-shape-text-session-out-of-hooks-20260723.task.md` | UI | 完了。Fabric の imperative session controller を `ui/hooks` から `ui/canvas` へ移動。 |
| `move-canvas-ui-contracts-out-of-model-lib-20260723.task.md` | UI | 完了。Canvas UI/runtime 型、toolbar、Fabric style bridge を `ui/canvas` に移動。 |
| `collapse-redundant-notes-barrels-20260723.task.md` | Common | 完了。冗長な Notes barrel facade を各実体の `index.ts` へ統合。 |

Worker は通常 Git commit を作成できない環境だったため、Manager が完了内容を確認し、対象ファイルだけを stage / commit しました。queue task は運用上 ignore 対象で、PR には含めていません。Worker が生成した task summary は `summary/20260723/` に残しています。

### 4.2 Worker 運用上の注意

- Common Worker の最初の DTO task は Codex 初期化時に失敗 summary を生成しました。その後の再実行で実装は完了しています。失敗 summary は履歴として保持しています。
- Canvas task では同名 summary が2件生成されています。これは重複 Worker 通知時の運用記録であり、source code の重複ではありません。
- 現在 `codex-queue/bin/worker-status.sh` で実行中・待機中 task がないことを確認済みです。
- 次回も、task 作成前に `git status --short`、`codex-queue/tasks*/queued`、`codex-queue/tasks*/running` を確認し、同じ task を再投入しないでください。

## 5. 実施した修正とコミット

### 5.1 Response DTO の配置

`src/modules/notes/remote/types.ts` に置かれていた API response DTO を `src/modules/notes/contracts/response.ts` へ移動しました。

- `remote` は fetch / transport / operation の責務に限定。
- API response の型は `contracts` が所有。
- UI、remote、server の import を更新。
- API payload、HTTP response、DB schema は変更なし。

コミット: `4d2a5b3 refactor: move note response types to contracts`

### 5.2 Backup 一覧の request race

`src/modules/backup/ui/components/backup-page.tsx` に、非同期 request の順序を管理する guard、unmount 後の state 更新防止、一覧取得中の create 操作抑制を追加しました。

- 古い GET の結果が新しい一覧を上書きする問題を防止。
- backup API の契約・ファイル名・DB 処理は変更なし。

コミット: `b9fb96c fix: guard backup list request races`

### 5.3 Shape text session の配置

`src/modules/notes/ui/hooks/shape-text-editor-session.ts` を `src/modules/notes/ui/canvas/shape-text-editor-session.ts` へ移動しました。

- React hook ではない Fabric imperative controller を hook ディレクトリから切り離した。
- lifecycle、commit / cancel、cleanup、history の挙動は維持。

コミット: `40dd6e3 refactor: move shape text session out of hooks`

### 5.4 Canvas UI 契約の配置

Canvas の UI/runtime 専用要素を `src/modules/notes/ui/canvas/` に集約しました。

- `NoteCanvasEditorProps`、style control、selected style、Fabric interaction state。
- Fabric style bridge。
- toolbar definition / type。
- Canvas runtime contract。
- Canvas UI public facade。

純粋な Notes Canvas helper は `src/modules/notes/lib/canvas-editor-types.ts` に残し、`lib` から `ui` への逆依存を作らない構造にしています。

コミット: `6d911b9 refactor: move canvas UI contracts under module UI`

### 5.5 冗長な Notes barrel の整理

次の二重 facade を廃止し、既存の実体を `index.ts` から直接 export する形にしました。

- `contracts/index.ts` → `note.schema.ts`
- `model/index.ts` → `note-editor-form.ts`
- `presenters/index.ts` → `notes.mapper.ts`
- `infrastructure/index.ts` → `command.repository.ts`

公開 export と挙動は維持し、互換 facade まで一律削除する変更はしていません。

コミット: `0447553 refactor: collapse redundant notes barrels`

## 6. 検証結果

最終ブランチに対して次を実行し、すべて成功しています。

| コマンド | 結果 |
| --- | --- |
| `npm run lint` | PASS |
| `npx tsc --noEmit --pretty false` | PASS |
| `npm run build` | PASS。Next.js 16.2.9 webpack、11 route を生成 |
| `npx prisma validate` | PASS |
| `node --test test/backup/*.test.js` | PASS。28 tests、0 failures |
| `git diff --check origin/main...HEAD` | PASS |

### 未実施の検証

次はまだ Browser QA の証跡がありません。Browser backend が利用できなかったため、静的検証や build の成功を Browser PASS と扱っていません。

- Canvas の pointer / touch 操作、重ね描き、消しゴム。
- shape text の double click、commit / cancel、cleanup。
- Canvas 保存・再読込、page resize 後の element / style / text / `searchText` 保持。
- wheel / trackpad / touch の page scroll と paper scroll。
- 375 / 768 / 1280 / 1440px の responsive toolbar、focus、ARIA。
- Backup UI の request race を含む実ブラウザ操作。

## 7. 現在の git / PR 状態

```text
base:   main @ bf1bc6f
head:   agent/refactor-review-boundaries-20260723 @ 0447553
PR:     https://github.com/ka-akehi/Cornell-Method/pull/18
state:  OPEN, isDraft=false
tree:   clean
workers: no active / queued task
```

次回開始時に PR の CI と review thread が更新されている可能性があります。まず PR #18 の checks と未解決コメントを確認し、修正が必要ならコメント単位で Worker task を作成してください。

## 8. 次回の推奨開始手順

### 8.1 最初に読む最小ファイル

1. `HANDOFF_2026-07-23.md`
2. `AGENTS.md`
3. `doc/implementation/MVP_CONTRACT.md`
4. `doc/technical/TARGET_ARCHITECTURE.md`
5. `doc/implementation/IMPLEMENTATION_STATUS.md`
6. `doc/testing/TEST_SCENARIOS.md`
7. `summary/20260723/architecture-refactor-boundary-cleanup-20260723-summary.md`
8. `summary/20260723/0627-move-notes-response-dtos-to-contracts-20260723-summary.md`
9. `summary/20260723/0629-fix-backup-list-request-race-20260723-summary.md`
10. `summary/20260723/0634-move-shape-text-session-out-of-hooks-20260723-summary.md`
11. `summary/20260723/0658-move-canvas-ui-contracts-out-of-model-lib-20260723-summary.md`
12. `summary/20260723/0708-collapse-redundant-notes-barrels-20260723-summary.md`

raw log、`.next`、生成物の大量一覧は再開時に読みません。

### 8.2 コマンド

```bash
git status --short
git branch -vv
codex-queue/bin/worker-status.sh
gh pr checks 18
gh pr view 18 --comments
```

PR review が未完了なら、先にその指摘を分類します。既存の5コミットを理由なく squash / rebase しません。

### 8.3 次の優先順位

1. PR #18 の CI / review thread を確認する。
2. 利用可能な環境で `doc/testing/TEST_SCENARIOS.md` の Canvas Browser QA を実施する。
3. runtime 問題が出た場合だけ、gesture、shape-text lifecycle、scroll、responsive、保存境界ごとに一目的 task を投入する。
4. Browser QA と PR review が終わるまで、行数だけを理由に `use-note-canvas-runtime.ts` などを追加分割しない。
5. MVP 契約を更新しないまま Phase 2 の autosave、Undo、review task、Card / D&D、PDF、タグ管理 UI を追加しない。

## 9. 受け入れ上の判断基準

- 「概ね適合」ではなく、Target Architecture の禁止依存、配置、public facade、依存方向を確認する。
- `components` には component と必要な公開 facade を置き、設定・型・renderer bridge は責務に応じた場所へ置く。
- 中間 facade は公開境界として必要な場合だけ残す。実体を一度再 export するだけの facade は増やさない。
- リファクタリングでは API payload、DB schema、Canvas JSON、CSS class、ユーザー操作を保つ。
- 静的検証、API runtime、Browser UI QA を別々の証拠として記録する。
- Worker task は一つの目的にし、完了後は summary と検証結果を確認してから Manager がコミットする。
