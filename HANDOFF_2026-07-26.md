# Cornell Method Notebook 引き継ぎ資料

作成日: 2026-07-26（JST）
対象: このチャット開始時点から、残存必須 QA、MVP クローズ、Vercel / Supabase 移行手順の文書化まで

この資料は、次回の Manager が会話履歴や Worker の raw log を読み直さず、現在の判断、確認済み範囲、未確認範囲、次の発注候補を把握して作業を再開するためのものです。詳細な実測値は個別 summary を参照し、この資料には判断に必要な結論だけを残します。

## 1. 現在の結論

- 現行 MVP の必須クローズは完了している。`AGENTS.md` §9 の受け入れ条件 4 件は `[x]` である。
- 現行 MVP の DB は、ローカル個人利用を前提に Prisma + SQLite のまま維持する。今すぐ Supabase へ移行しない。
- Vercel 上でノートの CRUD と永続保存を行う場合は、SQLite ファイルをそのまま使わず、Supabase Postgres などの外部永続 DB へ移行する。Supabase は有力候補だが唯一の選択肢ではない。
- Vercel / Supabase 移行は実装済みではない。手順だけを `doc/technical/MVP_TECHNICAL_DESIGN.md` に追記済みで、別の設計・実装・検証 task として扱う。
- このチャットの Worker task には Browser backend / app-server 権限エラーで失敗したものがある。失敗通知そのものを PASS 根拠にせず、Manager fallback の実測 summary または別の完了 summary を根拠にする。

## 2. このチャットで行ったことと発注者判断

### 2.1 残存 QA の棚卸し

- 既存 summary を確認し、重複確認と Phase 2 を除いて、現行 MVP に必須の残存 QA だけを対象にした。
- 発注者判断により、既に確認済みの 3px / 5px の再確認は行わず、厳密 4px、wheel / trackpad 固有入力、mobile edit 全体、review 成功 UI などは必須クローズ対象外に残した。
- Canvas shape text、既存ノート desktop edit、`nextReviewDate` を個別目的の task として Common queue に投入した。

### 2.2 Worker 制約と Manager fallback

- Worker の Browser backend は空状態で、app-server は `Operation not permitted` により初期化できないケースがあった。
- Worker が実測できなかった場合は、Manager 側の権限付き headless Playwright Chromium で最小限の fallback を実施した。
- fallback の実測結果は次の summary に固定し、Worker の raw log は後続入力にしない。

## 3. 必須 QA の確認結果

### Canvas shape text 必須 subset

- rect の図形内文字を編集して commit できることを確認した。
- font size と alignment の style、他要素保持、ellipse の Escape cancel、tool switch、保存・再読込、cleanup を確認した。
- 全 tool の反復 lifecycle や全保存境界を確認したわけではないため、シナリオ全体を PASS へ繰り上げていない。
- 詳細: [`summary/20260725/2230-mandatory-qa-manager-fallback-20260725.md`](summary/20260725/2230-mandatory-qa-manager-fallback-20260725.md)

### 既存ノート desktop edit

- 1280 / 1440px で既存ノートの title、noteDate、source、tag、Cue、Canvas、Summary、`nextReviewDate` を復元した。
- 編集、保存、再読込、キャンセル、主要 field 到達性、body / document の viewport-wide overflow 不在を確認した。
- 確認用ノートは DELETE `204`、削除後 GET `404`、一覧検索の残留 `totalCount=0` を確認した。console error / page error は `0` だった。
- Worker task の失敗は履歴として残し、fallback 実測を根拠にした。

### `nextReviewDate`

- `noteDate=2026-07-25` の新規初期値 `2026-08-01` を確認した。
- 手動指定 `2026-08-05` が noteDate 変更後も保持されることを確認した。
- 未設定を再読込し、noteDate 変更後も空欄を維持することを確認した。

### 必須 QA の境界

- 上記は現行 MVP の必須 subset の確認であり、すべてのテストシナリオを全体 PASS にしたものではない。
- 厳密 4px、wheel / trackpad 固有入力、375 / 768px の既存ノート mobile edit、`nextReviewDate` の review 成功 UI 反映、Canvas shape text full lifecycle は未確認として残す。
- autosave、soft-delete Undo、review task、NoteCard / D&D、PDF export、タグ管理 UI などの Phase 2 は現行 MVP runtime QA の対象外である。

## 4. MVP クローズの成果物と受け入れ条件

### 受け入れ条件

`AGENTS.md` §9 の次の 4 件を完了扱いにした。

- [x] 一覧 → 詳細 → 編集 → 保存 → 閲覧の操作デモ
- [x] `npm run lint` の成功
- [x] Prisma migrate / seed 手順の README 記載
- [x] 主要画面スクリーンショットの README 追加

### 操作デモ

- 実操作 WebM: [`doc/assets/demos/mvp-note-flow.webm`](doc/assets/demos/mvp-note-flow.webm)
- README リンク: [`README.md`](README.md) の MVP 受け入れ材料節
- デモは一覧 → 詳細 → 編集 → 保存 → 閲覧 / 再読込を実行し、cleanup と console / page error 0 も確認した。
- 元の Worker demo task は失敗したが、Manager fallback で同じ目的の WebM を作成した。失敗を PASS 根拠にはしていない。
- 詳細: [`summary/20260725/2325-mvp-flow-demo-manager-fallback-20260725.md`](summary/20260725/2325-mvp-flow-demo-manager-fallback-20260725.md)

### 同期した資料

- [`doc/implementation/IMPLEMENTATION_STATUS.md`](doc/implementation/IMPLEMENTATION_STATUS.md)
- [`doc/implementation/MVP_CONTRACT.md`](doc/implementation/MVP_CONTRACT.md)
- [`doc/testing/TEST_SCENARIOS.md`](doc/testing/TEST_SCENARIOS.md)
- [`README.md`](README.md)
- この資料の旧版から更新した現行 HANDOFF

## 5. Vercel / Supabase 移行の現在地

### 現状

- 現在の schema は `provider = "sqlite"` である。
- Prisma runtime は `PrismaBetterSqlite3` を使っている。
- `config/project-env.js` と `prisma.config.ts` は `file:` SQLite URL を前提にしている。
- backup provider は SQLite `.db` ファイルのコピーを前提にしている。
- したがって、Vercel の環境変数へ Postgres URL を設定するだけでは動かない。

### 文書化済みの移行手順

- [`doc/technical/MVP_TECHNICAL_DESIGN.md:164`](doc/technical/MVP_TECHNICAL_DESIGN.md#vercel公開時の実施手順) に実施順序を追記した。
- 内容は、公開目的の決定、Supabase project、runtime / migration 接続の分離、Prisma adapter / URL 解決 / backup の変更、migration、既存 `dev.db` の移行または空 DB 開始、Preview / Production、認証、完了条件、ロールバックである。
- `DATABASE_URL` と `DIRECT_URL` はプレースホルダーとしてのみ扱い、実際の秘密情報は資料へ書かない。
- Vercel 本番保存を実装するまでは、ローカル SQLite MVP と Supabase 移行作業を混ぜない。

### 次に Vercel を進める場合の候補

1. Supabase Postgres を第一候補にするか、Neon / Turso 等を比較する。
2. DB provider / Prisma adapter / URL 解決 / migration / data import / backup-export を一つの設計・実装計画に分ける。
3. 既存 `dev.db` を移行するか、空 DB から始めるかを決める。
4. 個人ノートを保護する認証または Vercel Deployment Protection を決める。
5. Preview で CRUD、Canvas 復元、検索、削除、必要なら PDF / export と backup / restore を確認してから Production へ進める。

Vercel 移行は、発注者が公開を進めると決めた時点で、仕様確認 task、実装 task、runtime 検証 taskを分離して Common / API / UI queue へ投入する。まず `DATABASE_URL` だけを差し替える task は作らない。

## 6. 現在の Git / Worker 状態

この資料の作成開始前に確認した値:

- branch: `main`
- latest commit: `c5cb125 add`
- 作成開始前の worktree: `git status --short` に出力なし
- queue: 2026-07-26 01:32 JST の `worker-status` で全 320 task 処理済み、active worker 0
- 今回の handoff Worker task は app-server permission error で failed。fallback でこの資料を作成する。

この資料の作成後は、`HANDOFF_2026-07-26.md`、`AGENTS.md`、自動 summary が未コミット変更として出る可能性がある。次回は推測せず `git status --short` を再実行する。

## 7. 次回の最小再開手順

1. `git status --short` で未コミット変更とユーザー変更を確認する。
2. `git branch -vv` と `git log -1 --oneline` で branch / commit を確認する。
3. `codex-queue/bin/worker-status.sh` で queued / running / done / failed を現在値として確認する。
4. [`HANDOFF_2026-07-26.md`](HANDOFF_2026-07-26.md) を読む。
5. 方針判断が必要な場合だけ、次を読む。
   - [`AGENTS.md`](AGENTS.md)
   - [`doc/implementation/MVP_CONTRACT.md`](doc/implementation/MVP_CONTRACT.md)
   - [`doc/implementation/IMPLEMENTATION_STATUS.md`](doc/implementation/IMPLEMENTATION_STATUS.md)
   - [`doc/testing/TEST_SCENARIOS.md`](doc/testing/TEST_SCENARIOS.md)
   - [`doc/technical/MVP_TECHNICAL_DESIGN.md`](doc/technical/MVP_TECHNICAL_DESIGN.md)
6. 必須 MVP の再確認が必要になった場合は、まず [`summary/20260725/2230-mandatory-qa-manager-fallback-20260725.md`](summary/20260725/2230-mandatory-qa-manager-fallback-20260725.md) を読み、確認済み範囲を再利用する。
7. Vercel を進める場合は [`summary/20260726/0009-document-vercel-supabase-migration-procedure-20260726-2df1bad7-summary.md`](summary/20260726/0009-document-vercel-supabase-migration-procedure-20260726-2df1bad7-summary.md) と技術設計書の移行手順を起点にする。
8. raw log、`.next`、大量の生成物一覧を後続入力にしない。summary の `Next Read` と本資料の最小手順を使う。

## 8. 引き継ぎ資料の検証方針

- 変更後は `git diff --check` を実行する。
- summary を後続入力にする場合は `sh tools/check-summary.sh <summary>` を実行する。
- 新しい HANDOFF を作成した場合は `AGENTS.md` の最新リンクを更新し、旧 HANDOFF を残さない。
- docs 以外の変更が必要になったら、仕様 / 調査 task と coding task を分離する。
- Worker の失敗は環境制約の証跡として保持し、PASS判定は実測結果・完了summary・受け入れ条件の組み合わせで行う。

## 9. 主な根拠 summary

- [`summary/20260725/2230-mandatory-qa-manager-fallback-20260725.md`](summary/20260725/2230-mandatory-qa-manager-fallback-20260725.md)
- [`summary/20260725/2325-mvp-flow-demo-manager-fallback-20260725.md`](summary/20260725/2325-mvp-flow-demo-manager-fallback-20260725.md)
- [`summary/20260725/2327-finalize-mvp-acceptance-materials-20260725-1d32f0ee-summary.md`](summary/20260725/2327-finalize-mvp-acceptance-materials-20260725-1d32f0ee-summary.md)
- [`summary/20260725/2335-sync-mvp-final-handoff-20260725-1316c5e5-summary.md`](summary/20260725/2335-sync-mvp-final-handoff-20260725-1316c5e5-summary.md)
- [`summary/20260726/0009-document-vercel-supabase-migration-procedure-20260726-2df1bad7-summary.md`](summary/20260726/0009-document-vercel-supabase-migration-procedure-20260726-2df1bad7-summary.md)
- [`summary/20260726/0131-create-handoff-20260726-59762a2b-summary.md`](summary/20260726/0131-create-handoff-20260726-59762a2b-summary.md)（Worker failed、Manager fallbackへ切替した履歴）
