# Cornell Method Notebook 引き継ぎメモ

作成日: 2026-07-03

## 目的

このメモは、2026-07-02 から 2026-07-03 にかけて進めた現状整理、AI 関連の将来構想整理、ワークフロー図の開始ノード修正、SVG 再生成の注意点を、次回作業へ引き継ぐためのものです。

次回はこのファイルを起点に、`CURRENT_STATUS.md` と関連 summary を最小限読んで再開してください。

## 次回最初に読むもの

| ファイル | 役割 |
| --- | --- |
| `AGENTS.md` | 仕様の正本、Manager / Worker 方針、最新引き継ぎ参照 |
| `CURRENT_STATUS.md` | 現コードと設計済み範囲の差分をまとめたリポジトリ直下サマリ |
| `doc/requirements/CORNELL_METHOD_SPEC_REVIEW.md` | Cornell Method 観点の仕様レビュー、AI 関連の将来構想 |
| `doc/workflows/MVP_WORKFLOW_DESIGN.md` | MVP 業務フロー、今回開始ノードを修正した正本 |
| `doc/workflows/assets/mmd/mvp-workflow-design-01-diagram.mmd` | ノート作成フローの Mermaid source |
| `doc/workflows/assets/mmd/mvp-workflow-design-02-diagram.mmd` | 復習対象確認フローの Mermaid source |
| `summary/20260702/1848-current-status-root-summary-944f6325-summary.md` | `CURRENT_STATUS.md` 作成タスク |
| `summary/20260702/2224-document-ai-quiz-design-decisions-2fc54865-summary.md` | AI クイズ設計決定の記録 |
| `summary/20260702/2237-document-ai-cue-suggestion-backlog-c573a7f2-summary.md` | AI Cue 候補提案の記録 |
| `summary/20260702/2340-restore-workflow-start-node-labels-b019eaea-summary.md` | ワークフロー開始ノード修正タスク |
| `summary/20260702/2346-regenerate-workflow-svg-from-restored-start-labels-b0bcc642-summary.md` | SVG 再生成タスク。ただし内容妥当性は未確認だったため注意 |

## 現在の大きな状態

### 設計と実装の現在地

`CURRENT_STATUS.md` をリポジトリ直下に作成済みです。

主な結論:

- `AGENTS.md` は最終仕様の正本です。
- 現コードは最終仕様より MVP 寄りです。
- `doc/implementation/IMPLEMENTATION_STATUS.md` は現コードより進んだ内容を実装済みとして書いている可能性があるため、実装済み判断の根拠にしないでください。
- 実装済み確認は必ず `src/app/**`, `src/lib/**`, `prisma/schema.prisma`, `scripts/**` の現コードで裏取りしてください。

現コードで確認済みの大枠:

- Next.js App Router + React + Prisma + SQLite 構成。
- `Notebook`, `Tag`, `NotebookTag`, `Cue` の MVP 寄り schema。
- `/notes`, `/notes/new`, `/notes/[id]`, `/backup`。
- `GET/POST /api/notes`, `GET/PATCH/DELETE /api/notes/:id`, `POST /api/notes/:id/review`, `GET /api/tags`, `GET/POST /api/backups`。
- Markdown は textarea + `react-markdown` preview。
- 復習は詳細画面内の本文非表示モードと `nextReviewDate` / `reviewedAt` ベース。

未実装または未確認の大枠:

- `NotebookDraftState`, `NotebookReviewProgress`, `SoftDeleteBuffer`, `BackupLog`。
- `CueCard`, `NoteCard`, `NoteCueLink`。
- 自動保存、409 楽観ロック、Undo Snackbar、`POST /api/undo`。
- `/tasks/review` と復習タスク API。
- PDF エクスポート。
- `/notes/backup`。
- D&D、ショートカット、`@uiw/react-md-editor`, `react-day-picker` range mode。

### 次の重要分岐

次に進める前に、以下のどちらを優先するか決める必要があります。

| 選択肢 | 内容 | Manager 推奨 |
| --- | --- | --- |
| A: MVP 安定化 | 現 schema / 現 UI を前提に、`doc/testing/TEST_SCENARIOS.md` の MVP 項目を通す | 推奨 |
| B: 最終仕様へ拡張 | Draft / Undo / ReviewProgress / NoteCard / PDF などを `AGENTS.md` に合わせて追加する | MVP 安定後 |

理由: 現コードと最終仕様の差が大きいため、いきなり Phase 2 相当へ進むと migration、UI、API、テストの手戻りが大きいです。

## AI 関連の決定事項

AI 関連は MVP / 既存 Phase 2 の実装対象ではなく、保留案・将来構想として整理済みです。

### AI クイズ生成

配置:

- 復習タスク画面ではなく、ノート詳細の復習モード内で解く想起支援機能。
- 復習タスク画面は、今日復習すべきノートへ誘導する入口。

Cornell Method との整合:

- 復習時は左側の Cue / キーワード / 質問を表示し、右側本文を隠す。
- AI クイズは左側 Cue を出題軸・ヒントとして使い、右側本文やサマリーの理解を問う。
- 左側に見えている語句そのものが答えになる問題は極力避ける。

クイズ形式:

- 一問一答と穴埋めを主軸にする。
- 答えが長い場合に限り、選択式も余地あり。
- 採点ではなく、正誤判定を行う。
- 解答後に答えと解説を表示する。

生成量:

- 1 セット 5〜10 問は「1 回の復習で出題される量」。
- クイズ生成自体はいくつでも可能。
- 同じ答えでも、問題文が異なる候補が生成されるのは許容。

生成・保存:

- 初期はユーザーが「クイズ生成」ボタンを押したときにオンデマンド生成する。
- 最初は保存しない方針で十分。
- 将来 UX を重視する場合は、クイズ候補プールを保存する。
- 保存する場合も同じクイズの固定再利用ではなく、生成を続けて候補を増やし、復習時にランダムまたは条件付きで 5〜10 問を出題する。
- 保存後バックグラウンド生成は、Vercel など外部展開やジョブ基盤を考える段階で再評価する。

実現方式:

- 外部 API は使わない。
- Rust API 実装が完了した後、Rust 側で Local LLM を扱う方針を第一候補にする。
- 初期はモデル自体を学習させず、ノート、Cue、サマリー、生成クイズ、復習結果を蓄積し、Local LLM に渡す知識ベースを育てる。
- 十分なデータが蓄積された段階で、追加学習 / fine-tuning に挑戦する余地を残す。

### AI Cue / キーワード / 質問の不足候補提案

位置づけ:

- ノート整理時または復習前の整理支援。
- AI クイズとは別の将来構想。

体験方針:

- ユーザーが先に本文、サマリー、Cue / キーワード / 質問を書く。
- AI はその後に、本文・サマリー・既存 Cue を見て、不足していそうな候補を提案する。
- AI は左側を自動入力しない。

候補対象:

- 重要概念。
- 問いに変換できる論点。
- 既存 Cue と重複しないキーワード。

操作:

- `追加`
- `編集して追加`
- `無視`

理由:

- Cornell Method では、ユーザー自身が重要点を選び、左側に整理する行為自体が学習体験として重要なため。

## ワークフロー図の修正状況

ユーザー指摘:

- ノート作成から復習予定化までのフロー図は `/notes/new を開く` から始まる方がよい。
- 復習対象確認から復習済み更新までのフロー図は `/notes を開く` から始まる方がよい。
- 途中で SVG が Mermaid ノード ID 表示になったり、古い開始ノードへ戻ったりした。

現在の正しい状態:

- `doc/workflows/MVP_WORKFLOW_DESIGN.md`
  - 図 1: `Start["/notes/new を開く"] --> InputCore["タイトル・学習日・本文を入力"]`
  - 図 2: `Start["/notes を開く"] --> Filter["復習対象フィルタを有効化"]`
- `doc/workflows/assets/mmd/mvp-workflow-design-01-diagram.mmd`
  - 同上。
- `doc/workflows/assets/mmd/mvp-workflow-design-02-diagram.mmd`
  - 同上。
- `doc/workflows/assets/svg/mvp-workflow-design-01-diagram.svg`
  - `/notes/new を開く` と `タイトル・学習日・本文を入力` を含む。
- `doc/workflows/assets/svg/mvp-workflow-design-02-diagram.svg`
  - `/notes を開く` と `復習対象フィルタを有効化` を含む。
- 古い SVG ラベル `学習完了` / `復習作業を開始` は対象 SVG から消えています。

検証済みコマンド:

```bash
npm run diagrams:svg
rg -n '学習完了|復習作業を開始' doc/workflows/assets/svg/mvp-workflow-design-01-diagram.svg doc/workflows/assets/svg/mvp-workflow-design-02-diagram.svg
rg -n '/notes/new を開く|タイトル・学習日・本文を入力' doc/workflows/assets/svg/mvp-workflow-design-01-diagram.svg
rg -n '/notes を開く|復習対象フィルタを有効化' doc/workflows/assets/svg/mvp-workflow-design-02-diagram.svg
rg -n 'class="flowchart"|flowchart-v2' doc/workflows/assets/svg/mvp-workflow-design-01-diagram.svg doc/workflows/assets/svg/mvp-workflow-design-02-diagram.svg
```

結果:

- `npm run diagrams:svg`: 成功。`Rendered 23 SVG diagrams.`
- 古いラベル検索: 一致なし。
- 期待ラベル検索: 一致あり。
- Mermaid 公式形式: `class="flowchart"` / `flowchart-v2` あり。

注意:

- `npm run diagrams:svg` は 23 SVG 全体を再生成します。
- 今回の全体再生成により、対象 2 SVG 以外に `doc/diagrams/assets/svg/mvp-er-diagram-01-notebook-cue-tag-notebooktag.svg` も差分に出ています。

## 依存関係と SVG 生成環境

`regenerate-workflow-svg-from-restored-start-labels-b0bcc642` は `done` になりましたが、summary 上は変更ファイルなしで、内容妥当性も未確認でした。

その後 Manager 側で直接確認したところ、以下の問題がありました。

- `node_modules` に `playwright` がなく、`npm run diagrams:svg` が `Cannot find module 'playwright'` で失敗。
- `npm install` は最初、ネットワーク制限で `registry.npmjs.org` に到達できず失敗。
- ネットワーク許可付き `npm install` で依存復元。
- Playwright Chromium が未導入で `Executable doesn't exist` になったため、`npx playwright install chromium` を実行。
- その後 `npm run diagrams:svg` は成功。

今後、別環境で SVG を再生成する場合は以下が必要です。

```bash
npm install
npx playwright install chromium
npm run diagrams:svg
```

`npm install` 後の注意:

- `npm audit` で moderate 3 件の警告が出ています。
- 今回は SVG 再生成のため依存復元を優先し、audit 対応は未実施です。

## Worker / Manager 運用上の注意

今回の重要な教訓:

- Worker task が `done` でも、成果物の内容妥当性は Manager が確認する必要があります。
- summary に `生成物の内容妥当性はこの summary ではレビューしていない` とある場合は、必ず対象ファイルを直接確認してください。
- SVG 生成のような成果物更新は原則 Worker task 化します。ただし今回は複数回 Worker で不整合が残ったため、Manager が最小限の直接生成と検証を行いました。
- 次回以降も、通常は Manager が直接実装・生成物修正を抱え込まず、Worker task に切ってください。

## 現在の主な差分

`git status --short` で確認された主な差分:

- `AGENTS.md`
  - AI クイズ生成、AI Cue 候補提案の将来構想を追記。
  - 最新引き継ぎ参照をこのファイルへ更新。
- `CURRENT_STATUS.md`
  - リポジトリ直下の現状サマリとして新規作成。
- `doc/requirements/CORNELL_METHOD_SPEC_REVIEW.md`
  - AI クイズ生成と AI Cue 候補提案の設計論点を追記。
- `doc/workflows/MVP_WORKFLOW_DESIGN.md`
  - 代表フロー図と開始条件を `/notes/new を開く` / `/notes を開く` 起点へ修正。
  - 一部 Markdown table が整形されています。
- `doc/workflows/assets/mmd/mvp-workflow-design-01-diagram.mmd`
  - 開始ノードを `/notes/new を開く` に修正。
- `doc/workflows/assets/mmd/mvp-workflow-design-02-diagram.mmd`
  - 開始ノードを `/notes を開く` に修正。
- `doc/workflows/assets/svg/mvp-workflow-design-01-diagram.svg`
  - `.mmd` と一致する SVG に再生成。
- `doc/workflows/assets/svg/mvp-workflow-design-02-diagram.svg`
  - `.mmd` と一致する SVG に再生成。
- `doc/diagrams/assets/svg/mvp-er-diagram-01-notebook-cue-tag-notebooktag.svg`
  - `npm run diagrams:svg` の全体再生成に伴う差分。
- `summary/20260702/*`
  - Worker task の完了 summary 群。
- `wezterm.lua`
  - 既存またはユーザー由来の変更。今回の作業対象外なので触らないこと。

## 次に検討するタスク候補

### 1. MVP 安定化方針の決定

目的:

- 次を MVP 安定化で進めるか、最終仕様拡張へ入るかをユーザーと決める。

推奨:

- MVP 安定化。

完了条件:

- `CURRENT_STATUS.md` の分岐点 A / B のどちらで進めるか決定。
- 決定内容を `CURRENT_STATUS.md` または設計 docs に反映する Worker task を作る。

### 2. `IMPLEMENTATION_STATUS.md` の現コード同期

目的:

- 現コードより進んだ実装済み記述を是正し、後続 Worker が誤認しないようにする。

対象:

- `doc/implementation/IMPLEMENTATION_STATUS.md`
- 必要に応じて `doc/implementation/MVP_IMPLEMENTATION_TASKS.md`

制約:

- 調査・ドキュメント同期タスク。コード変更なし。

### 3. SVG 全体再生成差分の確認

目的:

- `doc/diagrams/assets/svg/mvp-er-diagram-01-notebook-cue-tag-notebooktag.svg` の差分が Mermaid 再生成の自然差分か確認する。

対象:

- `doc/diagrams/assets/svg/mvp-er-diagram-01-notebook-cue-tag-notebooktag.svg`
- 対応 `.mmd`

完了条件:

- 意図しない内容変更でなければ採用。
- 不要差分なら、ユーザー確認後に戻すかどうか判断。

### 4. 依存関係 / audit の棚卸し

目的:

- `npm install` 後の moderate 3 件を調査し、対応要否を判断する。

制約:

- まずは調査タスク。`npm audit fix` は別判断にする。

## 再開時の推奨手順

1. `git status --short` を確認する。
2. `HANDOFF_2026-07-03.md` を読む。
3. `CURRENT_STATUS.md` を読む。
4. ユーザーに MVP 安定化か最終仕様拡張かを確認する。
5. 必要な Worker task を 1 目的 1 task file で作成する。

確認コマンド例:

```bash
git status --short
rg -n 'Start\\["/notes/new を開く"\\]|Start\\["/notes を開く"\\]' doc/workflows/MVP_WORKFLOW_DESIGN.md doc/workflows/assets/mmd
rg -n '学習完了|復習作業を開始' doc/workflows/assets/svg/mvp-workflow-design-01-diagram.svg doc/workflows/assets/svg/mvp-workflow-design-02-diagram.svg
npm run diagrams:svg
```

期待:

- `.md` / `.mmd` は `/notes/new を開く` と `/notes を開く` 起点。
- 古い SVG ラベルは一致なし。
- `npm run diagrams:svg` は `Rendered 23 SVG diagrams.` で成功。
