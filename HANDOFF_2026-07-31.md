# Cornell Method Notebook 引き継ぎ資料

作成日: 2026-07-31（JST）
対象: GitHub Issue / PR のレビュー指摘対応、元 PR ブランチへの修正反映、リモートブランチ整理、develop への差分統合、依存関係復旧、検証結果

この資料は、次回の Manager / Worker が会話ログや raw log を読み直さずに再開できるよう、確定事実・判断理由・未確認事項・最小の再開手順をまとめたものです。秘密情報、接続 URL の実値、raw log、長大な command output は記録しません。

## 1. 現在地

### 1.1 Git / remote の基準状態

資料作成開始時点で確認した基準状態は次のとおりです。

| 項目 | 確定値 |
|---|---|
| 現在の branch | `develop` |
| `HEAD` / `origin/develop` | `4f67c78 chore: archive task summaries` |
| `origin/main` | `79dfd13 Merge develop into main (#64)` |
| origin に残る remote branch | `origin/main`、`origin/develop` のみ（`origin/HEAD -> origin/main`） |
| 作業ツリー | 作業開始時は clean |

この handoff 作成時点では、Manager が commit するまで対象資料の差分が未コミットで残る状態でした。その後の 2026-07-31 evidence task では `summary/20260731/` に runtime / source-reader summary が追加され、今回の docs-only 追補も未コミットで残ります。アプリコード、テストコード、設定、依存関係、DB、生成物、queue の task state は今回変更しません。

ローカルには過去の `agent/*` branch ref が残る場合がありますが、remote の整理結果は上表を正とします。

### 1.2 develop の直近コミット

| commit | 内容 |
|---|---|
| `4f67c78` | `chore: archive task summaries`。作業経緯を保存する summary 68 件を develop に記録 |
| `4730523` | `docs: document Cue roundtrip E2E coverage`。Cue の作成後 GET による本文・順序復元検証を `doc/testing/TEST_SCENARIOS.md` に反映 |
| `79dfd13` | `Merge develop into main (#64)`。PR #41、#47、#49、#50、#51 などを含む main 側の統合点 |

`4f67c78` は `git diff-tree` で 68 ファイルを含むことを確認済みです。これらは Worker / Manager の完了要約であり、製品コードの代替ではありません。

## 2. 7/30 から 7/31 までの作業経緯

### 2.1 旧 handoff からの引き継ぎ

旧 `HANDOFF_2026-07-30.md` は、`agent/fix-date-editor-scroll-20260727`、HEAD `34fa64e`、PR #51 の clean checkout という古い境界で書かれていました。そこに記録されていた UI polish、E2E、Postgres migration fallback の多くは、その後 PR #47 / #49 / #50 / #51 と PR #64 の main 統合へ反映済みでした。

旧資料の未確認事項をそのまま現行 fact に昇格させず、GitHub の Issue / PR 状態と current diff を再確認してから、以下の修正・統合を進めました。

### 2.2 GitHub の未対応 Issue / PR の確認と Worker task

Manager は open Issue と既存 PR のレビュー指摘を確認し、既に develop / PR に反映済みのものと、追加対応が必要なものを分離しました。追加対応の対象は #52、#57、#58、#59、#62 です。#48、#53、#54、#55、#56 は既存反映済みと判断し、新たな修正は投入していません。

| Issue | Worker task の完了内容 |
|---|---|
| #62 | 日付入力の `showPicker()` / focus fallback を維持しつつ、label の標準 activation を壊さないよう修正 |
| #59 | 作成後に `GET /api/notes/:id` を行い、Cue の `text` と `order` の roundtrip を E2E で確認 |
| #58 | `playwright` と `@playwright/test` をともに 1.61.0 に固定し、lockfile を同期 |
| #52 / #57 | Playwright の server teardown 完了後に fixture cleanup を行う順序を修正。SQLite の DB / sidecar cleanup を維持 |
| #52 / #57 | migration、既存 server 検出、child spawn、readiness failure の setup failure 経路でも cleanup するよう補強 |

初回の #52 / #57 cleanup Worker は app-server sandbox の `Operation not permitted` で失敗し、権限付き Worker で再実行しました。これはコード原因の確定ではなく、Worker 実行環境の制約として扱っています。レビュー thread の reply / resolve 操作は行っていません。

### 2.3 誤った branch への集約 commit と復旧

作業途中、複数の修正を誤って旧 PR #41 の head branch `agent/fix-date-editor-scroll-20260727` へ集約し、commit `f8e1368` として push しました。これは元 PR の変更境界を汚し、重複 commit やレビュー対象の混同を招くため、正しい PR branch へ戻す必要がありました。

- UI 修正は元の PR #47 branch `agent/ui-editor-polish-20260730` へ commit `78b94e0` として反映。
- E2E 修正は元の PR #49 branch `agent/e2e-mvp-flow-20260730` へ commit `822083f` として反映。
- 反映確認後、誤った集約 commit `f8e1368` を旧 branch の local / remote から除去し、PR #41 branch を元の `34fa64e` に戻した。

この復旧では、旧 dirty worktree の未コミット差分を破棄せず、main に既に存在する変更を再適用して重複させないことを優先しました。

### 2.4 PR merge と remote branch の整理

PR #47 / #49 の修正反映後、両 PR は merge 済みになりました。PR #64 の `develop` → `main` merge 後には remote の `develop` が一度自動削除され、不要になった PR branch と worktree metadata、再生成可能な E2E report / result を整理しました。

その後、current diff の統合先として `develop` を再作成 / 更新し、現在の `origin/develop` を push しました。Open PR のない旧 `agent/fix-date-editor-scroll-20260727` と、Closed PR #61 に対応していた `agent/fix-codex-review-sync-race-develop` も remote から削除しました。最終的な origin の branch は `main` と `develop` だけです。

## 3. current diff の棚卸しと develop への統合

旧 dirty worktree の差分を `origin/main` と比較し、次のように分類しました。

- UI polish、Markdown input / preview、tag validation、Cornell divider、Postgres native fallback、E2E cleanup などの大半は `origin/main` に同一内容が存在した。
- `test/notes/editor-metadata-contract.test.js` は main 側に追加された date picker / label activation test を保持する必要があり、旧 worktree のファイル全体で上書きしないと判断した。
- unique な差分は、実際の E2E が行っている Cue `text` / `order` roundtrip を説明する `doc/testing/TEST_SCENARIOS.md` の 1 文だった。
- summary は運用記録として保全し、製品コードの修正や PR の close reference と混同しないようにした。

発注者の明示指示に従い、current diff を develop に取り込みました。main に既にある実装は重複 commit せず、develop の起点から不足していた Cue roundtrip の文書差分を `4730523` としてコミットし、Worker / Manager summary 68 件を `4f67c78` としてアーカイブしました。これにより、差分の棚卸し結果を残しつつ、次回作業者が raw log なしで経緯を追える状態にしています。

## 4. 依存関係の一時障害と復旧

branch / worktree の整理中に依存ディレクトリを処理した影響で、`node_modules` が一時的に空または不整合になり、`npm run dev` が Next.js / Prisma の実行に必要な依存を解決できず起動できない状態になりました。これはアプリコードや route の欠落ではなく、作業中の依存ディレクトリ状態が原因です。

次の操作で復旧しました。

1. `npm ci --no-audit --no-fund` で lockfile に基づく依存関係を再構築。
2. `npm run prisma:generate` で SQLite / Postgres 用 Prisma Client を再生成。
3. `npm run dev` 起動後、`/notes` が HTTP 200 になることを再確認。

復旧後の `npm run lint`、TypeScript、unit / contract、build も下記のとおり成功しています。`node_modules` は commit 対象外です。

## 5. 実施済み検証

| 確認 | 結果 | 備考 |
|---|---|---|
| `npm run lint` | PASS | 依存復旧後に再確認 |
| `npx tsc --noEmit --pretty false` | PASS | TypeScript 型検証 |
| `node --test test/notes/*.test.js test/postgres/*.test.js test/e2e-cleanup-contract.test.js` | PASS、68 tests | UI / Markdown / note-paper / Postgres / E2E cleanup contract |
| `npm run build` | PASS | Prisma generate を含む Next.js production build |
| `npm run test:e2e` | 権限付き runtime で PASS、3 tests | isolated `prisma/e2e.db` を使う主要 MVP flow。終了後の DB / sidecar cleanup も確認済み |
| `npm run test:e2e` の本 Worker 環境での再試行 | BLOCKED | sandbox の localhost bind が `127.0.0.1:4173` で `EPERM`。E2E 実装不備の根拠にはしない |
| `git diff --check` | PASS | current diff の whitespace check |
| `sh tools/check-summary.sh ...` | PASS | archived Worker / Manager summary の必須見出し・conflict marker を確認 |
| `npm run dev` + `GET /notes` | PASS | 依存復旧後、HTTP 200 を確認 |

E2E の PASS は、localhost bind が許可された権限付き runtime での結果です。本 Worker の sandbox では同じ server を起動できなかったため、現在環境での再現結果と混同しないでください。unit / contract の PASS や E2E の PASS を、ブラウザの視覚 QA の PASS へ読み替えてはいけません。

### 5.1 2026-07-31 追加 evidence

今回の追加 evidence は、過去の desktop / Canvas subset の履歴を置き換えず、実測できた範囲と blocker を分離して記録します。

| 対象 | 判定 | 確認できた範囲 / 未確認範囲 | 根拠 |
|---|---|---|---|
| Canvas scroll / wheel / touch handoff と scroll 中 drawing | `BLOCKED` | Browser backend の `agent.browsers.list()` は `[]`。localhost route は HTTP 000、新規 server bind は `listen EPERM` で、375 / 768 / 1280px の scroll metrics、wheel / trackpad、touch / pointer、scroll 中の要素増減・既存 geometry / points / style / text / `searchText`、保存・再読込は未取得。静的な scroll wrapper / cancel handler は runtime PASS ではない。 | `summary/20260731/worker-canvas-scroll-wheel-touch-qa-20260731.md` |
| 375 / 768px mobile note editor・viewer・review・overflow | `BLOCKED` | Browser backend `[]`、専用 4173 server bind `EPERM`、既定 headless executable 不在、system Chrome 終了、headless shell の MachPort permission error により page/session を作成できなかった。既存 3000 番 curl 200 は HTTP 到達性の一部確認に留まり、visual / interaction / console 証跡はない。 | `summary/20260731/worker-mobile-note-runtime-20260731.md` |
| Postgres source reader native failure fallback | `PASS（isolated evidence の範囲）` | frozen SQLite fixture に require / constructor の `ERR_DLOPEN_FAILED` を temporary injection し、`/usr/bin/sqlite3` CLI fallback、normal native との row digest / count 比較、Canvas validation、source bytes / SHA-256 / WAL / SHM 不変、temporary cleanup を確認。targetless reconcile は target config 不足で Postgres 接続前に exit `1`。 | `summary/20260731/worker-postgres-native-reader-fallback-20260731.md`、`summary/20260731/1804-recheck-postgres-native-reader-fallback-evidence-20260731-d5caeaf3-summary.md` |

Postgres の `PASS` は isolated frozen SQLite fixture に対する reader fallback evidence のみを意味する。実際の壊れた native binary / operator machine packaging、実 Postgres target との baseline / row reconcile、production / hosted readiness は未確認である。

### 5.2 ノート一覧ライブ検索 UI の文書同期

2026-07-31 の UI decision と現行 source / focused contract test を照合し、`/notes` の現行仕様を画面設計・棚卸し・テスト観点・実装状況へ同期した。

- visible な `検索` button は置かない。query は入力停止から 300ms 後、Enter は pending debounce を取り消して即時、日付・タグ追加 / 削除・review toggle は最新 query を含む全条件で即時検索する。
- `クリア` は pending debounce を取り消し、全条件と review toggle を初期化して即時検索する。From > To は request 前に拒否し、日付 blur は validation のみを行う。
- 文書同期中に後続 UI task `remove-review-toggle-on-off-label-7693ff1d` が完了し、ユーザーの最新判断として visible な `ON` / `OFF` badge は削除された。`復習対象のみ` だけを visible label とし、非選択時の neutral style、選択時の amber style、`aria-pressed` を持つ keyboard-operable toggle button を維持する。desktop ではタグチップ増加時もタグ操作行の位置を保つ。
- header は `h1` と新規作成導線を維持し、冗長な補助文を表示しない。一方、文書内の「画面の主目的」は UI copy ではないため設計説明として保持した。
- `doc/screens/MVP_UI_WIREFRAMES.md` にリンクされた 2026-07-08 の NTE-010 PNG は旧 checkbox / Search button を含む historical visual reference と明記し、画像自体は変更していない。

この docs-only task の変更文書は `doc/screens/MVP_UI_WIREFRAMES.md`、`doc/screens/MVP_SCREEN_DESIGN.md`、`doc/screens/MVP_SCREEN_INVENTORY.md`、`doc/testing/TEST_SCENARIOS.md`、`doc/implementation/IMPLEMENTATION_STATUS.md`、本 handoff の 6 件。コード、test、設定、依存関係、生成物、画像、summary はこの task から変更していない。並行して完了した後続 UI task の `filters.tsx` / layout contract test 差分と summary は別 task の成果物として保持した。

静的検証は focused source-contract test 5 件、`npm run lint`、`npx tsc --noEmit --pretty false`、`npm run build`、`git diff --check` が PASS。Browser visual / interaction runtime は未確認であり、300ms の実時間、Enter / toggle の実ブラウザ操作、loading 遷移、responsive alignment、header の視覚状態を runtime PASS とは扱わない。既存の 2026-07-31 runtime / Postgres evidence とその判定境界は変更していない。

## 6. 未実施・残課題

以下は未確認であり、実施済みとは扱いません。

- Browser backend を使った視覚 / pointer / wheel QA。用紙余白、Cornell divider、長い tag、date picker と label、Cue / Summary の入力枠、Cue / Canvas の scroll handoff は未確認です。2026-07-31 の Canvas scroll / wheel / touch follow-up は Browser backend `[]`、localhost route 到達不可、server bind `EPERM` で `BLOCKED` でした。7/25 の desktop / Canvas subset は別の確認済み履歴として保持します。
- 375 / 768px の mobile note editor、既存 note edit、viewer、review、長い入力・validation overflow、console / page error は未確認です。2026-07-31 の試行は Browser backend `[]` と headless Chromium 起動制約により `BLOCKED` でした。
- 実際の壊れた SQLite native binary / operator machine packaging の failure smoke、実 Postgres target との baseline / row reconcile は未確認です。isolated frozen SQLite fixture への require / constructor failure injection → `sqlite3` CLI fallback、read-only snapshot、row digest、Canvas validation、source hash / size / sidecar 不変、targetless reconcile の未接続は `PASS（isolated evidence）` として確認済みです。
- GitHub Actions / CI 上の E2E、Chromium install cache の確認。
- Vercel / Supabase 等の hosted deployment、env fail-closed、Basic Auth、migration、既存 SQLite import、再デプロイ後の永続性、backup / restore drill。
- PDF / HTML export、起動時自動 backup、Undo / soft delete など、MVP 契約外または Phase 2 以降の機能。これらは今回の Issue / PR 対応の未確認項目ではありません。

現時点で GitHub の対象 Issue に対する Worker 修正と local 検証は完了しています。残課題は runtime / hosted 環境の確認であり、実環境への変更は対象・接続情報・実施許可を分けて判断してください。

## 7. Next Read / 最小再開手順

### Next Read

最初に次の順で読みます。

1. `HANDOFF_2026-07-31.md`
2. `AGENTS.md`
3. `doc/implementation/MVP_CONTRACT.md`
4. `doc/implementation/IMPLEMENTATION_STATUS.md`
5. `doc/testing/TEST_SCENARIOS.md`
6. `summary/20260731/current-diff-develop-inventory.md`
7. Issue / E2E の継続時だけ `summary/20260731/manager-fix-open-issues-20260731.md`、`e2e/web-server.js`、`e2e/global-teardown.js`、`e2e/mvp-note-flow.spec.js`
8. UI QA の継続時だけ `src/app/styles/note-paper.css`、`src/shared/markdown/markdown-field.tsx`、`src/modules/notes/ui/components/editor/{inputs,tags,cues,summary}.tsx`
9. migration の継続時だけ `scripts/postgres-migration-common.js`、`test/postgres/data-migration-contract.test.js`、`README.md`
10. 2026-07-31 の runtime / source-reader follow-up を再開するときは、まず `summary/20260731/worker-canvas-scroll-wheel-touch-qa-20260731.md`、`summary/20260731/worker-mobile-note-runtime-20260731.md`、`summary/20260731/worker-postgres-native-reader-fallback-20260731.md`、`summary/20260731/1804-recheck-postgres-native-reader-fallback-evidence-20260731-d5caeaf3-summary.md` と、対応する `doc/implementation/IMPLEMENTATION_STATUS.md` / `doc/testing/TEST_SCENARIOS.md` の 2026-07-31 追補だけを読む。Browser 再試行は Browser backend / localhost bind / headless Chromium が利用可能な環境、実 Postgres reconcile は明示許可された isolated target がある別 task で行う。
11. ノート一覧ライブ検索 UI を再開するときは、まず `summary/20260731/1814-fix-notes-filter-review-toggle-alignment-b1a25f5b-summary.md`、`summary/20260731/1830-refine-notes-live-filter-controls-6efda521-summary.md`、`summary/20260731/1838-remove-notes-list-redundant-intro-copy-64fb3ea2-summary.md`、`summary/20260731/1849-remove-review-toggle-on-off-label-7693ff1d-summary.md` を読む。次に `src/modules/notes/ui/components/list/list.tsx`、`src/modules/notes/ui/components/list/filters.tsx`、`test/notes/list-filter-layout-contract.test.js`、`test/notes/list-filter-live-search-contract.test.js`、`test/notes/list-header-contract.test.js` だけを確認する。文書上の現行境界は `doc/screens/MVP_UI_WIREFRAMES.md` の NTE-010 節と `doc/implementation/IMPLEMENTATION_STATUS.md` §7.2 を参照する。

### 最小限の開始コマンド

```sh
git status --short
git branch --show-current
git branch -vv
git log -1 --oneline
git rev-parse origin/main
git rev-parse origin/develop
git branch -r
```

依存関係が欠落または不整合の場合だけ、次を実行します。

```sh
npm ci --no-audit --no-fund
npm run prisma:generate
```

通常の再開時は、最小検証として次を実行します。E2E は localhost bind が許可された環境で行います。

```sh
npm run lint
npx tsc --noEmit --pretty false
node --test test/notes/*.test.js test/postgres/*.test.js test/e2e-cleanup-contract.test.js
npm run build
```

以上。次回作業では、Browser QA の `BLOCKED`、isolated source-reader evidence の限定 `PASS`、実 native binary / 実 Postgres target / CI / hosted deployment の未確認を混同せず、別の検証単位として扱ってください。
