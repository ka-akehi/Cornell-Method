# Cornell Method Notebook 引き継ぎメモ

作成日: 2026-06-22

## 目的

このメモは、2026-06-22 に実施した設計成果物整理、図面SVG生成運用、可読性監査、Manager / Worker 運用上の注意を、次回作業へ引き継ぐためのものです。

次回はこのファイルを起点に、必要に応じて summary と設計ドキュメントを参照してください。

## 次回最初に読むもの

| ファイル | 役割 |
| --- | --- |
| `AGENTS.md` | リポジトリ仕様、Manager / Worker 方針、開発ルール |
| `HANDOFF_2026-06-15.md` | 前回までのMVP設計方針 |
| `doc/README.md` | `doc/` 配下の分類入口 |
| `doc/diagrams/README.md` | Mermaid / SVG 生成運用 |
| `doc/diagrams/DIAGRAM_ASSETS.md` | Mermaid source と SVG の対応表 |
| `doc/diagrams/DIAGRAM_READABILITY_AUDIT.md` | 図面可読性監査の最終結果 |
| `summary/20260622/0112-diagram-readability-audit-report-9aa56aa5-summary.md` | 図面可読性監査レポート作成タスク |
| `summary/20260622/0118-split-screen-transition-diagrams-db63b7d4-summary.md` | 画面遷移図分割タスク |
| `summary/20260622/0123-split-detail-state-diagrams-4588ac8d-summary.md` | 状態図分割タスク |

## 今日の大きな成果

### Summary / Manager / Worker 運用

- `summary/` 運用をこのリポジトリ向けに導入済み。
- Worker 完了時に `summary/YYYYMMDD/HHMM-*-summary.md` を作成する運用になっている。
- Manager は、実装・設計成果物・生成物の修正を原則 Worker task 化する方針を再確認した。
- 今回、Manager が図面修正を直接進めかけたため、ユーザーから指摘を受けた。次回以降は設計成果物や生成物の品質改善は最初から task 化すること。

## `doc/` 配下の整理

`doc/` は設計書関連のドキュメントとして、分類別ディレクトリへ整理済み。

主な分類:

| ディレクトリ | 役割 |
| --- | --- |
| `doc/requirements/` | 要件定義、MVP分類、システム仕様 |
| `doc/workflows/` | ワークフロー、業務フロー |
| `doc/screens/` | 画面設計、画面棚卸し |
| `doc/api/` | API設計 |
| `doc/data/` | データ設計 |
| `doc/technical/` | 技術設計、設計ツール運用 |
| `doc/diagrams/` | UML、ER、状態遷移、シーケンス、SVG成果物 |
| `doc/testing/` | テスト観点 |
| `doc/implementation/` | 実装状況、実装タスク |
| `doc/review/` | As-Is棚卸し、設計レビュー |
| `doc/design-studio/` | Google Stitch / Claude Design 代替運用ガイド |

旧 `doc/MVP_*.md` などのトップ直下ファイルは、分類ディレクトリへ移動されている。

## Design Studio / Google Stitch・Claude Design 代替

Google Stitch / Claude Design の完全代替ではないが、Codexで設計・レビューに使える環境として Design Studio 運用を導入済み。

主な成果物:

- `doc/design-studio/README.md`
- `doc/design-studio/templates/design-brief.md`
- `doc/design-studio/templates/variant-review.md`
- `doc/design-studio/templates/implementation-handoff.md`
- `.agents/plugins/cornell-design-studio/`

Figmaなしでも、画面設計書、ワイヤーフレーム、Mermaid図、SVG成果物、設計レビュー記録を作れる方針。

## Mermaid / SVG 生成運用

Mermaid を Markdown 内に書くだけで終わらせず、視覚確認用の `.mmd` と `.svg` を生成する運用を導入済み。

生成コマンド:

```bash
npm run diagrams:build
```

内訳:

```bash
npm run diagrams:extract
npm run diagrams:svg
```

関連ファイル:

| ファイル | 役割 |
| --- | --- |
| `scripts/extract-mermaid-diagrams.js` | Markdown から Mermaid ブロックを抽出 |
| `scripts/render-mermaid-diagrams.js` | Mermaid source から SVG を生成 |
| `doc/diagrams/DIAGRAM_ASSETS.md` | source / mmd / svg 対応表 |
| `doc/diagrams/README.md` | 図面生成運用ルール |

SVG は `doc/diagrams/assets/svg/`, `doc/workflows/assets/svg/`, `doc/screens/assets/svg/` へ分類別に出力される。

背景は白で固定。`scripts/render-mermaid-diagrams.js` が生成SVGへ以下を挿入する。

```xml
<rect x="0" y="0" width="100%" height="100%" fill="#ffffff"/>
```

## 図面可読性監査

ユーザーから、単一の画面遷移SVGだけではなく、図面成果物全体の可読性を見るべきと指摘された。

そのため、全SVGを監査し、結果を `doc/diagrams/DIAGRAM_READABILITY_AUDIT.md` に残した。

最終状態:

| Classification | Count |
| --- | ---: |
| OK | 18 |
| Minor fix | 5 |
| Split recommended | 0 |
| Needs decision | 0 |

検証結果:

- Mermaid図総数: 23
- SVG生成数: 23
- 白背景入りSVG: 23
- `npm run diagrams:build`: 成功
- 未修正項目: なし

## 修正された図面

### 画面遷移図

元は1枚の横長SVGだったが、以下の3図へ分割済み。

正本:

- `doc/diagrams/MVP_SCREEN_TRANSITION_DIAGRAM.md`

生成SVG:

- `doc/diagrams/assets/svg/mvp-screen-transition-diagram-01-main-route-root-nav-notes-new-detail.svg`
- `doc/diagrams/assets/svg/mvp-screen-transition-diagram-02-detail-modes-view-edit-review.svg`
- `doc/diagrams/assets/svg/mvp-screen-transition-diagram-03-support-routes-backup-cancel-delete-return.svg`

分割観点:

- 主要導線
- 詳細内モード
- 補助導線

### 状態遷移図

元は詳細画面モード、削除、エラー復帰が1枚に集中していたため、以下へ分割済み。

正本:

- `doc/diagrams/MVP_STATE_DIAGRAMS.md`

生成SVG:

- `doc/diagrams/assets/svg/mvp-state-diagrams-01-normal-mode-transitions.svg`
- `doc/diagrams/assets/svg/mvp-state-diagrams-02-delete-transitions.svg`
- `doc/diagrams/assets/svg/mvp-state-diagrams-03-error-recovery-transitions.svg`
- `doc/diagrams/assets/svg/mvp-state-diagrams-04-diagram.svg`

分割観点:

- 通常モード遷移
- 削除遷移
- エラー復帰遷移
- ノート復習状態

### シーケンス図

API参加者名が長く、横幅が広がりやすかったため、参加者名を短縮済み。

正本:

- `doc/diagrams/MVP_SEQUENCE_DIAGRAMS.md`

修正方針:

- 参加者名は `Notes API`, `Review API`, `Backup API` のように短くする。
- `POST /api/notes` などの HTTP method / path はメッセージ側へ移す。

## 今日実行された主なWorker task

| Task | 状態 | 概要 |
| --- | --- | --- |
| `diagram-assets-layout-verification-2c6408af.task.md` | done | 図面出力先を分類別に整理、SVG生成確認 |
| `screen-transition-diagram-readability-c69f5cab.task.md` | done | 画面遷移図の個別可読性改善 |
| `diagram-assets-readability-audit-2c140a4d.task.md` | done | 全SVG可読性監査の初回タスク。ただし成果物不足があり是正対象になった |
| `diagram-readability-audit-report-9aa56aa5.task.md` | done | `DIAGRAM_READABILITY_AUDIT.md` 作成 |
| `split-screen-transition-diagrams-db63b7d4.task.md` | done | 画面遷移図を3分割 |
| `split-detail-state-diagrams-4588ac8d.task.md` | done | 詳細画面状態図を分割 |

## 注意点 / 残リスク

### summary の内容妥当性

Worker summary は raw log 抑制用の要約として作られるが、内容妥当性まで十分に記録されない場合があった。

今回も以下のような summary があった。

- `生成物の内容妥当性はこの summary ではレビューしていない`

次回以降、Manager は完了通知だけで完了扱いにせず、成果物側にレビュー結果が残っているか確認すること。

### Manager は直接作業しすぎない

今回、Manager がSVG可読性改善を直接始めてしまい、ユーザーから指摘を受けた。

次回以降の基準:

- 設計書、UML、ワイヤーフレーム、SVGなど成果物の作成・修正・検証は原則 task 化。
- 実装、UI、API、ドキュメント同期、生成スクリプト変更も原則 task 化。
- Manager が直接行うのは、状況確認、方針整理、タスク作成、Worker完了後の確認、ユーザーへの判断材料提示まで。

### `package.json` の重複

`package.json` の `devDependencies` に以下の重複キーが見える。

- `@mermaid-js/mermaid-cli`
- `puppeteer`

JSONとしては後勝ちになるが、整理タスクを切る余地がある。今すぐ図面生成は動いている。

### npm audit

依存追加時に以下を確認済み。

- `npm audit signatures`: 成功
- `npm audit --audit-level=moderate`: 既知の moderate vulnerability で失敗

主に `brace-expansion` や Next.js transitive `postcss` 系。今日の図面運用は進めたが、依存整理・audit対応は別タスク候補。

## 次回の推奨再開手順

1. `git status --short` を確認する。
2. このファイルを読む。
3. `doc/diagrams/DIAGRAM_READABILITY_AUDIT.md` で図面監査の最終状態を確認する。
4. `npm run diagrams:build` が必要に応じて成功するか確認する。
5. 残作業を進める場合は、Manager が `codex-queue/tasks/queued/` に task を作る。

確認コマンド例:

```bash
git status --short
npm run diagrams:build
find doc/diagrams/assets/svg doc/workflows/assets/svg doc/screens/assets/svg -name '*.svg' | wc -l
rg -l '<rect x="0" y="0" width="100%" height="100%" fill="#ffffff"' doc/diagrams/assets/svg doc/workflows/assets/svg doc/screens/assets/svg | wc -l
```

期待値:

- SVG数: 23
- 白背景入りSVG数: 23

## 次に検討するタスク候補

優先度が高い候補:

1. `package.json` の重複 dependency キー整理。
2. `npm audit --audit-level=moderate` の扱い整理。
3. 現在の大量未コミット変更を、論理単位で整理・確認する。
4. 図面監査が完了したため、MVP実装側の未完了項目確認へ戻る。

補足:

- Phase 2 の画面や状態を追加するときは、既存図に詰め込まず、導線・状態単位で図を追加する。
- SVGは直接編集せず、正本Markdownの Mermaid を修正して `npm run diagrams:build` で再生成する。
