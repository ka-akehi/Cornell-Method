# Cornell Method Notebook 引き継ぎ資料

作成日: 2026-07-25（JST）
対象: `HANDOFF_2026-07-23.md` のレビュー・リファクタリング記録から、このチャットで実施した Canvas MVP runtime QA 完了まで

この資料は、次回の Manager が長い会話履歴や Worker の raw log を読み直さず、現在の判断、確認済み範囲、未確認範囲、再開手順を把握するためのものです。Worker の自動 summary は状態確認の起点にし、実測の根拠は個別の QA summary と本資料の判定を使います。

## 1. 現在の結論

- 現在の作業ブランチは `main`。この handoff 作成開始時点の先端は `a7a5c1e docs: complete canvas runtime QA`。
- 2026-07-25 の Canvas MVP remaining runtime QA は、Worker の Browser backend が利用できなかったため、Manager 側の権限付き headless Playwright Chromium で補完確認した。
- 寸法、style、保存・閲覧・編集・再読込、消しゴム、Undo/Redo、toolbar の responsive / ARIA / focus、touch の page / paper scroll は確認済み範囲として記録済み。図形内文字は rect commit、ellipse cancel、style、他要素保持、保存・再読込の必須 subset を確認した。
- 3px / 5px、unknown-target pen、shape-tool switch は既存の確認済み summary を採用し、重複実施していない。厳密な 4px 境界は発注者判断により再確認していない。
- `CANVAS-INTERACTION-001`、`CANVAS-GESTURE-001`、`CANVAS-SHAPE-TEXT-001` は subset の確認に留まり、シナリオ全体の判定は部分実施のまま。wheel / trackpad 固有入力と Phase 2 機能は今回の対象外。
- 最新の Manager fallback QA では、既存ノート desktop edit を 1280 / 1440px、`nextReviewDate` の新規初期値・手動値保持・未設定維持を確認した。review 成功 UI、375 / 768px の mobile edit は未確認であり、desktop の結果を mobile 専用確認へ拡張しない。
- アプリコード、設定、依存関係、DB schema、API 契約、Canvas JSON、恒久 fixture は変更していない。2026-07-25 の commit は QA 記録と docs のみ。

## 2. 前提となる既存引き継ぎ

`HANDOFF_2026-07-23.md` には、PR #18 作成までのレビュー・リファクタリングの詳細が記録されていた。主な判断は次のとおりで、今回も継続する。

- 変更は挙動を変えない責務・配置整理に限定し、DB schema、migration、API 契約、Canvas 保存形式、Phase 2 機能を推測で変更しない。
- `components` には表示 component を置き、型・設定・Fabric bridge などは責務に応じて `model` / `lib` / `ui/canvas` に置く。
- `remote` は transport / operation、`contracts` は API 型、冗長な一段再 export の facade は増やさない。
- Manager は目的・対象・完了条件を定義して Worker に発注し、Worker は一 task のみを実行する。仕様/調査/文書 task と coding task は分離する。
- 静的検証、API runtime、Browser UI runtime は別の証拠として記録する。Worker の失敗通知を PASS の根拠にしない。

## 3. このチャットでの経緯と発注者判断

### 3.1 Canvas gesture hardening まで

このチャットでは、既存の Canvas gesture QA の続きとして次の task を順に確認した。

- unknown metadata / unknown target の gesture guard の仕様確認・検証。
- malformed metadata converter の hardening。
- unknown-target pen gesture の UI 修正。修正は `main` にコミット済みと確認されたため、同じ実装を重ねて進めない方針にした。
- unknown-target pen runtime の確認。Browser runtime が使える場合の手順を案内し、ユーザーは dev server を起動してページ表示まで確認した。
- 前回成功した direct Browser 手順を再利用する方針を採用した。

発注者から「3px と 5px は確認済みなら不要」「無駄な確認は飛ばす」と明確に指定された。以後、確認済みの閾値を重複実施せず、未確認の境界だけを対象にした。

### 3.2 Browser runtime の制約と fallback

Worker の Browser backend は空配列で、app-server は `Operation not permitted` により初期化できなかった。ユーザーが Browser / Chrome を有効にする手順を確認し、dev server (`npm run dev -- --hostname 127.0.0.1 --port 3000`) を起動してページ表示まで行ったが、Worker 側の実測制約は解消しなかった。

そのため、前回成功時と同じ Manager 側の権限付き headless Playwright Chromium を使い、`http://127.0.0.1:3000/notes/new` を直接操作した。Worker の failed / blocked summary は環境状態の証跡であり、runtime PASS の根拠にはしていない。

### 3.3 省略した確認

- shape-tool switch runtime は Worker task が失敗したが、既存の Manager 実測 summary に確認結果があるため、必要な範囲を再利用した。
- exact four-pixel boundary task は失敗し、発注者が厳密 4px の再確認を不要と判断したため再実施していない。
- runtime QA を全部終わらせる方針は「現行 Canvas MVP の残存 QA を、重複確認と Phase 2 を除いて完了する」という範囲で実行した。

## 4. Worker task の状態

| Task / 領域 | 通知状態 | 実測・採用した根拠 |
| --- | --- | --- |
| unknown metadata gesture guard | Common completed | 既存 summary とコード確認。 |
| malformed metadata converter hardening | Common completed | 既存 summary とコード確認。 |
| unknown-target pen UI fix | UI completed | `main` に反映済み。 |
| unknown-target pen runtime | Common completed | Manager fallback / 既存 runtime summary。 |
| shape-tool switch gesture separation | Common failed | Manager fallback の実測 summary を採用。 |
| exact four-pixel boundary | Common failed | 発注者判断で再実施しない。 |
| touch scroll boundaries | Common completed | Worker の実測は blocked。Manager fallback で確認。 |
| dimension / style / save-reload / toolbar | Common failed | Browser backend / app-server 制限。Manager fallback で確認。 |
| eraser / history | Common completed | Worker の実測は blocked。Manager fallback で確認。 |
| runtime QA docs integration | Common completed | `doc/testing/TEST_SCENARIOS.md` と `doc/implementation/IMPLEMENTATION_STATUS.md` に統合。 |
| handoff 作成 task | Common failed | 同じ app-server 権限エラーで開始前に停止。現在の handoff は Manager fallback で作成中。 |

Worker の自動 summary は `summary/20260725/` に残す。失敗 summary、blocked summary、完了 summary を削除・上書きしない。

## 5. 2026-07-25 Manager fallback runtime QA

詳細な Canvas 実測記録は [`summary/20260725/canvas-runtime-qa-completion-20260725.md`](summary/20260725/canvas-runtime-qa-completion-20260725.md) にあり、最新の必須 QA 統合判定は [`summary/20260725/2230-mandatory-qa-manager-fallback-20260725.md`](summary/20260725/2230-mandatory-qa-manager-fallback-20260725.md) にある。以下は次回判断に必要な要点だけである。

### 5.1 寸法・要素保持

- 初期 page は `1200 x 800 px`。
- 幅・高さ `320` / `4000` は適用成功。
- `319` / `4001`、decimal、blank は inline error と `aria-invalid=true` で拒否。
- 幅 `320 → 4000` と高さの境界変更の前後で、rect の `id`、`x`、`y`、`width`、`height`、`style` は不変。

### 5.2 style と図形内文字

- standalone text: font size `8` / `96` は適用、`7` / `97` / `12.5` / blank は直前値を維持。
- line: stroke width `1` / `20` は適用、`0` / `21` / `1.5` / blank は直前値を維持。
- text color、line color、left / center / right alignment は選択 target の live style に即時反映。
- rect inline editor は line width を disabled、font / color / alignment を enabled とし、font `20`、color `#16a34a`、center が commit 後の `textStyle` に保存。

### 5.3 保存・閲覧・再読込・cleanup

- 一時タイトル `Runtime Persistence QA 20260725` を使用し、page `1280 x 900`、standalone text `PERSIST TEXT`（font `18`、color `#b91c1c`）、line strokeWidth `4` を保存。
- UI POST は `201`、保存後 GET は `200`。request と GET の Canvas `page` / `elements` は一致。
- viewer の assistive text、編集画面、reload 後の viewer → edit で title / page / text を復元。
- cleanup は DELETE `204`、削除後 GET `404`、タイトル query `totalCount=0`。

### 5.4 消しゴム・Undo/Redo

- text、rect、ellipse、line を個別 click / drag で whole-object erase。非対象の geometry / style / text / points は保持し、最後の object 数は `0`。
- 空履歴では Undo / Redo が disabled。
- rect create → Undo → Redo、text の末尾文字 Undo → Redo、page `1200x800 → 1280x900` の Undo → Redo を確認。

### 5.5 toolbar・responsive・touch

- 375 px の rail は client / scrollWidth `305 / 461`、768 px は `346 / 461`。
- 全 tool の `aria-label` / `aria-pressed` / `data-active`、Tab / Shift+Tab、focus-visible solid 2 px を確認。
- 640x480 は適用、319 は error / `aria-invalid=true`。body / document の viewport-wide overflow はなし。
- touch 375 px の縦 swipe 4 回後 `scrollY=1779`、footer 到達。
- touch 1280 px の 1920x1080 paper で local `scrollLeft 0 → 1069`、page `scrollY` は不変、body / document の横 overflow はなし。
- 全ケースの console error / warning と page error は `0`。

### 5.6 既存ノート desktop edit / nextReviewDate

- 既存ノートの `/notes/[id]` edit は 1280 / 1440px で title、noteDate、source、tag、Cue、Canvas、Summary、`nextReviewDate` を復元し、保存後再読込、キャンセル、主要 field 到達性、body / document の viewport-wide 横幅不在を確認した。console / page error は 0 で、確認用ノートは DELETE 204、GET 404、一覧 query の残留 `totalCount=0` を確認した。
- 新規 `noteDate=2026-07-25` の `nextReviewDate=2026-08-01` 初期表示・保存、手動 `2026-08-05` の `noteDate` 変更後保持、空欄の再読込・`noteDate` 変更後維持を確認した。
- review 成功 UI の画面反映、375 / 768px の mobile edit、wheel / trackpad 固有入力は今回の summary では確認していない。

## 6. QA の判定境界・残り

### 確認済みとして扱える範囲

- `CANVAS-DIMENSION-001`: 寸法入力、境界値、invalid、resize 前後の要素不変、保存・表示の確認済み範囲。
- `CANVAS-STYLE-001`: standalone text、line、color、alignment、rect editor style commit の確認済み範囲。
- `CANVAS-PERSISTENCE-STYLE-001`: standalone text / line の style、page、request / GET、viewer / edit / reload、cleanup の確認済み範囲。
- `CANVAS-TOOLBAR-STYLE-001`: rail、ARIA / active state、keyboard、focus-visible、page / paper scroll の確認済み範囲。
- 既存ノート desktop edit: 1280 / 1440px の詳細 field 復元、保存・再読込・キャンセル、主要 field 到達性、overflow 不在、cleanup の確認済み範囲。
- `nextReviewDate`: 新規初期値、手動値保持、未設定維持の確認済み範囲。review 成功 UI は未確認。

### まだ全体 PASS に繰り上げない範囲

- `CANVAS-INTERACTION-001` / `CANVAS-GESTURE-001`: 3px / 5px、既知要素、metadata boundary、unknown-target pen、shape-tool switch は別 summary で確認済みだが、厳密 4px、touch scroll 干渉などを含む全体は部分実施。
- `CANVAS-SHAPE-TEXT-001`: rect commit、ellipse cancel、font size / alignment style、他要素保持、tool switch、cleanup、保存・再読込の必須 subset は確認済み。全 tool の反復 lifecycle と全保存境界は未確認。
- wheel / trackpad 固有の物理入力は測定していない。
- 375 / 768px の既存ノート mobile edit は未確認。Canvas toolbar の 375 / 768 touch / rail 確認を、既存ノート edit 全体の mobile PASS とみなさない。
- `nextReviewDate` の review 成功 UI における画面反映は未確認。
- Phase 2（autosave、soft-delete Undo、review task、NoteCard / D&D、PDF export、タグ管理 UI 等）は現行 MVP runtime QA の対象外。

上記は「未完了の不具合」と同義ではない。新たに確認する場合は、厳密 4px、wheel / trackpad、shape text full lifecycle などから一目的ずつ task 化し、重複する 3px / 5px を再投入しない。

## 7. 現在の成果物・Git・queue

### MVP クローズ状態（2026-07-25 の確認）

- 必須 MVP クローズは完了。`AGENTS.md` §9 の 4 つの受け入れ条件はすべて `[x]` である。
  - [x] 一覧 → 詳細 → 編集 → 保存 → 閲覧の実操作デモ（WebM）。
  - [x] `npm run lint` の成功。
  - [x] Prisma migrate / seed 手順の README 記載。
  - [x] 主要画面スクリーンショットの README 追加。
- 実操作デモは [`doc/assets/demos/mvp-note-flow.webm`](doc/assets/demos/mvp-note-flow.webm) で、[`README.md`](README.md) の「MVP 受け入れ材料」節からリンクされている。Lint、Prisma 手順、スクリーンショットの既存根拠も同節と README の検証・データベース節にある。
- 厳密な 4px、wheel / trackpad 固有入力、375 / 768px の既存ノート mobile edit、review 成功 UI、Phase 2 は、§6 に記載した意図的な非ブロッカーとして今回の必須 MVP クローズ対象外に残す。
- Worker queue は、この HANDOFF 更新 task を runner が完了処理した後のクローズ時点の確認値として、未処理（待機・実行中） 0、完了 / 失敗を含む全 task 処理済みであることを記録する。次回確認時は `codex-queue/bin/worker-status.sh` で現在値を取り直す。

### QA 記録

- [`summary/20260725/canvas-runtime-qa-completion-20260725.md`](summary/20260725/canvas-runtime-qa-completion-20260725.md)
- [`summary/20260725/2230-mandatory-qa-manager-fallback-20260725.md`](summary/20260725/2230-mandatory-qa-manager-fallback-20260725.md)
- [`summary/20260725/2325-mvp-flow-demo-manager-fallback-20260725.md`](summary/20260725/2325-mvp-flow-demo-manager-fallback-20260725.md)
- [`summary/20260725/2327-finalize-mvp-acceptance-materials-20260725-1d32f0ee-summary.md`](summary/20260725/2327-finalize-mvp-acceptance-materials-20260725-1d32f0ee-summary.md)
- [`README.md`](README.md) の MVP 受け入れ材料（実操作デモ、主要画面スクリーンショット、検証・Prisma 手順）
- [`AGENTS.md`](AGENTS.md) §9 の MVP 受け入れ条件
- [`doc/testing/TEST_SCENARIOS.md`](doc/testing/TEST_SCENARIOS.md)
- [`doc/implementation/IMPLEMENTATION_STATUS.md`](doc/implementation/IMPLEMENTATION_STATUS.md)
- QA 記録の commit: `a7a5c1e docs: complete canvas runtime QA`

### 再開時に確認する現在値

この handoff 作成時点では、hand-off 用 docs と Worker failure summary が未コミットの可能性がある。次回は推測せず、次を実行して現在値を確認する。

```sh
git status --short
git branch -vv
git log -1 --oneline
codex-queue/bin/worker-status.sh
```

hand-off 作成 task `create-handoff-20260725-bedbb6dc` は app-server 権限エラーで failed。failure summary は履歴として保持し、raw log は読み直さない。

## 8. 次回の最小再開手順

1. `git status --short` で未コミット変更を確認し、ユーザー変更を戻さない。
2. `git log -1 --oneline`、`git branch -vv`、`codex-queue/bin/worker-status.sh` で branch / commit / queue を確認する。
3. この `HANDOFF_2026-07-25.md` を読む。
4. `AGENTS.md`、`doc/implementation/MVP_CONTRACT.md`、`doc/implementation/IMPLEMENTATION_STATUS.md`、`doc/testing/TEST_SCENARIOS.md` を読む。
5. 次の summary を必要最小限だけ読む。
   - `summary/20260725/canvas-runtime-qa-completion-20260725.md`
   - `summary/20260725/2230-mandatory-qa-manager-fallback-20260725.md`
   - `summary/20260725/canvas-unknown-target-pen-browser-qa-runtime-20260725.md`
   - `summary/20260725/canvas-shape-tool-switch-gesture-separation-runtime-20260725.md`
   - 必要な場合だけ各 Worker の failed / blocked summary。
6. raw log、`.next`、生成物の大量一覧は読まない。
7. 次の作業が必要なら、まず「厳密 4px を本当に確認するか」「wheel / trackpad を対象にするか」「shape text full lifecycle を確認するか」を発注者に確認し、一目的一 task で Common / UI / API queue を選ぶ。既確認の 3px / 5px は再投入しない。
8. PR / CI の状態を扱う場合は、現在の branch とリモート状態を確認してから `gh pr checks` / `gh pr view` を使う。旧 HANDOFF にある PR #18 の状態を現在値として再利用しない。

## 9. 引き継ぎ資料の検証方針

- この資料は runtime の新しい PASS を追加する場所ではなく、既存 summary と docs の再開点を固定する場所である。
- 次回変更後は `git diff --check` を実行する。
- `summary/20260725/canvas-runtime-qa-completion-20260725.md` を後続入力にする場合は `tools/check-summary.sh` を通す。
- docs 以外の変更が必要になったら、仕様/調査 task と実装 task を分け、Worker summary を確認してから次へ進む。
