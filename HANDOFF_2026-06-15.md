# Cornell Method Notebook 引き継ぎメモ

作成日: 2026-06-15

## 目的

このメモは、今回進めた設計見直しの内容を次回作業へ引き継ぐためのものです。

次回はこのファイルを起点に、必要に応じて詳細設計ドキュメントを参照してください。

## 現在の前提

- 発注者はユーザー。
- Manager / Worker が作業者としてシステム完成まで進める。
- 発注者は設計学習も目的としているため、重要な判断ポイントは Manager 側から明示する。
- 仕様や方針が不明な場合、Manager / Worker は推測で進めず、発注者へ質問する。
- 現行実装は古いライブラリや過去仕様を含むため、新設計の制約として扱わない。
- 実装段階ではフルリニューアルする。
- ただし As-Is 棚卸しは参考資料として残す。

## 参照すべき主要ドキュメント

| ファイル | 役割 |
| --- | --- |
| `AGENTS.md` | リポジトリ全体のルール、アプリ仕様、Manager / Worker 運用方針 |
| `doc/DESIGN_REVIEW_PLAN.md` | 設計見直し全体の進め方 |
| `doc/AS_IS_DESIGN_INVENTORY.md` | 現行実装の棚卸し |
| `doc/CORNELL_METHOD_SPEC_REVIEW.md` | Cornell Method アプリとして仕様が適切かのレビュー |
| `doc/MVP_CLASSIFICATION_DRAFT.md` | MVP / Phase 2 / 保留 / 削除候補の分類 |
| `doc/MVP_DATA_DESIGN.md` | MVP データ設計 |
| `doc/MVP_SCREEN_DESIGN.md` | MVP 画面設計 |
| `doc/MVP_API_DESIGN.md` | MVP API 設計 |
| `doc/MVP_TECHNICAL_DESIGN.md` | MVP 技術選定・将来移行方針 |
| `doc/MVP_IMPLEMENTATION_TASKS.md` | Worker 向け実装タスク分割案 |
| `learning-notes/DESIGN_LEARNING_NOTES.md` | 発注者個人の設計学習メモ |

## AGENTS.md の整理

当初は root の `AGENTS.md` と `doc/AGENTS.md` が存在していたが、発注者は `AGENTS.md` を1つにしたい方針。

現在は root の `AGENTS.md` を主参照とし、`doc/AGENTS.md` は使わない方針で整理済み。

root `AGENTS.md` には以下を集約する。

- Repository Instructions
- Application Specification
- Development Policy
- Manager / Worker Policy
- 発注者が設計初心者であることを踏まえた Manager 側の助言方針
- 設計学習メモを `learning-notes/DESIGN_LEARNING_NOTES.md` に保存する方針

## Cornell Method としての見直し結果

当初仕様は「ノート管理アプリ」としての要素が強く、Cornell Method の学習サイクルを支えるには以下が不足していた。

- Cue から本文を思い出す復習体験
- Summary を書く習慣を支える仕組み
- Review の記録
- 次回復習日の管理

そのため、MVP では単なる CRUD ではなく、以下の Cornell Method の流れを支える。

1. 記録する
2. Cue / キーワード / 質問を整理する
3. Summary を書く
4. Cue から本文を想起する
5. 復習済みとして記録する

## MVP 確定方針

MVP は「ローカル個人利用で、Cornell Method の学習サイクルを最小限回せるアプリ」とする。

MVP に含めるもの:

- ノート一覧
- ノート新規作成
- ノート詳細
- ノート編集
- Cornell レイアウト
- Cue / キーワード / 質問リスト
- Markdown 本文
- Summary
- タグ
- タイトル検索
- 日付絞り込み
- タグ絞り込み
- 復習対象フィルタ
- 本文を隠す復習モード
- 復習予定日
- 復習済み管理
- 最低限のバックアップ
- API エラー形式の統一
- 基本バリデーション

MVP から外し、Phase 2 以降に送ったもの:

- PDF 出力
- Cue / Tag の差分更新
- Markdown 専用エディタライブラリ
- Rust API
- Vercel deploy
- Supabase / Neon / Turso など外部DB移行
- Basic認証相当
- ドラフト自動保存
- Undo スナックバー
- 高度な復習タスク
- D&D 並び替え
- タグ管理 UI
- バックアップログのDB管理
- A11y 強化

## 重要な設計判断

### ノート本文

MVP ではカード分割せず、1つの Markdown 本文として扱う。

理由:

- Cornell Method の Notes 領域は、まず連続した本文として扱う方が自然。
- カード分割はUI、DB、並び替え、復習単位、編集体験を複雑にする。
- 後からカード化することは可能。

### Cue

Cue は Markdown 本文に埋め込まず、独立した Cue リストとして扱う。

理由:

- Cue 領域を本文と分けるのが Cornell Method の基本構造に合う。
- 復習モードで Cue だけを表示し、本文を隠しやすい。
- 将来、Cue 単位の復習やリンクに拡張しやすい。

### 復習画面

MVP では復習専用画面を別に作らず、ノート詳細画面に復習モードを持たせる。

理由:

- 表示対象は同じノートであり、画面を分けると状態管理が増える。
- 一覧の復習対象フィルタから対象ノートを探し、詳細画面で復習する流れで足りる。
- 高度な復習キューや間隔反復は Phase 2 に送る。

### Cue / Tag 更新

MVP では全置換で更新する。

理由:

- 個人利用かつMVP規模では差分更新の性能メリットが小さい。
- 差分更新は実装、検証、競合制御が重くなる。
- 大量データ、自動保存、履歴管理が必要になった段階で Phase 2 として再検討する。

### Markdown エディタ

MVP では `textarea + preview` で開始する。

理由:

- 高機能エディタライブラリは依存関係と保守範囲を増やす。
- MVP では Markdown を書けて確認できれば十分。
- ツールバー、ショートカット、エディタ内プレビューは Phase 2 に送る。

### API 実装言語

MVP では TypeScript / Next.js Route Handler を採用する。

Rust API は Phase 2 の検討事項。

理由:

- MVP の処理は CRUD、検索、復習済み更新、バックアップが中心。
- TypeScript で UI / API / Prisma / validation を揃えられる。
- Rust API にすると 2プロセス構成、型共有、CORS、DB層分断などが増える。

### DB

MVP ではローカル SQLite + Prisma を採用する。

理由:

- 個人ローカル利用に合っている。
- セットアップが軽い。
- 外部サービスの無料枠や接続情報管理に依存しない。

## Vercel / 外部DB 方針

Vercel deploy と外部DB移行は Phase 2 とする。

Vercel にデプロイすると、個人利用目的でもURLが生成される。そのため、Vercel に載せる段階ではアクセス制御とセットで扱う。

Phase 2 で Vercel deploy を行う場合:

- 無料範囲を優先する。
- アプリ側Basic認証相当を実装する。
- `BASIC_AUTH_USER` / `BASIC_AUTH_PASSWORD` を環境変数で管理する。
- 認証情報をリポジトリにコミットしない。
- production URL / generated URL の扱いを確認する。

SQLite ファイルをそのまま Vercel の永続DBとして使う方針は採用しない。

外部DB候補:

| 候補 | 種類 | 位置づけ |
| --- | --- | --- |
| Supabase | PostgreSQL | DB/Auth/Storage を含めて扱える有力候補 |
| Neon | Serverless PostgreSQL | Vercel + Prisma + PostgreSQL で有力候補 |
| Turso | libSQL / SQLite互換 | SQLite に近い設計感を維持したい場合の有力候補 |
| Cloudflare D1 | SQLite系 serverless DB | Cloudflare Workers に寄せる場合の候補 |
| PlanetScale | MySQL系 | 今回は優先度低め |

Manager 推奨:

- Vercel + PostgreSQL なら Supabase / Neon を比較する。
- SQLite 互換を重視するなら Turso を比較する。
- Cloudflare Workers へ寄せるなら D1 を検討する。

## MVP データ設計概要

MVP の主要モデル:

- `Notebook`
- `Cue`
- `Tag`
- `NotebookTag`

`Notebook` の主な項目:

- `id`
- `title`
- `noteDate`
- `sourceType`
- `sourceTitle`
- `overview`
- `body`
- `summary`
- `nextReviewDate`
- `reviewedAt`
- `createdAt`
- `updatedAt`

MVP では採用しないモデル:

- `NoteCard`
- `NoteCueLink`
- `NotebookDraftState`
- `NotebookReviewProgress`
- `SoftDeleteBuffer`
- `BackupLog`

## MVP 画面設計概要

画面:

- `/notes`
- `/notes/new`
- `/notes/[id]`
- `/backup`

`/notes`:

- ノート一覧
- 検索
- 日付絞り込み
- タグ絞り込み
- 復習対象フィルタ

`/notes/new`:

- ノート作成
- Cue リスト入力
- Markdown 本文入力
- Summary 入力
- タグ入力

`/notes/[id]`:

- 閲覧モード
- 編集モード
- 復習モード

`/backup`:

- バックアップ作成
- バックアップ一覧

## MVP API 設計概要

API:

- `GET /api/notes`
- `POST /api/notes`
- `GET /api/notes/:id`
- `PATCH /api/notes/:id`
- `DELETE /api/notes/:id`
- `POST /api/notes/:id/review`
- `GET /api/tags`
- `GET /api/backups`
- `POST /api/backups`

エラー形式:

```json
{
  "code": "VALIDATION_ERROR",
  "message": "入力内容を確認してください",
  "errors": {}
}
```

## 次回の実装順序

次回は `doc/MVP_IMPLEMENTATION_TASKS.md` をもとに Worker タスクへ投入する。

Manager 推奨の順序は DB/API 先行。

理由:

- データ構造が画面の土台になる。
- Note / Cue / Tag / Review の保存仕様を先に固めた方が UI の手戻りが少ない。
- Worker タスクを API / UI に分割しやすい。

最初に着手するタスク:

1. `mvp-prisma-schema`
2. `mvp-validation-schemas`
3. `mvp-notes-api`
4. `mvp-tags-api`
5. `mvp-backup-api`

その後 UI タスクへ進む。

## 次回開始時の確認事項

次回作業開始時に確認すること:

1. `git status --short` を確認する。
2. `AGENTS.md` の方針を読む。
3. この引き継ぎメモを読む。
4. `doc/MVP_IMPLEMENTATION_TASKS.md` の先頭タスクを確認する。
5. DB/API 先行で進めてよいか、発注者へ最終確認する。

## 未確定・確認待ち

現時点で大きな未確定事項はない。

ただし、実装前に以下は発注者確認を推奨する。

| 確認事項 | Manager 推奨 |
| --- | --- |
| 実装順序を DB/API 先行にするか | DB/API 先行 |
| MVP では現行コードを流用せずフルリニューアルするか | フルリニューアル |
| Vercel / 外部DB は Phase 2 のままでよいか | Phase 2 |

## 注意点

- Phase 2 に送った要件は却下ではない。MVP 完成後に再評価する。
- 無料枠や料金は変わるため、Vercel / Supabase / Neon / Turso などは移行時点で公式情報を再確認する。
- 個人ノートを扱うため、クラウド公開時はアクセス制御を必須とする。
- 発注者の設計学習に関わる質問へ回答した場合は、`learning-notes/DESIGN_LEARNING_NOTES.md` に追記する。
