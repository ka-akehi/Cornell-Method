# Cornell Method Notebook 引き継ぎメモ

作成日: 2026-07-16（JST）

## この handoff の目的

このチャット開始時点の Worker 完了通知から、DOC-001 / QA-001 の Manager 確認、受け入れ証跡の整理、次の UI-001 の着手地点までをまとめる。次回は raw log や `.next` の生成物を読み直さず、この handoff と `Next Read` の最小ファイルから作業を再開する。

## 1. 現在の全体状態

- 現行 MVP の実装・受け入れ判断の正本は `doc/implementation/MVP_CONTRACT.md`。
- `AGENTS.md` は製品全体のロードマップを管理し、自動保存、Undo、専用復習タスク、NoteCard / D&D、PDF などは Phase 2 以降として扱う。
- 仕様判断 D-01〜D-05、仕様整合 SPEC-001〜003、実装状況整理 DOC-001、受け入れ証跡整理 QA-001 は完了した。
- 現在の次の候補は Phase 2 の P1 UI-001「復習時 Summary の初期開示を制御」である。
- Worker queue はこの handoff 作成時点で `queued` / `running` に実タスクがなく、QA-001 は `done` に移動済み。
- まだ commit は作成していない。現在の未コミット変更は今回以前の作業分を含むため、commit / reset / checkout は発注者の判断なしに行わない。

## 2. このチャットで行ったこと

### 2.1 開始時点: DOC-001 完了通知の確認

開始時に次の Worker 通知を受け取った。

- `doc-001-reconcile-implementation-status-20260716-retry-1d5f3620.task.md`

Manager として次を確認した。

- task は `codex-queue/tasks/done/` に移動済み。
- [DOC-001 summary](summary/20260716/0058-doc-001-reconcile-implementation-status-20260716-retry-1d5f3620-summary.md) が作成済み。
- `doc/implementation/IMPLEMENTATION_STATUS.md` は、実在する route / API / schema / UI と、未実装・Phase 2 の機能を分離する内容へ更新済み。
- `git diff --check` は成功。
- summary が列挙する `.next` / `next-env.d.ts` の更新は生成物・mtime 情報であり、今回の実質的な対象文書変更とは区別した。

その後、`summary/20260716/current-spec-design-task-list.md` の DOC-001 を完了へ更新した。

### 2.2 QA-001 の投入と完了確認

ユーザーの「次のタスクに進んでください」に対し、Phase 1 の次の P0 である QA-001 を `codex-queue/tasks` に投入した。

- task: `codex-queue/tasks/done/qa-001-reorganize-acceptance-evidence-20260716.task.md`
- summary: [QA-001 summary](summary/20260716/0115-qa-001-reorganize-acceptance-evidence-20260716-summary.md)
- 対象: `doc/testing/TEST_SCENARIOS.md`, `README.md`
- Worker 完了後、Manager が対象差分、参照パス、`git diff --check` を再確認した。
- `summary/20260716/current-spec-design-task-list.md` の QA-001 を完了へ更新した。

QA-001 の Worker summary には `.next/dev/cache/turbopack/**` と `.next/dev/trace` が多数列挙されているが、tracked な実質変更は `README.md` と `doc/testing/TEST_SCENARIOS.md` の 2 ファイルである。`.next` は次回の根拠として読まない。

## 3. DOC-001 で確定した実装状況

[実装状況サマリ](doc/implementation/IMPLEMENTATION_STATUS.md) は、現行コードの実態を次のように整理している。

### 現行 MVP として実装済み

- `/` → `/notes` redirect。
- `/notes` の一覧、フリーワード・日付・タグ・復習対象 filter、ページング。
- `/notes/new` の明示保存による新規作成。
- `/notes/[id]` の閲覧・編集・復習モード、確認後の物理削除。
- `/backup` の手動バックアップ一覧・作成。
- 実在する API は `GET/POST /api/notes`、`GET/PATCH/DELETE /api/notes/:id`、`POST /api/notes/:id/review`、`GET /api/tags`、`GET/POST /api/backups`。
- Prisma model は `Notebook`、`Tag`、`NotebookTag`、`Cue` の 4 つ。
- 本文・Summary の Markdown preview、GFM、sanitize、preview checkbox の表示専用化。
- タグ保存時の自動作成、最大 12 件、同一ノート内の重複防止。

### 実装との差分・Phase 2

- 新規 `nextReviewDate = noteDate + 7日` の初期値は未実装。
- 復習時 Summary の初期非表示は未実装。本文だけが初期非表示で、Summary は常時表示される。
- 概要は保存されるが、本文・Summary と同じ Markdown preview / sanitize 表示ではない。
- 削除は確認後の物理削除。`deletedAt` は schema に残る互換 field であり、soft delete / Undo の実装ではない。
- `NotebookDraftState`、autosave、optimistic lock / 409、`SoftDeleteBuffer`、`/api/undo`、`/tasks/review`、`/api/review-tasks`、NoteCard / D&D、PDF export、タグ管理 UI、backup log / retry API は未実装または Phase 2。

## 4. QA-001 後の受け入れ証跡

受け入れ証跡の正本は [TEST_SCENARIOS.md の受け入れ証跡マトリクス](doc/testing/TEST_SCENARIOS.md#受け入れ証跡マトリクス) である。各行に ID、route / 画面状態、viewport または実行形態、確認日、fixture、判定、summary / 根拠を持たせた。

### PASS として記録された範囲

- 2026-07-05: 主要 UI flow（redirect、一覧、作成、タグ、編集保存、復習、検索、削除、バックアップ）。viewport は元 summary に記録されていない。
- 2026-07-05: Notes CRUD、review、検索、タグ、validation、not found、backup API。
- 2026-07-05: Markdown sanitize / checkbox の runtime 確認。
- 2026-07-05: `npm run backup:copy` と最新 3 世代保持。
- 2026-07-04〜2026-07-05: Prisma validate / generate、`npm run lint`、`npm run build`。
- 2026-07-14: NTE-020 `/notes/new` の 375 / 768 / 1280 / 1440px。Cornell の局所横スクロール、desktop split、Preview 配置を確認。
- 2026-07-15: NTE-030 `/notes/[id]` の閲覧 / 復習 1440px。概要 → Cornell → Summary の順序、本文の初期マスク、表示 / 再マスク、page error なしを直接確認。

### FAIL（静的照合）として記録された差分

- `MVP-GAP-001`: 新規 `nextReviewDate = noteDate + 7日` 初期値。
- `MVP-GAP-002`: 復習時 Summary 初期非表示と Cue → 本文 → Summary の段階的開示。
- `MVP-GAP-003`: 概要の Markdown preview / sanitize。

これは runtime の未実施とは別に、コードと MVP 契約の照合で未達が確認できるため FAIL とした。

### 未実施のまま残した範囲

- `NTE020-EDIT-ALL`: `/notes/[id]` の Policy C edit runtime（375 / 768 / 1280 / 1440px）。
- `NTE020-OVERFLOW-375`: 長い Markdown、長いタグ、長い field error の 375px overflow。
- `NTE030-MOBILE-375-768`: 閲覧 / 復習の 375 / 768px runtime。
- `MVP-REVIEW-EDGE-001`: 既存未設定 nextReviewDate の非補完、noteDate 変更時の手動設定日維持、review 後の画面反映を一つの edge-case fixture で確認する runtime。
- Phase 2 機能全般。MVP の PASS 集計には含めない。

README には、NTE-020 の screenshot が新規作成画面の証拠であり edit runtime の証拠ではないこと、NTE-030 の mobile が未確認であること、契約差分が別途 FAIL であることを追記した。

## 5. 現在の未コミット状態

この handoff 作成前の status では、次の変更が存在していた。

- 変更済み: `AGENTS.md`, `README.md`, `doc/README.md`, `doc/implementation/IMPLEMENTATION_STATUS.md`, `doc/screens/MVP_SCREEN_DESIGN.md`, `doc/screens/MVP_SCREEN_INVENTORY.md`, `doc/testing/TEST_SCENARIOS.md`
- 削除済みの旧 handoff: `HANDOFF_2026-07-15.md`（この handoff 作成時に repository 運用に従い削除）
- 未追跡の主な成果物: `doc/implementation/MVP_CONTRACT.md`、`summary/20260715/` の handoff / review summary、`summary/20260716/` の SPEC / DOC / QA summary と task list
- 今回の handoff 作成により `HANDOFF_2026-07-16.md` が追加され、`AGENTS.md` の最新 handoff pointer が更新された。
- これらは既存作業を含むため、次回は `git status --short` と `git diff --stat` を最初に再確認する。

`git diff --check` は QA-001 確認時点と handoff 作成後の再確認で成功している。文書 task のため、QA-001 では `npm run lint` / `npm run build` は実行していない。次の UI coding task では lint / build と runtime QA を行う。

## 6. 次の開始地点: UI-001

### UI-001 復習時 Summary の初期開示を制御

- 優先度: P1
- queue: `codex-queue/tasks-ui`
- 依存: D-03、D-04、SPEC-002（すべて完了済み）
- 主な実装対象: `src/app/notes/_components/note-detail-modes.tsx`
- 完了条件: Cue → 想起 → Summary / 本文確認の順序が UI とテスト観点で一致する。

現在の実装は復習モードでも Summary を常時描画するため、MVP 契約の「復習時 Summary 初期非表示」に達していない。本文の初期マスクと表示 / 再マスクは既存挙動として維持する。

### UI-001 で先に決めるべき挙動

- 復習開始時: Cue と概要は表示、本文と Summary は非表示。
- 「本文を表示」操作: 本文領域だけを開く。Cue / Summary / 復習操作の位置を組み替えない。
- Summary の開示操作: Cue と本文確認後に Summary を開く。既存の本文表示ボタンと同じ領域に置くか、Summary 見出しの開閉操作にするかを実装 task で具体化する。
- 復習済み更新: `POST /api/notes/:id/review`、`reviewedAt`、任意の `nextReviewDate` 更新は既存契約を維持する。
- 閲覧モードと編集モードへの切替、Markdown sanitize、削除確認 UI には回帰を入れない。

UI-001 は coding task として `tasks-ui` に投入し、実装完了後に 1440px の既存確認を再実行する。可能なら NTE-030 の 375 / 768px runtime も別 QA として確認するが、未確認を実装 task の完了条件から推測で PASS にしない。

## 7. 次回の最小 Read

raw log と `.next` は読まない。次の順で確認する。

1. `AGENTS.md`
2. `HANDOFF_2026-07-16.md`
3. `summary/20260716/current-spec-design-task-list.md`
4. `summary/20260716/0115-qa-001-reorganize-acceptance-evidence-20260716-summary.md`
5. `doc/implementation/MVP_CONTRACT.md`
6. `doc/implementation/IMPLEMENTATION_STATUS.md`
7. `src/app/notes/_components/note-detail-modes.tsx`
8. `doc/testing/TEST_SCENARIOS.md`

必要になった場合のみ `README.md` と `summary/20260715/0217-create-handoff-20260715-nte020-nte030-4ee10290-summary.md` を追加で読む。

## 8. 運用上の注意

- Worker task の `done` は、特に screenshot task では成果物の妥当性を保証しない。task status、summary、対象ファイルの存在・mtime・サイズ・寸法・内容を分けて確認する。
- summary の `Changes Made` に `.next`、`prisma/dev.db`、cache だけが並ぶ場合、指定成果物が未生成の可能性がある。
- 次の coding task でも作業前後に `git status --short` を取り、既存の未コミット変更を戻さない。
- 現在の一時タスクリストは全 Phase 2 実装・QA が終わるまで削除しない。未確認項目が残る状態で checklist や handoff を完了扱いにしない。
