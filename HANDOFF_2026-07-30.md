# Cornell Method Notebook 引き継ぎ資料

作成日: 2026-07-30（JST）
対象: 前回引き継ぎ以降の E2E 確認、Postgres migration source reader retry、Canvas scroll handoff、ノート用紙レイアウト、Markdown、タグ、日付、Cue / Summary 入力枠の Worker task と Manager 検証

この資料は、次回の Manager / Worker が会話ログや raw log を読み直さずに再開できるよう、確定事実・判断・未確認事項・検証結果・最小の再開入力をまとめたものです。ノート本文、秘密情報、接続 URL の実値、raw log、長い command output は記録しません。

> **Issue #53 / 参照範囲の注意**
>
> この handoff は PR #51 の clean checkout から再開できることを基準にする。本文で「記録元 worktree」「未コミット」「未マージ」と明記した内容は、別の branch / 作業ツリーで確認した履歴であり、PR #51 に含まれる確定済みの実装 fact ではない。未コミットの E2E 成果物や summary は履歴の背景としてのみ記録し、再開時の入力には使わない。再開に必要なローカル参照は、末尾の `Next Read` に列挙したコミット済みファイルに限定する。

## 1. 現在地

### 1.1 Branch と worktree

#### fact（記録元 worktree の観測）

- 記録元 worktree の branch は `agent/fix-date-editor-scroll-20260727`。
- 記録元 worktree の `git log -1 --oneline` は `34fa64e fix: align new note page spacing`。
- `git branch -vv` では現在 branch の upstream remote ref が `gone` と表示される。remote の PR 状態はこの handoff からは確定しない。
- 記録元 worktree の作業開始時 queue snapshot（2026-07-30 02:58 JST）は Common 211/212（完了176、失敗35、実行中1、待機0）、UI 150/150、API 24/24、Overall 385/386。実行中はこの handoff Worker だった。

上記の branch、HEAD、queue は記録時点の作業ツリーの観測値であり、PR #51 の clean checkout の branch / HEAD を示すものではない。再開時は手順1で取り直す。

作業開始時の `git status --short` には、今回の作業以前から次の未コミット変更があった。

- 追跡済み変更: `.gitignore`、`AGENTS.md`、`doc/testing/TEST_SCENARIOS.md`、`package.json` / `package-lock.json`、`scripts/postgres-migration-common.js`、`src/app/styles/`、ノート editor の UI source、`src/shared/markdown/markdown-field.tsx`、ノート / Postgres 契約テスト。
- 未追跡の E2E 成果物: `e2e/database-fixture.js`、`e2e/mvp-note-flow.spec.js`、`e2e/web-server.js`、`playwright.config.js`。
- 未追跡の既存 summary 群: `summary/20260727/`、`summary/20260729/`、`summary/20260730/` の Worker / Manager summary。
- `HANDOFF_2026-07-26.md` は作業開始時点ですでに `D` だった。今回その削除を戻していない。

#### 今回の変更範囲

- 新規作成: `HANDOFF_2026-07-30.md`
- 更新: `AGENTS.md` の「最新引き継ぎ」参照1行のみ
- 削除: `HANDOFF_2026-07-26.md`（作業開始時点ですでに削除状態）
- 上記以外の未コミット変更、未追跡 summary / E2E 成果物、`HANDOFF_2026-07-26.md` の既存削除状態は保持する。
- アプリコード、設定、依存関係、DB、生成物、Issue、PR、queue の `running` / `done` / `failed` ファイルは変更しない。Worker progress の更新は queue 運用メタデータであり、リポジトリ成果物ではない。

### 1.2 判断の扱い

- **fact（この資料での確認方法）**: Manager が記録元 worktree の実ソース、契約テスト、build、E2E 実行結果を確認できたものを「確認済み」とする。
- **PR #51 との境界**: UI / migration / E2E の検証結果には、PR #51 に含まれない未コミット変更を対象にしたものがある。3〜5章ではその場合を明記し、PR #51 の clean checkout に実装済みとは表現しない。
- **assumption**: 既存 worktree のアプリコード / E2E / summary 変更はこの task の入力として保全し、戻さない。
- **unknown**: 契約テストが通っていても、Browser backend が使えなかった機能の実画面・実入力は PASS と表現しない。
- 初回 / 再試行 E2E Worker の `GPT-5.3-Codex-Spark` 使用不可は、ユーザー判断により今回の実装評価では無視した。Worker failure 自体は記録として残し、Manager が実行できた E2E 結果を別に扱う。
- 旧 handoff からの判断を継承し、Issue #44（legacy データの「学習元種別なし + 学習元タイトル」）はユーザー判断で保留、「そのままでよい」とする。追加 task、コード修正、`Closes #44` は行わない。

## 2. このチャットでの時系列

以下の時系列は、PR #51 とは別の変更を含む記録元 worktree での履歴である。summary、E2E 成果物、UI / migration source の未コミット変更を使った確認結果は、PR #51 の clean checkout に含まれる実装 fact ではない。

| 時刻（JST） | 目的 / 対象 | 完了状態と主要な検証 |
|---|---|---|
| 7/29 16:53 | Canvas の page scroll handoff runtime 確認 summary | Worker summary は `done`。summary 自体は進捗ファイル中心で、内容妥当性は後続レビュー対象。CSS / 契約テストを実装根拠とし、Browser backend 不在のため実入力の runtime PASS にはしていない。 |
| 7/29 16:56 | Postgres migration source reader の native fallback retry（記録元 worktree / Issue #56・PR #50 の別 follow-up） | 記録元 worktree の Worker summary では、`scripts/postgres-migration-common.js` と `test/postgres/data-migration-contract.test.js` に native fallback 関連の変更があったと報告されている。これは Issue #56 / PR #50 に属する別 follow-up の source / test であり、PR #51 の clean checkout に含まれる確定 fact ではない。実 native load failure / `sqlite3` CLI / read-only import の運用確認も未完了。 |
| 7/29 17:45 / 17:55 | Playwright MVP E2E の初回実装 / fixture・runner retry | 両 Worker は `failed`。主因は model unavailable / unsupported と runner・sandbox の `Operation not permitted`。途中成果物は残った。失敗を E2E 実装不備の確定根拠にはしない。 |
| 7/29 18:10 | Manager による E2E 実装確認（未コミット作業ツリー） | 未コミットの E2E 成果物を含む記録元 worktree で、`npm run test:e2e` は Chromium の主要 MVP フロー3件が PASS。隔離 `prisma/e2e.db` は終了後に残らない。local bind は権限付き環境で実行。この実行は CI、mobile viewport、MVP の手動 backup API、PDF export、Phase 2 を対象外とした。この結果は PR #51 の clean checkout の E2E 実装 / PASS を意味しない。 |
| 7/29 23:31–23:40 | ノート用紙の外側 / 内側 / section / footer / Cornell 間余白を縮小、Markdown 入力/プレビュー切替（未コミット作業ツリー） | 記録元 worktree の Worker task は完了。`MarkdownField` は入力を初期表示とし、アクセシブルな「入力」「プレビュー」切替を追加。本文 Markdown と Summary が opt-in。Manager は targeted lint、build、差分確認を通過。UI source と確認結果は PR #51 に含まれない。 |
| 7/29 23:55–23:58 | 親子境界線の連続性を整理（未コミット作業ツリー） | 親 `note-paper-section` を水平境界の主担当にし、重複線を整理。mobile の Cue / 本文間の縦線を除去。契約テスト、全ノートテスト44件、対象 lint、build、diff check を確認。UI source と確認結果は PR #51 に含まれない。 |
| 7/30 00:16–00:20 | Cornell Cue / 本文 divider を grid 全高へ延長（未コミット作業ツリー） | Cue 列の `border-right` を外し、grid pseudo-element が全高を担当。640px 以下は縦線を隠し、Cue 下の横線を維持。契約テスト5件、全ノートテスト45件、targeted lint、build、diff check を確認。UI source と確認結果は PR #51 に含まれない。 |
| 7/30 00:30–00:33 | divider の横位置を右端から Cue 30% 境界へ修正（未コミット作業ツリー） | desktop は `inset-inline-start: 30%`、detail の900px以下は `max(30%, 12rem)`、640px以下は縦線非表示。契約テスト5件、全ノートテスト45件、build、diff check を確認。UI source と確認結果は PR #51 に含まれない。 |
| 7/30 00:42–00:55 | metadata と Cornell の大きな空白を除去し、小さな内側余白を追加（未コミット作業ツリー） | 作成画面だけ metadata / meta grid の下 padding を0にし、Cornell section 上部へ `clamp(0.375rem, 0.75vw, 0.625rem)` を追加。罫線は維持。契約テスト6件、全ノートテスト46件、targeted lint、build、diff check を確認。UI source と確認結果は PR #51 に含まれない。 |
| 7/30 01:02–01:40 | 長いタグの layout、30文字超過の再送、暗黙的切り捨てを修正（未コミット作業ツリー） | 長いタグは一行 `truncate` 表示、完全名は state/API と `title` に保持。失敗時は入力を消さず、同じ不正値を再送しても追加しない。native `maxLength` を除去し、30文字超過を明示エラーにした。Manager は全ノートテスト49件、対象 lint、build、diff check を確認。UI source と確認結果は PR #51 に含まれない。 |
| 7/30 02:08–02:20 | 学習日 / 次回復習日の date input 操作を調整（未コミット作業ツリー） | input 本体のクリックは `showPicker()`、非対応時は `focus()` fallback。date label は `preventDefault()` で代理クリックによる picker 起動を抑制し、`htmlFor` / `id` は維持。契約テスト8件、全ノートテスト50件、targeted lint、build、diff check を確認。実ブラウザ操作は未確認。UI source と確認結果は PR #51 に含まれない。 |
| 7/30 02:31–02:50 | Cue / Summary の枠線を section 外枠から直接入力フォームへ移動（未コミット作業ツリー） | 途中で追加した section frame は撤去し、Cue 各 `textarea` と Summary の Markdown `textarea` に四辺枠・角丸・内側余白を付与。create 用透明化 CSS との競合を解消。Manager は最終的に note-paper 契約7件、全ノートテスト51件、対象 lint、build、diff check を確認。UI source と確認結果は PR #51 に含まれない。 |

## 3. UI / API / Common の現在結果

### 3.1 UI: 記録元 worktree の最終状態（未コミット、PR #51 対象外）

以下は、PR #51 には含まれない記録元 worktree の UI source / 契約テストを対象にした確認結果である。UI の現行実装 fact として再利用する場合は、clean checkout で source とテストを再確認する。

- **用紙余白 / 境界線**: app-main、用紙ガター、用紙内側、section、metadata、footer、Cornell 列間の余白を縮小。水平区切りは親 section に集約し、grid の上下線や重複 border は除去。
- **Cornell divider**: desktop の Cue/本文境界は grid 全高の pseudo-element を `30%` に配置。detail の900px以下では Cue の最小幅を考慮し、640px以下では縦 divider を隠して Cue 下の横区切りへ切り替える。右端へ置く指定はない。
- **metadata handoff**: 新規作成画面では metadata section と meta grid の下端を詰め、Cornell section 直後にだけ小さな内側余白を残す。既存詳細画面の通常余白は巻き込まない。
- **Markdown**: `MarkdownField` は入力 / プレビューの縦切替を持ち、初期 view は入力。本文 Markdown と Summary に適用。Preview の GFM、sanitize、表示専用 checkbox という既存契約は維持し、Canvas 本文は Markdown preview の対象にしない。
- **Cue / Canvas scroll**: Cue と Canvas に同じ `min(70vh, 48rem)` の縦方向の高さ基準を持たせ、Cue list は項目がある場合だけ縦 scroll、Canvas は用紙内の横 scroll と viewport の縦 scroll を分離。scroll boundary でページ scroll を止めない CSS 契約と mobile の page scrolling を維持する。実際の wheel / trackpad / touch handoff は未確認。
- **タグ**: 30文字以内、最大12件、重複排除、既存候補 / 新規追加を維持。長いタグ chip は一行で省略表示し、完全名を `title` と保存値に保持。`maxLength` による黙った切り捨ては除去し、超過値は追加時にエラー表示して拒否。成功時だけ入力を消去し、編集・削除時にローカルエラーを更新する。
- **日付**: date input の表示領域クリックで picker を開き、`showPicker()` が使えない場合は focus fallback。label クリックは picker を開かない。text input には date 用処理を適用しない。JST の日付のみ判定と、次回復習日の配置は旧 handoff の確定状態を継承。
- **Cue / Summary の入力枠**: section 全体の外枠ではなく、Cue の各 textarea と Summary の入力 textarea に直接 border / radius / padding を付与。Summary の preview 面は同じ入力枠の対象にせず、追加・削除・scroll・入力/preview 切替を維持。
- **その他の継承事項**: 学習元タイプ未選択時は学習元タイトルだけを disabled とし、未選択へ戻すと stale な学習元タイトルを payload 前に空にする。主タイトルは編集可能。Canvas の page 寸法変更は表示倍率ではなく用紙寸法を変更し、既存要素の geometry / style / points を自動変形しない。

### 3.2 API / migration（記録元 worktree とコミット済み基準の混在に注意）

この節は、PR #51 の clean checkout で確認できる内容と、記録元 worktree または Issue #56 / PR #50 の別 follow-up に依存する内容を分離する。後者の source / 契約テストを PR #51 の実装 fact として扱わず、migration 継続時は clean checkout のコミット済み source / test と別 follow-up の変更を明示的に分けて再確認する。

#### fact

- Local SQLite と hosted PostgreSQL を共存させる構成を維持。Postgres 用 schema、generated client、baseline migration は SQLite の schema / migration history と分離している。
- runtime の `DATABASE_URL` と Prisma CLI / migration の `DIRECT_URL` を分離し、hosted Preview / Production の DB 未設定・SQLite fallback・認証設定不備は fail closed とする。hosted page と `/api/*` は単一ユーザー Basic Auth の契約を持つ。
- SQLite snapshot から Postgres baseline へ import する operator workflow は、row count、ID / FK、scalar、Canvas `document_json`、page / element geometry、points / style、`search_text` の照合を前提とする。Postgres export / restore / retention は direct connection と operator 管理の外部 path を使い、Vercel filesystem / repository / `.next` / Local SQLite backup を Production backup 保存先にしない。
- `bodyMode: "canvas"` の Canvas document と text 要素由来の `searchText` を既存 Canvas JSON 保存領域で扱う。用紙サイズ変更だけを理由に Prisma migration は追加しない。
- PR #51 の clean checkout で確実に言えるのは、`scripts/postgres-migration-common.js` の source reader が `better-sqlite3` の処理で `error.code === "MODULE_NOT_FOUND"` になった場合に `sqlite3` CLI を検査し、`-readonly` と `PRAGMA query_only=ON` を付けた read-only query 経路へ進む既存コードがあることまでである。これは `ERR_DLOPEN_FAILED` などの native load failure を扱えること、また実環境でその経路へ到達できることを示さない。
- PR #51 の clean checkout にある `test/postgres/data-migration-contract.test.js` には、native load failure を発生させて fallback を確認する契約テストはない。記録元 worktree で確認された native fallback 実装・契約テストは、Issue #56 / PR #50 の別 follow-up に属する変更としてのみ記録する。

#### unknown / 保留

- Issue #56 / PR #50 の別 follow-up に属する native source reader fallback は、PR #51 の clean checkout の確定 fact ではない。clean checkout にある `MODULE_NOT_FOUND` 経路と native load failure 用の変更を混同せず、実際の `better-sqlite3` native load failure、環境側の `sqlite3` CLI、read-only snapshot / import を通した operator 実行、そして native load failure が fallback へ実際に到達することは未確認。前回 retry Worker の失敗 / summary の内容未レビューもあり、移行運用上は未解決として扱う。
- 実際の Supabase project / Vercel Preview・Production への接続、migration、既存 SQLite import、再デプロイ後の永続性、export / restore drill、hosted Basic Auth smoke、Production cutover は未実施。公開準備の code / contract であり、公開完了とは扱わない。
- UI / E2E task による API route の追加はなく、既存の MVP API 契約を維持している。API の hosted 実環境応答時間や実データを使った契約は別途確認が必要。

### 3.3 Common / E2E（未コミット作業ツリーの記録、PR #51 対象外）

- E2E の隔離 fixture は専用 `prisma/e2e.db` を使い、既存 `prisma/dev.db` を保護する。Next server lifecycle と Playwright Chromium 設定は作業ツリーに残っている。
- 初回 Worker は model unavailable / unsupported、再試行 Worker は model / runner / sandbox の制約（local bind の `Operation not permitted` を含む）で failed。これらは Worker 実行状態であり、Manager の最終 E2E PASS と分けて扱う。
- `GPT-5.3-Codex-Spark` をこの Worker で使えないことは、ユーザー判断で無視した。別モデル / Manager 実行で `npm run test:e2e` の結果を確認済み。

## 4. E2E の正確な状態（未コミット作業ツリーの確認。PR #51 の fact ではない）

### fact

- 未コミットの E2E 成果物を含む記録元 worktree で、`npm run test:e2e` は主要 MVP フロー3件が PASS。対象は redirect、ノート作成、詳細 / 編集 / 保存、一覧 query、review、delete の主要経路で、Chromium の隔離 DB 実行。これは PR #51 の clean checkout に E2E 実装または同じ PASS があることを示さない。
- Manager 検証では記録元 worktree の `prisma/e2e.db` が終了後に残らないこと、local server bind が必要なため権限付き環境で再実行したことを確認した。
- E2E 実装時点の記録元 worktree で、`node --test test/notes/*.test.js test/postgres/*.test.js` は52件 PASS、`npm ci --dry-run --ignore-scripts` も成功した。source / test の未コミット変更を含むため、PR #51 の clean checkout の結果には読み替えない。

### unknown

- GitHub Actions / CI 上の E2E、Chromium install cache、mobile viewport は未確認。
- この E2E 実行では MVP の手動 backup API、PDF export、Phase 2（Undo、soft delete、追加カード等）は coverage 外。PDF export と Phase 2 は MVP 外の確定境界であり、MVP 受け入れ範囲の判断待ちを意味しない。
- 実ブラウザでのスクリーンショット、視覚確認、入力操作は Browser backend が利用できなかったため、このチャットでは未実施。E2E PASS と Browser backend の視覚 QA は別物として扱う。

## 5. Manager 検証済みコマンドと結果（記録元 worktree。PR #51 の clean checkout の結果ではない）

| コマンド / 確認 | 結果 |
|---|---|
| `npm run test:e2e` | PASS、Chromium 3 tests。local bind のため権限付き環境で実行。 |
| `node --test test/notes/*.test.js` | 直近（Cue / Summary 入力枠確認後）は51 passed。 |
| `node --test test/notes/note-paper-spacing-contract.test.js` | 直近は7 passed。 |
| `node --test test/notes/editor-metadata-contract.test.js` | date input 確認後は8 passed。 |
| note-paper / Markdown の対象契約テスト | 記録元 worktree の確認時点で12 passed。以後の note-paper 更新も対象テストで確認済み。この結果は未コミット UI source を対象にした履歴である。 |
| 対象ファイルの `npx eslint ...` | UI TSX と契約テストは成功。CSS lint は設定対象外 warning のみで error なし。 |
| `npm run build` | 記録元 worktree では成功。未コミット source を含むため、PR #51 の clean checkout の結果には読み替えない。 |
| `git diff --check` | 記録元 worktree では成功。 |
| `npm run lint` | **記録元 worktree では失敗（未追跡 E2E JavaScript の8 errors）**。E2E 成果物と Playwright 設定が未コミットのため、clean checkout では同じ失敗になるとは限らない。今回の handoff task の対象外であり、修正していない。 |

## 6. Remaining Unknowns / 次回に必要な判断

### 確定済みの MVP / Phase 2 境界

`doc/implementation/MVP_CONTRACT.md` を正本とし、MVP と Phase 2 以降の境界は確定済みとして扱う。以下は次回に MVP へ含めるかを再判断する項目ではない。

- 現行 MVP に含むバックアップは、`/backup` の SQLite DB 手動コピー作成と最新 3 世代の一覧確認（`GET /api/backups` / `POST /api/backups`）に限る。
- PDF / HTML export、期間 export、起動時自動バックアップ、backup log、retry UI は MVP 外とし、別の検証 task / Phase 2 task として扱う。
- DB / API の Undo、soft delete、`SoftDeleteBuffer`、期限切れ purge、カード単位復元、および MVP 契約 §9 に列挙されたその他の高度機能は MVP 外の確定境界とし、Phase 2 以降の別 task として扱う。Canvas の client-side Undo / Redo は DB / API の Undo とは別の MVP 機能である。

### Remaining Unknowns

- Browser backend の復旧後、同じ viewport で用紙余白、Cornell divider の終端 / 位置、長いタグ、date picker の input / label 挙動、Cue / Summary の直接枠、Cue / Canvas の page scroll handoff を目視・実入力で確認する。
- PR #51 の clean checkout にある `MODULE_NOT_FOUND` 経路と、Issue #56 / PR #50 の別 follow-up に属する native source reader fallback を分けて扱い、実 native load error と `sqlite3` CLI がある operator 環境で read-only snapshot / import まで通す。clean checkout の既存経路だけでは native load failure の到達可能性を確認できない。
- Vercel / Supabase hosted 実環境で env fail-closed、Basic Auth、migration、既存データ照合、再デプロイ永続性、backup / restore drill を確認する。これは実環境の運用検証として残る unknown であり、MVP / Phase 2 の scope 判断とは分離する。
- GitHub Actions / CI 上の E2E、Chromium install cache、mobile viewport は未確認の運用・環境検証として別 task で扱う。PDF / HTML export、起動時自動バックアップ、backup log / retry UI、Undo / soft delete などの Phase 2 機能は、MVP 受け入れの未確認項目には含めない。
- `npm run lint` の E2E JavaScript `require()` 8件を修正するか、E2E JavaScript の lint 方針を設定として明示するかは未決定。この handoff task では判断・修正しない。
- PR #41 の remote open / non-draft、checks、base/head、Issue close 状態は GitHub 接続後に再取得する。旧 handoff の引き継ぎ値だけで確定しない。

### 次回に必要な判断

1. 直近 UI の Browser 実機確認を先に行うか、E2E lint / CI を先に整えるか。
2. hosted migration を実行する前に native fallback の operator smoke を行うか。

MVP / Phase 2 の境界を再判断する項目はない。上記の未実施確認は、確定済みの境界に沿った運用検証または別 task として扱う。

## 7. 次回の最小再開手順 / Next Read

### 手順

1. `git status --short`、`git branch -vv`、`git log -1 --oneline` を実行し、既存の未コミット変更を戻さない。
2. `codex-queue/bin/worker-status.sh` で queue の現在値を取り直す。
3. GitHub 接続が利用可能なら `gh pr view 41` と `gh pr checks 41` で PR #41 の remote state を再確認する。
4. Browser backend が利用可能なら、スクリーンショットではなく実画面で直近 UI を確認する。利用できない場合は CSS / contract test の結果を視覚 PASS に読み替えない。

### Next Read（最小）

- `HANDOFF_2026-07-30.md`
- `AGENTS.md`
- `doc/implementation/MVP_CONTRACT.md`
- `doc/testing/TEST_SCENARIOS.md`
- UI 継続時: `src/app/styles/note-paper.css`、`src/shared/markdown/markdown-field.tsx`
- UI editor 継続時: `src/modules/notes/ui/components/editor/body.tsx`、`src/modules/notes/ui/components/editor/cues.tsx`、`src/modules/notes/ui/components/editor/editor.tsx`、`src/modules/notes/ui/components/editor/inputs.tsx`、`src/modules/notes/ui/components/editor/metadata.tsx`、`src/modules/notes/ui/components/editor/summary.tsx`、`src/modules/notes/ui/components/editor/tags.tsx`
- UI 契約: `test/notes/note-paper-spacing-contract.test.js`、`test/notes/editor-metadata-contract.test.js`、`test/notes/markdown-preview-contract.test.js`、`test/notes/canvas-scroll-contract.test.js`
- migration 継続時（まず PR #51 の clean checkout を確認）: `scripts/postgres-migration-common.js`、`test/postgres/data-migration-contract.test.js`、`README.md`、`doc/technical/MVP_TECHNICAL_DESIGN.md`。Issue #56 / PR #50 の native fallback を確認する場合は、別 PR / branch の source / test を明示的に取得し、clean checkout の事実と混ぜない。
- E2E / CI 継続時: `doc/testing/TEST_SCENARIOS.md`。E2E fixture / runner / Playwright 設定は PR #51 の clean checkout に含まれないため、別 task / branch の変更として再取得する。

以上。次の task は、ここに記載した unknown を確認済みと表現せず、運用検証の順序・方針に関する必要な判断を Manager / 発注者へ戻してから進める。MVP / Phase 2 の境界判断は再開対象ではない。
