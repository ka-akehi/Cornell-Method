# Issue #88 / #89 Manager Triage Summary

作成日: 2026-08-03（JST）

## Scope

現在の未解決 Issue のうち、PR #84 の最新 HEAD `4890c780` に対する Code Review follow-up を確認した。

- #88: desktop rail が breakpoint で非表示になる際、rail 内のブランド・ナビリンク・新規ノートへフォーカスがあっても mobile menu button へ復帰しない。
- #89: `--app-accent` の塗り（`#b9681d`）と淡色文字の組み合わせが、通常サイズ文字の WCAG AA 4.5:1 を満たさない。notes list の「新規作成」だけでなく、backup 作成ボタン、Cue 番号、その他の同じ filled control も影響範囲に含む。

Issue #85 / #86 / #87 は前回対応済みで、今回の新規実装 task には重複投入しない。

## Queue routing

| Issue | Queue | 目的 | 依存 |
| --- | --- | --- | --- |
| #88 | `codex-queue/tasks-ui` | desktop rail 内の active element を breakpoint focus 復帰判定へ含める | なし |
| #89 | `codex-queue/tasks-ui` | accent-filled controls の foreground/background コントラストをAA基準以上へ是正する | なし |

2 task は対象領域が分離され、相互依存しないため並行投入する。#89 は共通色 token または複数UIの filled state に触れるが、#88 の AppChrome focus ロジックとは独立している。

## Acceptance boundary

- #88 は rail 内のフォーカスを失わせず、desktop→mobile breakpoint 変更時に表示中の mobile menu button へ復帰する。既存の mobile open、rail handle、Escape/backdrop、pathname change のフォーカス挙動を壊さない。静的回帰契約と lint/typecheck を実行する。
- #89 は影響対象の filled control について、通常サイズ文字のコントラスト比を少なくとも 4.5:1 とし、hover/focus/disabled の状態と既存の意味づけを維持する。色値または filled-state の選択を回帰テストで検証し、list/backup/Cue/paper/AppChrome の影響範囲を確認する。

Runtime browser QA は前回 handoff の記録どおり browser surface が利用可能になるまで未確認として扱う。

## Next Read

1. `HANDOFF_2026-08-03.md`
2. `summary/20260803/issues-88-89-triage-20260803.md`
3. `src/app/_components/app-chrome.tsx`
4. `src/app/styles/foundation.css`
5. `src/modules/notes/ui/components/list/list.tsx`
6. `src/modules/backup/ui/components/backup-page.tsx`
7. `src/modules/notes/ui/components/editor/cues.tsx`
