# GitHub Issues #85–#87 triage

## Scope

- Repository: `ka-akehi/Cornell-Method`
- Current branch: `agent/polish-notebook-ui-20260803`
- Current HEAD: `579e7a1`
- Open issues confirmed through the GitHub connector: #85, #86, #87
- All three issues have no comments.

## Findings

### #85 — close mobile navigation on pathname changes

- Queue: UI
- Target: `src/app/_components/app-chrome.tsx` and AppChrome contract tests
- Fix: add a pathname-change effect that closes the mobile overlay without calling the focus-restoring close handler. Existing Escape/click close and body-scroll cleanup must remain unchanged.

### #86 — keep the full collapsed rail handle onscreen

- Queue: UI
- Target: `src/app/styles/app-shell.css` and responsive contract test
- Fix: keep the open-state edge placement, but move the collapsed handle fully into the main-content side so the full 2.75rem hit area is visible despite the collapsed rail width and horizontal clipping.
- Dependency: after #85 because both may update AppChrome contract tests.

### #87 — update handoff after committing the redesign

- Queue: Common
- Target: `HANDOFF_2026-08-03.md`
- Fix: after #85/#86 are committed, update branch, HEAD, worktree, adopted scope, and verification state. Do not create a new handoff file unless the date changes; do not modify source code.
- Dependency: after Manager commits the UI fixes.

## PR handling

- Keep fixes on PR #84 (`develop` base, non-Draft Open PR).
- Before final PR update, add `Closes #85`, `Closes #86`, and `Closes #87` to the PR body, as required by repository instructions.
- Do not stage `summary/` or `floating-tooltip-mockup.png`.

## Next Read

- `src/app/_components/app-chrome.tsx`
- `src/app/styles/app-shell.css`
- `test/notes/app-chrome-responsive-contract.test.js`
- `HANDOFF_2026-08-03.md`

