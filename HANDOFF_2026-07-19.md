# Cornell Method Notebook 引き継ぎメモ

作成日: 2026-07-19（JST）  
対象: 紙面中心 UI から Canvas 本文へ移行した一連の作業

## 1. この handoff の目的と読み方

この資料は、2026-07-17 の旧 handoff 以降、2026-07-19 までに行われた紙面 UI の調整、overview の整理、Markdown Preview の改修、Canvas 本文の導入、用紙サイズ・スクロール・消しゴム・直線／矢印・toolbar の作業を、次の Manager / Worker が短時間で再開できるようにまとめたものです。

次回は、まずこの handoff の「現在の全体状態」「確定仕様」「未確認事項」「Next Read」を読みます。raw log、長い command output、`.next` の生成物は根拠にしません。個別 Worker の完了 summary は実装経緯を追うための参照先として記載しますが、現在のコードと `doc/implementation/MVP_CONTRACT.md` を優先します。

仕様の読み分けは次のとおりです。

- 現行 MVP の route、API、保存、削除、復習、Canvas の受け入れ判断は `doc/implementation/MVP_CONTRACT.md` が正本です。
- `AGENTS.md` と設計 docs にある autosave、Undo、専用復習タスク、カード分割、PDF などは、明記がない限り Phase 2 以降のロードマップです。
- 本資料では「現行コードで実装済み」「静的には確認できるがブラウザ未確認」「設計のみ／将来案」を分けて記録します。

## 2. 現在の全体状態、未コミット変更、検証方針

### 2.1 現在の実装状態

- アプリは Next.js App Router、React、Prisma、SQLite の既存構成を維持しています。
- ノートの見た目は、深いフォレスト色の共通 chrome と暖色の一枚紙面を主役にする紙面中心 UI です。Cue は左、本文は右、Summary は下という Cornell の骨格を維持します。
- Canvas 本文の新規ノートは `bodyMode="canvas"` を初期値にします。既存の Markdown 本文は `bodyMode="markdown"` として互換保持し、自動変換しません。
- Canvas の正本はアプリ所有の `CanvasDocumentV1` です。Fabric.js は編集・閲覧 renderer として使い、Fabric 固有 JSON を DB の正本にはしません。
- Canvas の保存は既存の `POST /api/notes`、`PATCH /api/notes/:id` に全体 payload として含め、`NotebookCanvas` の `documentJson` と `searchText` を同一 transaction で保存します。
- `Notebook` の旧 `overview` 列は削除済みです。現在の基本項目はタイトル、学習日、学習元、タグ、Cue、Canvas または legacy Markdown 本文、Summary、次回復習日、最終復習日時です。

### 2.2 未コミット変更の扱い

作業開始時点で、今回の Canvas 作業に関係する多数の未コミット変更がありました。`git status --short` で確認できた主な領域は、次のとおりです。

- `AGENTS.md`、`CURRENT_STATUS.md`、MVP 契約・実装状況・API・data・ER・screen・testing などの docs
- Prisma schema と Canvas / overview 関連 migration
- Canvas renderer、toolbar、note editor、detail modes、共通 CSS、API / repository / mapper
- Canvas 用依存関係と lockfile
- `summary/20260717/`、`summary/20260718/` の完了 summary

これらは発注者の既存作業として保持します。この handoff task で意図する変更は、次の documentation 操作だけです。

1. `HANDOFF_2026-07-19.md` を追加する。
2. `AGENTS.md` の「最新引き継ぎ」をこのファイルへ更新する。
3. 内容を統合した後に `HANDOFF_2026-07-17.md` を削除する。

既存のコード、schema、migration、依存関係、DB、設定、テスト、画像、summary、queue を戻しません。発注者の指示なしに commit、reset、checkout、rebase、既存 task の削除／再投入を行いません。

### 2.3 検証方針と現在の証跡

今回の Manager 再確認では、次が成功しています。

| コマンド | 結果 | 備考 |
| --- | --- | --- |
| `npm run lint` | PASS | ESLint 成功 |
| `npx tsc --noEmit --pretty false` | PASS | 型検査成功 |
| `npm run build` | PASS | Next.js webpack build、TypeScript、route 生成成功 |
| `git diff --check` | PASS | whitespace error なし |

ブラウザ実機の目視・操作 QA は完了扱いにしません。これまでの Worker summary では in-app Browser が利用できず、ローカル Next server も環境によって `listen EPERM` となっていました。したがって、Canvas の pointer 操作、保存・再読込、実際の wheel / touch scroll、各 viewport の見た目は未確認です。静的検証が成功したことは runtime PASS の代替ではありません。

`CURRENT_STATUS.md` と `doc/implementation/IMPLEMENTATION_STATUS.md` には、用紙サイズ UI が未実装だった時点の記載が一部残っています。最新 code と 2026-07-18 の task 結果では数値入力・実寸描画まで進んでいますが、runtime QA 未確認です。次回の QA 後に実装状況 docs をコード・契約・実機結果へ再照合してください。

## 3. このチャットの時系列整理

### 3.1 紙面 UI の線、ヘッダー、余白

2026-07-17 夜から 2026-07-18 朝にかけて、新規ノート画面で次の問題を順に調査しました。

- 新規ノートだけ共通ヘッダーの brand、nav、状態表示の位置がずれる。
- 紙面 shell、section、Cornell、Cue textarea、Preview がそれぞれ線を持ち、余計な水平線やヘッダー重複が見える。
- 余白を詰めたいが、本文を読み書きするための行数や本文領域そのものを勝手に縮めるべきではない。

`new-note-paper-lines-header-fix` 以降の修正で、共通 header の構造を整理し、紙面 rule と section divider の重複を減らし、create 状態の重複表示を抑えました。最終判断は「余計な線と上下の padding / margin を調整する。ただし本文の行数、本文の min-height、本文の記載領域は勝手に縮めない」です。余白の修正を本文量の削減で実現しないことを、今後の UI 修正でも維持します。

### 3.2 overview と Summary の整理

作業途中で、紙面モックにあった `overview` と、Cornell の下部にある `Summary` が別概念であることを確認しました。

- `overview`: 旧来の基本情報寄りの概要欄。本文を書くための本質的な領域ではない。
- `Summary`: ノート本文を要約し、次のアクションを書く Cornell の下部領域。現行 MVP で残す。

「概要を残す」意図と「Summary を残す」意図を混同しないようにし、`overview` は UI、schema、validation、payload、repository、mapper、検索、migration から end-to-end で削除しました。削除時には、旧 `overview` 列を参照する保存経路が残っていたため、overview カラム不在による保存エラーも発生しました。その後、Prisma schema / migration、API 入力、form state、repository、mapper、生成 client を同期し、現行 DB と通常の保存経路を `overview` なしへ合わせました。

関連 docs と ER 図も同期しました。現行の API / data / implementation / requirements の記載と ER の current source / SVG は更新済みです。一方、過去時点の review snapshot や歴史的な mock HTML に `overview` という語が残ることがあります。それらは当時の記録であり、現行 schema や MVP 契約の根拠として扱いません。

参照 summary:

- `summary/20260718/1015-remove-overview-field-end-to-end-6f3c1a2e-bdee76bb-summary.md`
- `summary/20260718/1038-sync-docs-after-removing-overview-8d2b7f41-28e2212e-summary.md`
- `summary/20260718/1044-sync-current-er-diagram-after-overview-removal-2f4a6c81-998f382a-summary.md`

### 3.3 Markdown の soft line break

Cue と Summary の Markdown Preview で、Markdown の単一改行を Preview に反映する soft line break 対応を入れました。初期の依存関係追加と build で問題が出たため、依存関係と build を整理し、最終コードは `remark-gfm` と `rehype-sanitize` を維持しながら、`markdown-field.tsx` 内の小さな remark plugin で text node の改行を `<br>` 相当へ変換します。code / inlineCode の内容は変換しません。Preview の checkbox は read-only、クリック時に編集値を変更しない契約を維持します。

参照 summary:

- `summary/20260718/1054-enable-markdown-soft-line-breaks-9e72c4b1-fddd5a5b-summary.md`
- `summary/20260718/1103-resolve-markdown-soft-break-dependency-build-4c6e91b2-3b4d6e65-summary.md`

### 3.4 Markdown 本文から自由入力 Canvas へ

本文を Markdown のカードや複数欄へ分割する方向ではなく、図、線、矢印、手書きストローク、文字を同じ座標系で扱える bounded Canvas へ方針転換しました。Cornell の学習順序を壊さないため、Cue と Summary は残し、Canvas は本文領域だけに置きます。Canvas 本文と Markdown editor を同じ本文欄に併存させる dual canonical は採用しません。

最初の technology spike では Fabric / Konva と保存形式、SSR、focus、scroll、app-owned JSON の境界を確認しました。最終のノート UI は Fabric adapter を採用し、Fabric 固有の stage JSON ではなく `CanvasDocumentV1` を保存する方針になりました。

参照 summary:

- `summary/20260718/1113-freestyle-canvas-policy.md`
- `summary/20260718/1118-design-freeform-note-canvas-direction-5b8d2e71-b3fcf7c6-summary.md`
- `summary/20260718/1238-canvas-editor-technology-spike-9344817c-summary.md`
- `summary/20260718/1248-resolve-canvas-spike-dependencies-and-runtime-qa-b38c8cc9-summary.md`

### 3.5 Canvas 永続化 API / DB / UI 統合

Canvas 永続化は、既存の notes API を拡張する形で進めました。

1. `Notebook.bodyMode` と 1:1 の `NotebookCanvas` を追加し、Canvas JSON と text 検索用 `searchText` を SQLite に保存する境界を定義しました。
2. `notebookInputSchema` で `bodyMode` と `canvas` の整合性、`CanvasDocumentV1` の version、寸法、要素数、point 数、serialized size を検証するようにしました。
3. create / update repository が Notebook、Cue、Tag link、Canvas を transaction で保存し、Canvas text 要素から `searchText` を再生成するようにしました。
4. detail mapper が保存済み JSON を復元し、Canvas 欠落や version 不一致を空白へ黙って置き換えずエラーにします。
5. note editor と detail view / review に Canvas editor / viewer を統合しました。新規は Canvas を初期選択し、既存 Markdown は互換表示します。

この途中で、overview 列を削除した DB に古い overview 参照が残った保存エラーが発生しました。overview 削除の end-to-end 同期を先に完了させ、Prisma client / build を更新してから Canvas の保存経路を再確認しました。用紙サイズ変更のために別 API や別 DB column を追加することはしていません。

参照 summary:

- `summary/20260718/1312-add-canvas-persistence-api-contract-96a6002c-summary.md`
- `summary/20260718/1334-integrate-hybrid-canvas-note-ui-fa9249f5-summary.md`
- `summary/20260718/1900-make-body-full-freeform-canvas-1f35709e-summary.md`

### 3.6 focus、useEffect、用紙サイズ、実寸描画

- Canvas 初期化の `useEffect` dependency に関する lint / runtime 上の問題を、callback / ref の境界を整理して解消しました。Fabric の初期化・cleanup と保存済み document の再反映を分離しています。
- Canvas に focus したときにブラウザが本文欄へ scroll jump する問題は、viewport の focus に `preventScroll: true` を使うことで抑えました。Canvas の shortcut は Canvas viewport に focus がある場合だけ処理します。
- `CanvasDocumentV1.page.width / height` を可変の整数 px とし、入力 UI、範囲 validation、保存済み document の hydration を追加しました。親の note `<form>` の submit と Canvas の用紙サイズ適用が競合しないよう、toolbar の button type、ref、`pageKey`、uncontrolled input の初期値更新を整理しました。
- Canvas を zoom のように縮小表示するのではなく、用紙そのものを指定寸法で Fabric / DOM に設定する実寸描画へ変更しました。

参照 summary:

- `summary/20260718/1910-prevent-canvas-focus-scroll-jump-3623b5ef-summary.md`
- `summary/20260718/1939-support-variable-canvas-paper-dimensions-d6fbe36a-summary.md`
- `summary/20260718/2004-document-canvas-paper-size-spec-5bd99c66-summary.md`
- `summary/20260718/2020-add-canvas-paper-dimensions-inputs-134fa1b6-summary.md`
- `summary/20260718/2027-fix-nested-canvas-size-form-hydration-21854123-summary.md`
- `summary/20260718/2147-render-canvas-paper-at-actual-size-39c61f6c-summary.md`

### 3.7 Canvas 上のスクロール

Canvas 上で wheel / Fabric がページ縦スクロールを阻害する原因を調査しました。manual wheel forwarding や親要素の `overflow-y: hidden` で無理に制御する案は採用していません。最終的に、次の責務分離へ収束しました。

- ページ全体の縦スクロールは通常 flow のまま維持する。
- 用紙が本文列より広い場合だけ `.note-canvas-horizontal-scroll` が `overflow-x: auto` で横スクロールする。
- 水平 wrapper の `overflow-y` は `clip` とし、そこを縦 scroll container にしない。
- `note-canvas-viewport` は `overflow: visible`。Fabric は `allowTouchScrolling: true` とする。
- Canvas の上に fixed overlay を置かず、Summary / footer へ wheel、trackpad、touch で戻れることを優先する。

参照 summary:

- `summary/20260718/2201-fix-canvas-wheel-scroll-4eb96530-summary.md`
- `summary/20260718/2213-restore-page-scroll-over-canvas-8ad5ce17-summary.md`
- `summary/20260718/2223-fix-fabric-page-scroll-over-canvas.md`
- `summary/20260718/2224-fix-fabric-scroll-blocking-86317fec-summary.md`
- `summary/20260718/2238-diagnose-canvas-page-scroll-blocker.md`
- `summary/20260718/2240-diagnose-page-scroll-blocker-over-canvas-b743419b-summary.md`
- `summary/20260718/2243-change-canvas-horizontal-overflow-to-clip-7572b44c-summary.md`

### 3.8 消しゴム、直線・矢印、toolbar

- 「消去」は意味が曖昧だったため、現行 UI の `erase` を「消しゴム（全体）」へ変更しました。クリックまたはなぞりで Fabric の hit target を object 単位で削除し、1 gesture を最大 1 つの history entry にします。
- 自由線の一部だけを切る部分消去は、現行実装へ混ぜず設計 doc だけを作成しました。最初の対象は `stroke` のみで、line / arrow / shape / text は whole erase の対象です。
- 直線・矢印が左上から伸びる問題は、page 座標と Fabric の local / group 座標の変換を修正しました。移動後の point を変換して bounds を再計算し、矢印の group の位置と矢尻も整合させます。
- 既存 object の上から line / arrow / shape / pen を開始して余計な線を作る問題は、既存 target がある場合に drag draft を開始しない guard を維持・修正しました。
- draw.io 風 toolbar は、操作、描く、線、図形、文字、消去、履歴、用紙に役割を分け、active state、ARIA、tooltip、responsive local rail を実装しました。

参照 summary:

- `summary/20260718/2257-canvas-eraser-tool.md`
- `summary/20260718/2258-replace-delete-with-eraser-tool-1b7231e4-summary.md`
- `summary/20260718/2304-design-partial-canvas-eraser-21708862-summary.md`
- `summary/20260718/2327-fix-fabric-line-arrow-coordinates.md`
- `summary/20260718/2329-fix-canvas-line-arrow-placement-movement-563d27c2-summary.md`
- `summary/20260718/2322-design-drawio-like-canvas-toolbar-95dfafe0-summary.md`
- `summary/20260718/2342-implement-drawio-like-canvas-toolbar-b338cd7d-summary.md`

## 4. 確定した製品・UI 仕様

### 4.1 現行 MVP として確定したこと

- 本文は bounded Canvas を主とする。Cue と Summary は残す。
- `bodyMode="canvas"` の本文欄に Markdown editor / Markdown Preview を併存させない。Canvas の text element は plain text であり、Markdown/GFM 本文ではない。
- 既存 `bodyMode="markdown"` のノートは互換表示する。既存 Markdown を Canvas へ自動変換しない。
- `overview` は現行のノート項目ではない。下部の `Summary / 要約と次の一歩` を残し、overview と Summary を同じ欄として再導入しない。
- Cue と本文はデスクトップで約 30% / 70%。Cue は通常の DOM input / Markdown text list、本文は Canvas です。
- 保存は現行 MVP の明示保存です。editor の変更を確定するのは「保存」ボタンで、Canvas の client history は DB へ保存しません。
- 削除は確認後の物理削除、復習は `/notes/[id]` 内の Cue → 本文確認 → Summary 確認です。autosave、soft delete、5 秒 Undo、専用復習 task は今回の Canvas 実装に含めません。

### 4.2 用紙サイズ

- `CanvasDocumentV1` の default page は幅 1200 px、高さ 800 px、背景 `paper`。
- 幅・高さは各 320〜4000 px の整数入力です。
- 入力値は zoom / Fit / 50% / 100% / 200% ではなく、用紙そのものの寸法です。
- 用紙サイズ変更で既存要素の `x`, `y`, `width`, `height`, `points`, `style` を自動変更しません。
- 境界外になる要素も削除、移動、縮小、clip しません。後から用紙を広げたときに同じ page 座標で復元できるよう JSON をそのまま保持します。
- 用紙サイズ変更は既存 `NotebookCanvas.documentJson` の更新だけで完結します。寸法専用の Prisma migration、DB column、resize API は追加しません。

### 4.3 Canvas 操作の境界

- `選択`は既存 object の選択、移動、resize 用です。
- `ペン`、`直線`、`矢印`、`四角`、`円`、`テキスト`は空白から新規作成する tool です。
- 描画 tool で既存 object 上を pointer down した場合は新規 object を作らず、既存 object を意図せず移動・変形しません。既存 text 上の text 操作など、対象 object へ入る処理は別途 runtime で確認します。
- tool は選択後も active のまま連続描画できる sticky tool とします。tool 選択、focus、tooltip、描画開始をキャンセルした no-op は Canvas document history に積みません。
- Canvas の Undo / Redo は client memory の snapshot です。現行 history は最大 50 snapshot で、DB や API の Undo ではありません。

### 4.4 消しゴム

- 現行の表示名は `消しゴム（全体）` です。
- click または drag / なぞりで hit した Fabric object 全体を削除します。
- stroke、line、arrow、rect、ellipse、text が対象です。1 gesture 中に同じ object を二重削除せず、hit があった場合だけ一つの history entry を作ります。
- 自由線の部分消去は未実装です。`doc/designs/CANVAS_PARTIAL_ERASER_DESIGN.md` の段階導入案に従う別 task とし、現在の whole erase の意味を変更しません。

### 4.5 draw.io 風 toolbar

toolbar の目的は draw.io の全機能を再現することではなく、道具の役割と現在状態を分かりやすくすることです。現在の group は次のとおりです。

`操作（選択） / 描く（ペン） / 線（直線・矢印） / 図形（四角・円） / 文字（テキスト） / 消去（消しゴム（全体）） / 履歴（Undo・Redo） / 用紙（幅・高さ・適用）`

- tool button は `data-active` と `aria-pressed` で active を示します。
- `role="toolbar"`、各 group の `role="group"`、日本語 `aria-label`、短い visible label、`title`、`aria-describedby` を使います。
- Undo / Redo は native `disabled` とし、tool の active state とは別扱いです。
- desktop では描画 group を見せ、狭幅では描画 rail だけを toolbar 内で local horizontal scroll できるようにします。主要操作、消しゴム、履歴は rail の奥へ隠しません。
- 用紙入力は独立 group です。Canvas の表示倍率や描画 tool と混同させません。
- snap、grid、guides、connector routing、waypoint、複数選択、group / ungroup、layer panel、rotate、minimap、infinite canvas、palette、rich text、画像、export toolbar は対象外です。

## 5. 完了した主な Worker task と変更された主要領域

完了 summary の path は、raw log の代わりに使う再開用の参照です。各 summary の `.next` 列挙は根拠にせず、次の task 名と現在の source / design doc を照合してください。

### 5.1 紙面・overview・Markdown の前段

- `new-note-paper-lines-header-fix` — `summary/20260717/2327-new-note-paper-lines-header-fix.md`。新規画面の余計な線、header 配置、Cue / Preview の線重複を整理。
- `remove-overview-field-end-to-end-6f3c1a2e-bdee76bb` — `summary/20260718/1015-remove-overview-field-end-to-end-6f3c1a2e-bdee76bb-summary.md`。overview の form / API / DB / mapper / migration を end-to-end で削除。
- `sync-docs-after-removing-overview-8d2b7f41-28e2212e` — `summary/20260718/1038-sync-docs-after-removing-overview-8d2b7f41-28e2212e-summary.md`。current docs の記載を同期。
- `sync-current-er-diagram-after-overview-removal-2f4a6c81-998f382a` — `summary/20260718/1044-sync-current-er-diagram-after-overview-removal-2f4a6c81-998f382a-summary.md`。current ER source / SVG を同期。
- `enable-markdown-soft-line-breaks-9e72c4b1-fddd5a5b` — `summary/20260718/1054-enable-markdown-soft-line-breaks-9e72c4b1-fddd5a5b-summary.md`。単一改行の Preview 反映を導入。
- `resolve-markdown-soft-break-dependency-build-4c6e91b2-3b4d6e65` — `summary/20260718/1103-resolve-markdown-soft-break-dependency-build-4c6e91b2-3b4d6e65-summary.md`。依存関係／build の問題を解消し、最終実装を安定化。

### 5.2 Canvas の方向、永続化、UI 統合

- `design-freeform-note-canvas-direction-5b8d2e71-b3fcf7c6` — `summary/20260718/1118-design-freeform-note-canvas-direction-5b8d2e71-b3fcf7c6-summary.md`。Cue / Summary を残し、本文だけを bounded freeform Canvas にする方式を確定。
- `canvas-editor-technology-spike-9344817c` — `summary/20260718/1238-canvas-editor-technology-spike-9344817c-summary.md`。Fabric / Konva、SSR、adapter、保存形式、focus / scroll の比較入口を作成。
- `resolve-canvas-spike-dependencies-and-runtime-qa-b38c8cc9` — `summary/20260718/1248-resolve-canvas-spike-dependencies-and-runtime-qa-b38c8cc9-summary.md`。spike の依存関係と静的／runtime 前提を整理。
- `add-canvas-persistence-api-contract-96a6002c` — `summary/20260718/1312-add-canvas-persistence-api-contract-96a6002c-summary.md`。`bodyMode`、`NotebookCanvas`、Canvas validation、JSON / searchText の API 契約を追加。
- `integrate-hybrid-canvas-note-ui-fa9249f5` — `summary/20260718/1334-integrate-hybrid-canvas-note-ui-fa9249f5-summary.md`。note editor、detail view / edit / review に Canvas editor / viewer を統合。
- `make-body-full-freeform-canvas-1f35709e` — `summary/20260718/1900-make-body-full-freeform-canvas-1f35709e-summary.md`。本文をカード／Markdown 欄へ分割せず、自由線・図形・線・矢印・文字を扱う Canvas に整理。

### 5.3 Canvas の操作、寸法、scroll、eraser、toolbar

- `prevent-canvas-focus-scroll-jump-3623b5ef` — `summary/20260718/1910-prevent-canvas-focus-scroll-jump-3623b5ef-summary.md`。Canvas focus 時の scroll jump を `preventScroll` と focus 境界で抑制。
- `support-variable-canvas-paper-dimensions-d6fbe36a` — `summary/20260718/1939-support-variable-canvas-paper-dimensions-d6fbe36a-summary.md`。可変 page 寸法と validation の実装入口を追加。
- `document-canvas-paper-size-spec-5bd99c66` — `summary/20260718/2004-document-canvas-paper-size-spec-5bd99c66-summary.md`。1200x800 default、320〜4000 px、要素 geometry 不変、zoom 分離を文書化。
- `add-canvas-paper-dimensions-inputs-134fa1b6` — `summary/20260718/2020-add-canvas-paper-dimensions-inputs-134fa1b6-summary.md`。toolbar に幅・高さ・適用・field error を追加。
- `fix-nested-canvas-size-form-hydration-21854123` — `summary/20260718/2027-fix-nested-canvas-size-form-hydration-21854123-summary.md`。親 form 内の寸法 input の hydration / apply state を整理。
- `render-canvas-paper-at-actual-size-39c61f6c` — `summary/20260718/2147-render-canvas-paper-at-actual-size-39c61f6c-summary.md`。保存済み page 寸法で editor / viewer を実寸描画。
- `change-canvas-horizontal-overflow-to-clip-7572b44c` — `summary/20260718/2243-change-canvas-horizontal-overflow-to-clip-7572b44c-summary.md`。横 scroll wrapper の y 軸を clip にしてページ縦 scroll と分離。
- `replace-delete-with-eraser-tool-1b7231e4` — `summary/20260718/2258-replace-delete-with-eraser-tool-1b7231e4-summary.md`。削除語を whole-object eraser の操作契約へ変更。
- `design-partial-canvas-eraser-21708862` — `summary/20260718/2304-design-partial-canvas-eraser-21708862-summary.md`。自由線のみを対象にした将来の部分消去方式を設計。実装なし。
- `fix-canvas-line-arrow-placement-movement-563d27c2` — `summary/20260718/2329-fix-canvas-line-arrow-placement-movement-563d27c2-summary.md`。直線・矢印の page / Fabric 座標と移動後の保存を修正。
- `design-drawio-like-canvas-toolbar-95dfafe0` — `summary/20260718/2322-design-drawio-like-canvas-toolbar-95dfafe0-summary.md`。toolbar の group、役割、ARIA、responsive、対象外機能を設計。
- `implement-drawio-like-canvas-toolbar-b338cd7d` — `summary/20260718/2342-implement-drawio-like-canvas-toolbar-b338cd7d-summary.md`。toolbar の group、active / tooltip / accessible description、local rail、用紙入力を実装。

## 6. 現在の Canvas 実装契約

### 6.1 `CanvasDocumentV1`

共有契約は `src/shared/canvas/canvas-document.ts` にあります。

```ts
{
  schemaVersion: 1,
  page: { width: number, height: number, background: "paper" },
  elements: CanvasElementV1[]
}
```

`CanvasElementV1` は `id`, `type`, `x`, `y`, `width`, `height`, `rotation`, `style`, `z` を持ち、`stroke` / `line` / `arrow` は `points`、`text` は `text` を持ちます。現在の type は `stroke`, `line`, `arrow`, `rect`, `ellipse`, `text` です。

validation の現在の上限は、要素 1,000 件、全 stroke point 20,000 点、serialized document 2 MiB です。page 寸法は finite integer で、各 320〜4000 px。未知の schema version、背景、type、有限値でない座標、正でない要素サイズ、上限超過は保存前に拒否します。page 外の要素や points は禁止していません。

`extractCanvasSearchText()` は text element の本文だけを z 順に連結します。手書き線、線、矢印、図形は一覧のフリーワード検索対象ではありません。

### 6.2 保存・復元

- `prisma/schema.prisma` の `Notebook.bodyMode` は `markdown` または `canvas` です。
- `NotebookCanvas` は `notebookId` を PK / FK とする 1:1 table で、`schemaVersion`, `documentJson`, `searchText`, timestamps を持ちます。
- `bodyMode="canvas"` のとき `Notebook.body` は空文字にし、本文の正本は `NotebookCanvas.documentJson` です。
- `bodyMode="markdown"` のときは既存 `Notebook.body` を使い、Canvas は指定しません。
- `POST /api/notes` と `PATCH /api/notes/:id` は `bodyMode` と Canvas document を受け取り、repository が validation、serialize、searchText 生成、transaction 保存を行います。
- `GET /api/notes/:id` は保存済み Canvas document を復元し、`NoteCanvasViewer` / `NoteCanvasEditor` へ渡します。用紙サイズだけを理由に自動変換しません。
- Notebook の物理削除では relation cascade により Canvas も削除されます。
- Canvas JSON が欠落／壊れている／stored schema version と一致しない場合、空白へ置換して保存済みデータを上書きせず、エラーを表示します。

### 6.3 Fabric renderer と app-owned JSON の分離

`src/app/spikes/canvas/_lib/fabric-adapter.ts` が `CanvasDocumentV1` と Fabric object の変換境界です。

- `fabricDocumentToCanvas()` は document の z 順要素を Fabric へ展開します。
- `fabricCanvasToDocument()` は Fabric object の metadata、移動後の位置、scale、rotation、text を page 座標へ戻します。
- Fabric の stage / object JSON、selection、camera、pointer state、history は保存形式に入れません。
- Fabric は `note-canvas-editor.tsx` / `note-canvas-viewer.tsx` から lazy import され、viewer は read-only object として描画します。
- Canvas の表示寸法は `document.page` と inline / Fabric `setDimensions()` に同期し、用紙サイズ変更と表示倍率を同じ値として扱いません。

### 6.4 Editor の操作と履歴

`note-canvas-editor.tsx` の現行 tool union は `select`, `pen`, `line`, `arrow`, `rect`, `ellipse`, `text`, `erase` です。

- `select`: Fabric の既存 object を選択し、移動・resize した `object:modified` を document history に commit。
- `pen`: Fabric PencilBrush の自由線を `stroke.points` へ取り込みます。
- `line` / `arrow` / `rect` / `ellipse`: 空白から pointer drag して一要素を作ります。drag 中は preview object を置き、pointer up で確定します。
- `text`: 空白をクリックして editable Textbox を作成し、空文字は commit 前に除去します。
- `erase`: `EraseSession` で gesture 中の target を Set 管理し、対象 object 全体を remove。hit があった場合だけ一つの snapshot を commit。
- `Cmd/Ctrl+Z` と `Cmd/Ctrl+Shift+Z` は Canvas viewport に focus があるときの local undo / redo です。Delete / Backspace は active object の削除に使われますが、UI 上の「消しゴム（全体）」の意味とは分けて扱います。
- 新しい commit では redo future を破棄します。history は最大 50 snapshot、DB には保存しません。

直線・矢印は page coordinate と Fabric local / group coordinate がずれると左上から描画されるため、adapter で local points、bounds、group の left / top、移動後の transform を整合させています。移動時に余計な線が生成される経路も、editor の既存 target guard と drag draft の扱いを修正して抑えています。editor は既存 target がある line / arrow / shape の pointer down で新規 drag を開始しません。

### 6.5 Canvas の scroll 契約

現在の DOM / CSS は次の責務分離です。

```text
note-canvas-viewport       overflow: visible（ページ縦 scroll を許可）
└─ note-canvas-horizontal-scroll
     overflow-x: auto
     overflow-y: clip      （用紙が広い時だけ横 scroll）
   └─ note-canvas-stage     実寸の用紙
      └─ Fabric lower / upper canvas
```

`overflow-y: hidden`、Canvas 上の manual wheel forwarding、親 page の `overflow-y: hidden`、fixed overlay toolbar は再導入しません。横 scroll は Canvas 用紙 wrapper と toolbar の drawing rail に閉じ込め、ページ全体の横 overflow を出さないことが契約です。

### 6.6 Toolbar の契約

toolbar は `note-canvas-toolbar.tsx` と `globals.css` にあります。幅・高さ input は `type="number"`、整数、min / max を持ち、`適用`または Enter で page dimensions を更新します。入力値が不正なら field 単位の `aria-invalid` と alert を表示し、document は変更しません。

current tool の status は `role="status" aria-live="polite"` で提供します。設計 doc にある Escape による未完了 gesture 破棄、no-op 理由や whole erase 件数の詳細 live status は、現在のコードでは十分に確認できていないため、runtime QA または別の accessibility task で扱います。

## 7. 未確認事項、既知の制約、残タスク

### 7.1 最優先の未確認事項

- ブラウザで `/notes/new` を開き、Canvas が Fabric 初期化後に正しい page 寸法で見えること。
- 空白からの pen / line / arrow / rect / ellipse / text 作成。
- 既存 object 上で描画を始めた場合に余計な線・図形が作られないこと。
- `選択`で line / arrow / shape / text を移動・resize し、page 座標と保存 JSON が一致すること。
- whole erase の click / drag、同一 gesture の一履歴、Undo / Redo。
- 用紙を 1920 x 1080 に変更したとき、既存 element の geometry が変わらず、保存・再読込後も page 寸法と要素が復元されること。
- Canvas 上で wheel / trackpad / touch のページ縦 scroll が Summary / footer まで通り、広い用紙だけが nested horizontal scroll できること。
- 375 / 768 / 1280 / 1440 px で toolbar group、active state、focus ring、tooltip / accessible description、drawing rail、用紙入力に到達できること。

### 7.2 現行 MVP の残差分

Canvas 以外にも、現行 MVP 契約との差分として次が残っています。

- 新規 `nextReviewDate = noteDate + 7日` の初期値が、現行 form では空欄のままです。これは Canvas task で変更していません。
- autosave、draft state、409 optimistic lock、soft delete、5 秒 Undo Snackbar、専用復習 task、NoteCard / D&D、PDF export、タグ管理 UI などは未実装で、Phase 2 以降です。
- `CURRENT_STATUS.md` / `IMPLEMENTATION_STATUS.md` の用紙サイズ欄は、最新の source と runtime 判定へ更新が必要です。ただし browser QA 前に PASS と書き換えません。

### 7.3 Canvas の設計のみ／将来 task

- `doc/designs/CANVAS_PARTIAL_ERASER_DESIGN.md` に基づく partial eraser は、まず自由線 `stroke` の points を分割する pure geometry task として着手します。line / arrow / shape / text の部分消去は同時に導入しません。
- toolbar の Escape、gesture cancel、no-op 理由、whole erase 結果の live status は設計の受け入れ項目として残っています。先に runtime で現状を測り、必要なら一目的の accessibility task に分けます。
- draw.io の snap、connector routing、複数選択、layer、minimap などは将来案にも含めず、現在の MVP toolbar の対象外です。

## 8. 次回の推奨作業手順と Worker task 分割案

### 8.1 再開時の最小手順

1. `HANDOFF_2026-07-19.md`、`AGENTS.md`、`doc/implementation/MVP_CONTRACT.md` を読む。
2. `git status --short` を確認し、上記の既存未コミット変更を戻さない。
3. Browser と local server が利用できる環境で、fixture または新規ノートを用意する。
4. `/notes/new` で Canvas を初期表示し、ペン、直線、矢印、図形、テキストを空白から作る。
5. `選択`へ切り替え、直線・矢印・図形・文字を移動／resize する。既存 object 上から描画を開始して余計な object が出ないことも確認する。
6. `消しゴム（全体）`で click / なぞり削除し、Undo / Redo を確認する。部分消去が行われないことも確認する。
7. 用紙サイズを 1920 x 1080 にして適用し、要素の geometry が不変であることを確認する。
8. 保存し、`/notes/[id]` へ遷移して閲覧、再読込、編集へ戻る。Canvas page、要素、text search、Summary、Cue が整合することを確認する。
9. Canvas 上でページ縦 scroll、広い用紙の横 scroll、toolbar の responsive / keyboard 到達性を確認する。
10. 実施日、viewport、fixture、操作、判定、未確認事項を `doc/testing/TEST_SCENARIOS.md` に記録する。未実施を PASS にしません。

### 8.2 問題が出た場合の Worker 分割

一つの問題につき一つの目的へ分けます。

- 座標問題: `fabric-adapter.ts` と line / arrow の純粋な座標変換・再現だけを対象にする。
- scroll 問題: `note-canvas-horizontal-scroll`、Fabric pointer / wheel、page scroll metrics だけを対象にする。manual wheel forwarding や親の hidden overflow を安易に追加しない。
- 用紙サイズ問題: page input、保存済み page、actual-size renderer、geometry 不変だけを対象にする。migration を増やさない。
- toolbar accessibility 問題: group、active、ARIA、focus、Escape / status だけを対象にする。Canvas JSON や adapter を同時に変更しない。
- partial eraser: pure geometry → editor gesture → persistence / QA の順に分割し、whole erase を regression 固定する。
- docs reconciliation: runtime QA 結果と契約を `CURRENT_STATUS.md` / `IMPLEMENTATION_STATUS.md` / test scenarios に同期する。コードの修正 task と混ぜない。
- `nextReviewDate + 7日`、autosave、Phase 2 機能は Canvas の残作業へ混ぜない。

## 9. Next Read

次回は以下だけを最小の再開起点とします。`.next` の生成物、raw log、cache、trace は含めません。

1. `HANDOFF_2026-07-19.md`
2. `AGENTS.md`
3. `doc/implementation/MVP_CONTRACT.md`
4. `doc/implementation/IMPLEMENTATION_STATUS.md`
5. `doc/designs/CANVAS_PARTIAL_ERASER_DESIGN.md`
6. `doc/designs/CANVAS_TOOLBAR_DESIGN.md`
7. `summary/20260718/2329-fix-canvas-line-arrow-placement-movement-563d27c2-summary.md`
8. `summary/20260718/2342-implement-drawio-like-canvas-toolbar-b338cd7d-summary.md`
9. `src/app/notes/_components/note-canvas-editor.tsx`
10. `src/app/notes/_components/note-canvas-toolbar.tsx`
11. `src/app/notes/_components/note-canvas-viewer.tsx`
12. `src/app/globals.css`
13. `src/app/spikes/canvas/_lib/fabric-adapter.ts`
14. `src/shared/canvas/canvas-document.ts`

保存経路の不具合を調べる場合だけ、次に `src/modules/notes/contracts/note.schema.ts`、`src/server/notes/infrastructure/command.repository.ts`、`src/server/notes/presenters/notes.mapper.ts`、`prisma/schema.prisma` を追加で読みます。最初から API 全体や `.next` を読み直す必要はありません。
