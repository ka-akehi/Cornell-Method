# Cornell Method Notebook 引き継ぎメモ

作成日: 2026-07-04

## 目的

このメモは、2026-07-04 の会話開始時点から進めた以下の作業を、次回作業へ引き継ぐためのものです。

- 画面遷移図と状態遷移図の可読性改善
- MVP 設計書が要件定義〜詳細設計として足りているかの確認
- MVP 詳細設計の弱点補強
- 現コードと MVP 設計の差分棚卸し
- Prisma / lint / build の確認

次回作業開始時は、このファイルを起点に、必要な summary と設計書だけを読んで再開してください。raw log は読まないでください。

## 次回最初に読むもの

| ファイル | 役割 |
| --- | --- |
| `AGENTS.md` | 仕様の正本、Manager / Worker 方針、最新引き継ぎ参照 |
| `HANDOFF_2026-07-04.md` | 今回の作業引き継ぎ |
| `doc/review/MVP_DETAIL_GAP_INVENTORY.md` | MVP 設計と現コードの Gap / Unknown / 次タスク候補 |
| `doc/screens/MVP_SCREEN_DESIGN.md` | MVP 画面設計、UI 状態・条件の補強済み正本 |
| `doc/screens/MVP_SCREEN_INVENTORY.md` | MVP 画面一覧、各画面の UI 状態表 |
| `doc/api/MVP_API_DESIGN.md` | MVP API 設計、エラー形式・validation 補強済み |
| `doc/data/MVP_DATA_DESIGN.md` | MVP DB 設計、Prisma model / migration / seed 方針 |
| `doc/technical/MVP_TECHNICAL_DESIGN.md` | 技術設計、Prisma / backup 運用補強済み |
| `doc/testing/TEST_SCENARIOS.md` | MVP の UI/API/DB/運用テスト観点 |
| `summary/20260704/2307-inventory-mvp-design-code-gaps-f026fbbc-summary.md` | Gap 棚卸し Worker task |
| `summary/20260704/2308-strengthen-mvp-api-error-detail-design-6a39e9e1-summary.md` | API 詳細設計補強 Worker task |
| `summary/20260704/2311-strengthen-mvp-db-ops-detail-design-10c05d7f-summary.md` | DB/Prisma/backup 詳細設計補強 Worker task |
| `summary/20260704/2313-strengthen-mvp-ui-detail-design-77c7e7e7-summary.md` | UI 詳細設計補強 Worker task |
| `summary/20260704/2343-verify-mvp-build-prisma-lint-292af900-summary.md` | Prisma / lint / build 確認 Worker task。ただしコマンド結果は下記の Manager 再確認結果を正とする |

## 現在の結論

MVP に限れば、要件定義〜基本設計〜詳細設計を進めるための設計書は一通り揃いました。

現在のフェーズは、設計書を増やす段階ではなく、以下へ進む段階です。

1. 現コードの主要フロー確認
2. Gap / Unknown のうち MVP で直すものの確定
3. 必要な実装修正 task の投入
4. 受け入れ条件に沿った動作確認

今回の検証では、Prisma schema、Prisma Client 生成、lint、production build はすべて成功しています。したがって、現時点で「実装継続不能なビルド破損」は確認されていません。

## 今回完了した Worker task

| task | queue | 状態 | 主な成果 |
| --- | --- | --- | --- |
| `simplify-screen-transition-diagrams-df02266e.task.md` | Common | done | 画面遷移図を分割・整理 |
| `improve-state-diagram-readability-c219e1fb.task.md` | Common | done | 状態遷移図を flowchart 化して可読性改善 |
| `inventory-mvp-design-code-gaps-f026fbbc.task.md` | Common | done | `doc/review/MVP_DETAIL_GAP_INVENTORY.md` 作成 |
| `strengthen-mvp-api-error-detail-design-6a39e9e1.task.md` | Common | done | MVP API の error / validation / response 詳細を補強 |
| `strengthen-mvp-db-ops-detail-design-10c05d7f.task.md` | Common | done | MVP DB / Prisma / backup 運用詳細を補強 |
| `strengthen-mvp-ui-detail-design-77c7e7e7.task.md` | Common | done | MVP UI の loading / error / empty / disabled / success 状態を補強 |
| `verify-mvp-build-prisma-lint-292af900.task.md` | Common | done | Prisma / lint / build 確認 task |

## Manager が直接補正した内容

Worker task 完了後に、Manager が成果物を確認し、必要最小限の直接補正を行いました。

### 画面遷移図

ユーザー指摘:

- `doc/screens/assets/svg/mvp-screen-inventory-01-diagram.svg` が確認できない。

原因:

- Mermaid label に `SCR-COMMON Navigation<br/>...` が入り、SVG/XML として tag mismatch になっていた。

対応:

- `doc/screens/MVP_SCREEN_INVENTORY.md`
- `doc/screens/assets/mmd/mvp-screen-inventory-01-diagram.mmd`

上記の `<br/>` を使う label を、1 行の安全な label に変更。

検証:

```bash
npm run diagrams:svg
```

結果:

- 成功。`Rendered 23 SVG diagrams.`
- Playwright で対象 SVG が表示可能なことを確認。

### 状態遷移図

ユーザー指摘:

- 状態遷移図の可読性がよくない。

Worker task 後も以下が残っていたため、Manager が補正しました。

- normal diagram が左寄りで読みづらい。
- error diagram の label が重なりやすい。
- review diagram が横に広すぎる。

対応ファイル:

- `doc/diagrams/MVP_STATE_DIAGRAMS.md`
- `doc/diagrams/assets/mmd/mvp-state-diagrams-01-normal-mode-transitions.mmd`
- `doc/diagrams/assets/mmd/mvp-state-diagrams-03-error-recovery-transitions.mmd`
- `doc/diagrams/assets/mmd/mvp-state-diagrams-04-diagram.mmd`

主な変更:

- normal mode は `flowchart LR` で閲覧へ戻る流れに整理。
- error recovery は error node を個別化して label overlap を軽減。
- review state は `flowchart TD` にして縦方向の lifecycle に変更。

検証:

```bash
npm run diagrams:svg
rg '<br|This page contains the following errors|tag mismatch' doc/screens/assets/svg doc/diagrams/assets/svg
```

結果:

- SVG 生成成功。
- XML error / tag mismatch を示す文字列は検出なし。

注意:

- `npm run diagrams:svg` は 23 SVG 全体を再生成します。
- その副作用で `doc/diagrams/assets/svg/mvp-er-diagram-01-notebook-cue-tag-notebooktag.svg` も差分に出ています。

## MVP 設計書の状態

今回の判断:

- MVP の設計フェーズは完了扱いにしてよい。
- ただし最終仕様全体ではなく、現コード寄りの MVP 範囲に限定する。
- `AGENTS.md` の最終仕様は Draft / Undo / Review task / PDF export などを含むが、現コードは MVP 寄り。

今回補強された弱点:

- UI の loading / error / empty / disabled / success 状態。
- API の共通 error format と endpoint ごとの validation / not_found / server_error。
- DB / Prisma / migration / seed 不要 / backup 運用。
- 現コードと MVP 設計の Gap / Unknown 棚卸し。

## MVP Gap 棚卸しの要点

`doc/review/MVP_DETAIL_GAP_INVENTORY.md` が今回の Gap 管理の起点です。

特に次に見るべきもの:

- `UI-009`: NoteEditor に既存タグ候補 select / autocomplete がない可能性。
- `UI-014`: 編集保存後に閲覧状態・最新状態へ戻る挙動の確認が必要。
- `TEST-001`: Prisma / lint / build は今回成功済み。必要なら棚卸し表を更新する。
- `TEST-002`: happy path の E2E / 手動検証記録が未実施。
- `DOC-003`: README screenshots / demo が未対応。

次タスク候補として特に優先度が高いもの:

1. MVP 主要フローの手動確認または E2E 確認。
2. API CRUD / validation / backup の動作確認。
3. 既存タグ候補選択 UI の有無確認と、必要なら実装修正。
4. 編集保存後の状態更新確認と、必要なら実装修正。

## Prisma / lint / build 確認結果

Worker task:

- `verify-mvp-build-prisma-lint-292af900.task.md`

注意:

- 自動 summary は変更ファイル一覧が大きく、肝心の command result が薄かった。
- そのため Manager 側で同じ確認コマンドを再実行し、以下を確認済み。

実行結果:

```bash
npx prisma validate
```

- 成功。
- `prisma/schema.prisma` は valid。

```bash
npm run prisma:generate
```

- 成功。
- Prisma Client v7.8.0 を生成。

```bash
npm run lint
```

- 成功。

```bash
npm run build
```

- 成功。
- Next.js 16.2.9 / webpack build。
- TypeScript 成功。
- Static pages 10/10 generated。
- route 一覧に `/`, `/notes`, `/notes/new`, `/notes/[id]`, `/backup`, API routes が出力された。

結論:

- 現時点で Prisma / lint / build レベルの破損はない。
- 次は実際の MVP 動作確認に進める。

## 現在の git status の性質

2026-07-04 末時点で、多数の未コミット差分があります。

主な種類:

- MVP 設計補強 docs。
- screen / state diagram の Mermaid source と SVG。
- Gap inventory 新規作成。
- Worker task summary 新規作成。

代表的な変更ファイル:

- `doc/api/MVP_API_DESIGN.md`
- `doc/data/MVP_DATA_DESIGN.md`
- `doc/technical/MVP_TECHNICAL_DESIGN.md`
- `doc/screens/MVP_SCREEN_DESIGN.md`
- `doc/screens/MVP_SCREEN_INVENTORY.md`
- `doc/diagrams/MVP_STATE_DIAGRAMS.md`
- `doc/testing/TEST_SCENARIOS.md`
- `doc/implementation/MVP_IMPLEMENTATION_TASKS.md`
- `doc/review/MVP_DETAIL_GAP_INVENTORY.md`
- `summary/20260704/*.md`

注意:

- 検証コマンドにより `.next/` や Prisma Client 生成物は更新されたが、追跡対象のコード差分としては増えていない。
- `.next/` や `node_modules/` は作業生成物であり、通常は commit 対象ではない。

## 次に推奨する作業

次回は実装追加ではなく、まず動作確認 task を切るのが妥当です。

### 推奨 1: MVP 主要フロー確認

目的:

- UI 上で `一覧 -> 新規作成 -> 詳細 -> 編集 -> 保存 -> 削除 -> バックアップ` が通るか確認する。

キュー:

- UI または Common。
- ブラウザ操作を含むなら UI queue が自然。

制約:

- まず確認 task として実施。
- コード変更はしない。
- 問題が出たら、修正 task を別途切る。

### 推奨 2: API CRUD / validation / backup 確認

目的:

- MVP API の success / validation error / not_found / backup generation を確認する。

キュー:

- API。

制約:

- まず確認 task として実施。
- DB を触るため、事前に test DB / dev DB の扱いを明示する。

### 推奨 3: Gap inventory の TEST-001 更新

今回 Prisma / lint / build は成功したため、`doc/review/MVP_DETAIL_GAP_INVENTORY.md` の `TEST-001` は「確認済み」に寄せられます。

これは小さい docs task として切ってよいです。

### 推奨 4: 実装修正 task

動作確認で問題が確定してから切るべきです。

候補:

- 既存タグ候補選択 UI。
- 編集保存後の状態更新。
- README screenshots / demo。

## Worker / Manager 運用上の注意

- Manager は `codex-queue/*/running`, `done`, `failed` を直接編集しない。
- Worker task は 1 task 1 目的にする。
- 仕様/調査 task と実装 task を同じ task file に混ぜない。
- 確認 task では「コード・設定・依存関係・生成物を変更しない」を明記する。
- コーディング task のみ `CODEX_TASK_KIND: coding` を付ける。
- Worker が `done` でも、Manager は summary と成果物を確認する。
- summary が不十分な場合は、raw log ではなく必要最小限の再確認を行う。

## 過去 HANDOFF から引き継ぐ重要事項

2026-07-03 版 HANDOFF のうち、今後も重要な点:

- `doc/implementation/IMPLEMENTATION_STATUS.md` は現コードより進んだ内容を実装済みとして書いている可能性があるため、実装済み判断の根拠にしない。
- 実装済み確認は `src/app/**`, `src/lib/**`, `prisma/schema.prisma`, `scripts/**` の現コードで裏取りする。
- AI クイズ生成と AI Cue 候補提案は、MVP / 既存 Phase 2 には混ぜず、将来構想として扱う。
- SVG 再生成には依存関係と Playwright Chromium が必要。

## 次回開始時の推奨一言方針

「MVP 設計書は完了扱い。Prisma / lint / build は成功済み。次は主要フローと API の動作確認 task を切り、Gap inventory の Unknown を実装 task に落とす。」
