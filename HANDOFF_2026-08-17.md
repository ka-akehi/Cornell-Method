# Cornell Method Notebook 引き継ぎ資料

作成日: 2026-08-17（JST）
最終更新: 2026-08-17（JST）

## 現在の状態

- MVP Gate 0 は完了済み。
- Desktop Alpha の shell は Tauri + Node.js sidecar に決定済み。
- Application Support の保存先と製品識別子は承認済み。
- SQLite の user data bootstrap と初回 migration 失敗後の再試行処理は実装済み。
- 製品用 `src-tauri/` はまだ作成していない。
- 次の実装対象は Tauri shell の単一ウィンドウと sidecar lifecycle である。

現行 Web の route、API、Canvas、明示保存、物理削除、復習、手動 backup の契約は Desktop shell の都合で変更しない。
開発用の Next.js Web 起動経路も維持する。

## 承認済みの基盤

| 項目 | 決定値 |
| --- | --- |
| shell | Tauri + Node.js sidecar |
| 製品識別子 | `com.cornellmethod.notebook` |
| 製品実装ディレクトリ | `src-tauri/` |
| SQLite の正本 | Application Support 内の `live/notebook.sqlite` |

保存先は次の構成とする。

```text
~/Library/Application Support/com.cornellmethod.notebook/
├── live/notebook.sqlite
├── backups/
├── settings/
├── logs/
└── pending-restore/
```

`tools/desktop-poc/tauri/` は比較検証用であり、製品実装へ fixture、evidence、生成物、PoC 固定 port を移さない。

詳細は [DESKTOP_ALPHA_TAURI_FOUNDATION.md](doc/technical/DESKTOP_ALPHA_TAURI_FOUNDATION.md) を参照する。

## 完了済みの作業

### Desktop PoC

Tauri + Node.js sidecar を Desktop Alpha の基盤として承認した。
PoC の renderer UI automation が利用できないため smoke と最終 evidence に BLOCKED が残るが、これは既知の測定境界であり、shell 選定を覆す理由にはしない。

PoC の経緯や比較結果は [retry24 完了サマリー](summary/20260817/1232-finalize-desktop-poc-comparison-retry24.md) に残す。

### User data / SQLite bootstrap

- Application Support の root と相対 path を解決する処理を追加した。
- 初回利用時に必要な directory を作成し、現行 Prisma migration から SQLite を初期化する。
- 既存 DB は read-only で integrity、foreign key、schema、migration 状態を確認する。
- 正常、migration 不足、migration 未完了、破損または判定不能を区別する。
- sidecar へ渡す `DATABASE_URL` は絶対 `file:` URL とする。
- 初回 migration が失敗した場合は、同じ試行で claim した空ファイルだけを安全に削除し、次回起動で再試行できる。
- 非空の既存 DB や migration が書き込み済みの DB は削除・上書きしない。

実装は [desktop-storage.js](src/server/infrastructure/desktop-storage.js)、テストは [desktop-storage.test.js](test/desktop/desktop-storage.test.js) にある。

検証結果は次のとおり。

- Desktop storage test: 9/9 PASS
- `node --check`: PASS
- `npm run lint`: PASS。既存 PoC warning 10 件、error なし
- `git diff --check`: PASS

完了 summary は [1742-repair-desktop-user-data-sqlite-bootstrap-failure-recovery-summary.md](summary/20260817/1742-repair-desktop-user-data-sqlite-bootstrap-failure-recovery-summary.md) を参照する。

## 実装するタスクの順序

### 1. Tauri shell と single-window lifecycle

次に実施する task である。

- `src-tauri/` の製品側 Tauri config、Cargo、package を作成する。
- 起動時に user data / SQLite bootstrap を呼び出す。
- sidecar を起動し、ready 後に `/notes` を開く。
- single application instance と primary window 1つを維持する。
- 二重起動時は新しい window を作らず、既存 window を前面に出す。
- 最後の primary window を閉じたとき、sidecar と app-owned child process を停止する。
- window size / position を保存・復元する。
- dirty 状態の終了では「保存して終了」「保存せず終了」「戻る」を扱う。
- 保存失敗時は終了せず、編集内容と dirty 状態を保持する。

### 2. Settings

General、Updates、Data and Backup の入口を作る。
正確な文言と項目配置は実装 task で決める。
`/backup` は Settings の代替機能が受け入れられるまで維持する。

### 3. 更新と migration safety

更新確認、package の取得、署名・完全性確認、staging migration、失敗時の現行版維持を実装する。
更新 provider、最低対応 macOS、配布先は別途決める。

### 4. Manual backup / restore

手動 SQLite export、app 管理 backup、外部 backup file の restore を分ける。
restore 前の safety backup、integrity、foreign key、schema、Canvas、reopen の検証を実装する。

### 5. 完全なデータ削除

明示確認後に live DB、app 管理 backup、設定を削除し、初回利用時の状態へ戻す。
外部 SQLite export は削除しない。

### 6. 起動失敗、診断、privacy

DB を開けない場合の recovery UI、local log、診断 bundle、異常終了後の通知を実装する。
ノート本文、query、DB、token、user path を診断情報へ含めない。

### 7. Packaging と Desktop Alpha QA

Tauri app bundle、DMG、sidecar、native dependency の同梱を確認する。
その後、Apple Silicon Mac の packaged app で起動、保存、再起動、二重起動、終了、復旧を受け入れ確認する。

## Desktop Alpha に含めないもの

Canvas PNG、検索サジェスト、大規模一覧、autosave、Undo / soft delete、専用復習 task、NoteCard / D&D、定期 backup、PDF export は Desktop Alpha 完了後の別 task とする。

## 未決事項

- 更新 provider、manifest / package の配置先、最低対応 macOS
- sidecar と native dependency の配布方式
- 署名、Developer ID、notarization
- Settings の正確な文言と完全削除の確認文言
- 対象 Mac の preflight と packaged app の実機確認

未決事項を理由に、承認済みの Tauri shell、製品識別子、`src-tauri/`、Application Support layout を変更しない。

## 作業上の注意

- 作業前後に `git status --short` を確認し、既存の未コミット変更を戻さない。
- Worker は 1 task だけを実行する。
- 既存の Web 用 `DATABASE_URL`、PostgreSQL 分岐、`file:./dev.db` fallback を変更しない。
- 初回状態以外の DB に対して、自動 migration、削除、上書き、destructive repair を行わない。
- 外部サービスへの送信、Git commit、PR 作成は個別に承認を得る。

## 再開時に読む資料

1. [HANDOFF_2026-08-17.md](HANDOFF_2026-08-17.md)
2. [Tauri Desktop Alpha 基盤境界](doc/technical/DESKTOP_ALPHA_TAURI_FOUNDATION.md)
3. [Desktop Alpha 実装計画](doc/implementation/POST_MVP_IMPLEMENTATION_PLAN.md)
4. [MVP 契約](doc/implementation/MVP_CONTRACT.md)
5. [Target Architecture](doc/technical/TARGET_ARCHITECTURE.md)
6. `codex-queue/bin/worker-status.sh` の最新結果

## 次のアクション

1. `implement-desktop-single-window-lifecycle.task.md` を Common queue に投入する。
2. Worker 完了後、Tauri shell、sidecar 起動、single instance、終了 cleanup をレビューする。
3. PASS を確認してから Settings task を投入する。
