# Cornell Method Notebook 引き継ぎ資料

作成日: 2026-08-22（JST）
最終更新: 2026-08-27（JST）

## 現在の状態

- MVP Gate 0 は完了済み。現行 Web の route、API、明示保存、確認後の物理削除、詳細画面内の復習、CanvasDocumentV1、legacy Markdown、`/backup` の契約は変更しない。
- Desktop Alpha は全体として未完了である。製品側 `src-tauri/` は作成済みで、user data / SQLite bootstrap、single-instance recovery、既存 primary lifecycle、Settings shell / bridge / entrypoint の部分実装がある。更新系には provider、manifest、selection、download、signature / SHA-256、archive / bundle validation、update state、pending verification、`ApplyPreparation` を起点とする staged migration、rollback / recovery、candidate health、旧 bundle cleanup の実装がある。Issue #169〜#175 の restart 後再検証、partial cleanup / restore / switch retry safety、schema compatibility、durable rename まで実装・回帰検証済みである。これらはユーザーの明示操作による opt-in pipeline であり、自動 apply / 自動 restart は行わない。実際の macOS packaged app による health / switch / rollback は未検証である。
- 現在の作業位置は branch `develop`、現行 code HEAD は `ec0c4f0`。現在の差分は先行 docs sync と本追補の対象文書、および既存 summary である。今回の整合 task では指定 docs 以外の source、設定、依存関係、lockfile、テスト、生成物を変更しない。
- 2026-08-21 の UI / Rust 責務抽出と最終 audit は完了した。現時点で追加の責務分割 coding task は不要である。
- Settings は Mac menu と Web gear / mobile trigger の bridge、既存 primary WebView 内の 3カテゴリ modal、focus / keyboard 制御までである。General は読み取り専用、Updates は更新 backend の部分実装と未接続の準備中表示である。Data and Backup は native file-dialog / typed bridge、手動 plaintext SQLite export、managed backup catalog、managed / external restore 共通 pipeline、pending restore と明示 resume、complete data deletion、Settings UI まで実装済みで、static / disposable test 済みである。packaged runtime の実操作は未検証であり、現行 `/backup` は維持する。

## 完了済みの範囲

### Desktop foundation

- stable `settings/.instance.lock` を authority とする advisory lock、`settings/.instance.owner` の atomic replace、focus socket、bounded retry、stale socket recovery、active / unknown / permission endpoint の fail-safe を実装済み。
- secondary は focus 成功または `AlreadyRunningNotReady` で終了し、Tauri window / sidecar を作らない。primary の ready 後の window 作成、window state、close decision、sidecar / app-owned child process cleanup のコード境界がある。
- dynamic loopback の sidecar ready handshake と cleanup の実装はあるが、実プロセスでの確認は未完了である。

### Settings / responsibility audit

- Mac の Settings menu は新しい window や runtime を作らず、既存 primary WebView へ shared event を送る。
- Web gear / mobile trigger と Mac menu は同じ Settings bridge を使う。modal は General、Updates、Data and Backup の3カテゴリを持つ。
- `main.rs`、Rust lifecycle / runtime / instance module、AppChrome、close coordinator、editor dirty controller、Summary draft controller の責務抽出を完了し、最終 audit は「現時点で追加分割不要」と判定した。

### Update pipeline（backend 実装済み・packaged runtime 未検証）

- 現行コードには GitHub Releases provider、provider-neutral manifest の parsing / normalization / strict validation、compatible selection、公開 direct HTTPS URL 境界、package download、公開鍵署名・SHA-256 検証、archive / bundle validation、atomic な update state、pending update verification / staging revalidation がある。`apply_verified_update` は `Available` かつ `Verified` の candidate に対する引数なしの明示 command だけで起動し、candidate identity、signature・digest、canonical staging path、archive、bundle ID / version / architecture / arm64 Mach-O を再検証して `ApplyPreparation` を atomic に保存し、explicit restart handoff へ渡す。handoff の atomic 永続化が成功した後にだけ exit allowance と restart request を行い、永続化失敗時は restart と exit allowance を行わない。明示 handoff のない persisted `ApplyPreparation` は interruption として扱い、自動 apply / 自動 restart を行わない。
- persisted `ApplyPreparation` と明示 handoff を起点に、pending migration がある場合だけ migration 前 safety backup と DB staging copy を作成し、同じ candidate digest の既存 safety backup は内容と file identity を検証した後にだけ再利用する。複数件が一致する場合は選択・削除せず fail-closed とする。候補 DB の古い順の migration、schema / integrity / foreign key / reopen と、全 application table の read-back を確認してから atomic switch する。`sqlite_*` system table と `_prisma_migrations` だけを除外し、既存 table、column、row の消失・変更を switch 前に検出する。pending migration がない場合も `NO_PENDING` 前に candidate Prisma schema と live / staged SQLite schema の compatibility を検証し、不一致時は backup / switch / cleanup へ進まない。
- staged migration の runner、read-back、switch の failure は typed rollback checkpoint として永続化する。failure が発生した同じ startup で update recovery を実行し、recovery が成功した場合だけ bootstrap へ進む。recovery 自体の failure は fail-closed とし、migration を自動再実行しない。
- apply 直前には、署名済み archive と extracted candidate tree の全 entry を bytes、type、mode、size、追加・欠落、safe internal symlink target まで照合し、不一致を fail-closed にする。archive extraction と recovery は同じ safe internal relative symlink policy を使い、相対 target、bundle root 内、解決先の存在、cycle なし、`MAX_SYMLINK_HOPS` 内だけを許可する。absolute path、backslash、control byte、空 component、`.`、root 外 traversal、dangling link、cycle、hop 超過、special file は拒否する。
- `RestartHealthCheck` の `HealthPending` / `BundleSwitching` / `BundleSwitched` / `CleanupPending` / `RollbackPending` checkpoint は atomic に永続化される。candidate/current の path、bundle identity、version、architecture を検証し、candidate health には packaged app bundle root ではなく固定 runtime root `Contents/Resources/runtime` を渡す。candidate health 成功前は current app / live DB / safety backup を保持し、成功後だけ旧 bundle、staged artifact、不要な safety backup を cleanup する。switch / rollback の copy は safe internal symlink を target string のまま再作成し、cleanup は link 自体を unlink する。
- rollback、SQLite restore、旧 bundle への復帰、不要な staged artifact の cleanup、typed failure は fail-closed に扱う。DB / bundle rollback と restore が成功した場合も、terminal rollback state を記録する前に、対象 failed bundle marker を managed root / safe-tree 検証付きで削除する。marker cleanup に失敗した場合は `RollbackPending` と typed failure を保持する。rollback / restore 成功時は `Available + failure + pending candidate` へ atomic に遷移して `RollbackPending`、phase、recovery を解除し、rollback / restore 失敗時は `RollbackPending` と typed failure を保持する。
- PR #159 の Code Review Issue #164〜#175 は、候補 safety backup の再利用・曖昧性、rollback 後の failed bundle marker cleanup、restart handoff の永続化順序、staged migration failure の同一 startup recovery、全 application table の read-back 比較に加え、次の内容まで実装・static / disposable test・canonical docs に反映している。Issue の自動 close 用文言は PR 本文の責務とする。
- #169: restart 後、migration / candidate health 前に signed archive と staging tree 全体を再検証する。
- #170: missing leaf を安全に扱う idempotent partial cleanup retry と path / symlink safety を維持する。
- #171: rollback restore の exact temporary path tracking と stale temp cleanup を行う。
- #172: live DB restore を実切替 proof `Some(true)` の場合だけ許可する。
- #173: partial `switch_temp` を再利用せず、完全 tree 検証済み candidate source から retry する。
- #174: `NO_PENDING` 前に candidate Prisma schema と SQLite schema compatibility を検証する。
- #175: staged DB rename 後に live directory を sync し、post-rename failure を fail-closed にする。

## 未実装・未検証

- Settings の更新操作への backend 接続、Data and Backup の packaged runtime 実操作 QA。
- candidate health / bundle switch / rollback の packaged runtime、更新完了後の実機 cleanup、packaged artifact の受け入れ。
- packaged Apple Silicon GUI の起動、二重起動時の Dock / Finder 前面化、dirty close の実操作、save failure / discard、window state、sidecar process tree の完全終了。
- dynamic loopback の実 runtime、browser / DB read-back を伴う Summary explicit save、dirty close、Canvas / legacy Markdown 保存の E2E。
- startup failure recovery、local log、diagnostic bundle、privacy 境界。Data and Backup の static / disposable 実装は済んでいる。
- final `RunEvent::Exit` の sidecar cleanup は static lifecycle contract に実装済みだが、window close / Cmd-Q / dirty close の実 GUI 成功、process timing、orphan 非発生は未検証である。
- Canvas PNG、検索サジェスト、大規模一覧、autosave、Undo / soft delete、専用復習タスク、NoteCard / D&D、PDF export は Desktop Alpha の残課題または未採用候補として現行 MVP と分離する。

## 既知の検証制約

- 2026-08-24 の staged migration suite は 22/22 PASS、Desktop update Node suite は 77/77 PASS、desktop recovery suite は 14/14 PASS だった。Issue #169 focused test は 6/6 PASS で、各 Issue の回帰は全体 suite に含まれる。lifecycle/runtime tests は 15 PASS、7 SKIP（loopback / packaged runtime 依存）だった。
- 対象 ESLint、Desktop test / launcher / runtime helper の `node --check`、`cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`、`git diff --check` は PASS。対象 ESLint の PASS を repo-wide `npm run lint` の PASS と混同しない。
- 2026-08-25 の packaged runtime build retry は、`npm run desktop:prepare-node-runtime` と `cargo tauri build` が npm registry の `postcss` 取得時の DNS `ENOTFOUND` で停止し、production runtime、`.app`、DMG は生成されなかった。packaged QA は未検証であり、build 成功・artifact 生成・完了とは扱わない。検証証跡は `summary/20260825/0518-verify-desktop-alpha-packaged-runtime-build-20260825-summary.md`、`summary/20260825/0538-retry-desktop-alpha-packaged-build-after-toolchain-20260825-c19546c5-summary.md`、`summary/20260825/0551-investigate-desktop-runtime-install-repro-20260825-1257ae53-summary.md`、`summary/20260825/0601-retry-desktop-alpha-packaged-build-after-registry-recovery-20260825-69268d2a-summary.md` にある。
- Rust `cargo test --offline` は環境に `base64 0.22.1` crate がなく、compile 前に実行不能だった。full build、実 provider / package runtime、実 macOS packaged `.app` / DMG、Apple Silicon GUI による sidecar health、bundle switch、rollback / recovery、cleanup は未検証である。packaged DMG の配信可否も完了扱いにしない。
- static contract test、Rust format、`git diff --check`、既存 Web / API の証跡を packaged GUI や browser / DB runtime の PASS と扱わない。実 provider / package runtime、browser / DB read-back、packaged Apple Silicon GUI は未検証である。
- `npx tsc --noEmit` は既存の `src-tauri/target/debug/runtime`、`fabric`、`konva`、`@prisma/adapter-pg` の型解決問題で失敗しており、今回の hook / component 起因の新規エラーではない。

## 次の task 順

1. registry/DNS blocker などが解消して packaged build が成立した後、Apple Silicon packaged `.app` / DMG で candidate health、bundle switch、rollback / recovery、cleanup と lifecycle/runtime を結合 QA する。blocker 解消までは着手せず、packaged runtime でしか確認できない項目は未検証として記録する。
2. packaged Desktop Alpha contract QA（Data and Backup、lifecycle、sidecar health / restart / switch / rollback / cleanup、GUI・loopback・browser/DB read-back）を実施する。
3. 未実装の startup failure、local log、diagnostic bundle、privacy boundary を依存順に実装・検証する。
4. fresh packaged artifact で現行 MVP と Desktop Alpha の結合 QA を再実施し、最後に Alpha acceptance を判定する。

Data and Backup の実装 task は重複して投入しない。retention policy 等の未承認設計は確定しない。

追加の UI / Rust 責務分割は、独立した新機能や大きな state machine が追加された場合だけ再評価する。

## Next Read

1. `summary/20260825/0601-retry-desktop-alpha-packaged-build-after-registry-recovery-20260825-69268d2a-summary.md`（最新 retry の blocker と `.app` / DMG 未生成）
2. `summary/20260825/0551-investigate-desktop-runtime-install-repro-20260825-1257ae53-summary.md`（registry/DNS blocker の切り分け）
3. `doc/implementation/IMPLEMENTATION_STATUS.md` §5.4〜§5.4.1（Issue #164〜#175 と検証制約）
4. `doc/implementation/MVP_CONTRACT.md` §9.4（local-first / explicit apply / rollback 契約）
5. `doc/testing/TEST_SCENARIOS.md` §0.1〜§0.2 と §3（検証証跡・Issue #169〜#175 回帰条件）
6. Apple Silicon packaged `.app` / DMG runtime QA の未確認事項。Rust `cargo test --offline` は `base64 0.22.1` 不足が解消した環境で再試行する。
