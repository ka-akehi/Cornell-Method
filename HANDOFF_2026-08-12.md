# Cornell Method Notebook 引き継ぎ資料

作成日: 2026-08-12（JST）
最終更新: 2026-08-13（JST）

状態: MVP Gate 0 は完了済み。Electron と Tauri + Node.js sidecar の Desktop PoC は比較済みだが、native runtime、GUI、lifecycle、メモリ、`.app` / DMG の実測は未成立または BLOCKED であり、shell は未選定である。次の実施順は、対象 Mac 上で read-only preflight を実行できる運用経路の確認、対象 Mac の preflight、個別承認後の VM 準備と残る PoC 実測、発注者による shell 選定、その後の Desktop Alpha、最後に Desktop Alpha 後の採用済み Phase 2 である。現在の作業 Mac で実行された preflight は対象 Mac の結果ではない。

## Current Facts

- MVP Gate 0 は、発注者が実施し、テスト中に見つかった問題の修正と修正後の再確認を含めて完了と判断した人力結合テストを根拠に、2026-08-11 に完了した。
- Browser runtime、mobile、wheel / trackpad、実 DB read-back、E2E、外部 Postgres、追加の build 証跡は Gate 0 の blocker ではない。未確認の項目と過去の BLOCKED / NOT RUN は履歴または任意 QA として扱い、Gate 0 を未完了へ戻さない。
- 現行 MVP は、/notes、/notes/new、/notes/[id]、/backup を中心に、ノート CRUD、Cue、Summary、CanvasDocumentV1 本文、legacy Markdown 互換、検索、詳細画面内の復習、明示保存、確認後の物理削除、SQLite の手動 backup を提供する。
- 製品はローカル個人利用向けであり、SQLite をノートデータの唯一の正本とする。Desktop でも app bundle と Application Support 側の user data directory を分離する。開発、検証用の Next.js Web 起動経路は維持する。
- 現行 MVP の route、API、Canvas、明示保存、物理削除、復習、手動 backup の契約は Desktop shell の都合で変更しない。
- Desktop PoC は Electron と Tauri + Node.js sidecar の両候補を同条件で比較済みである。比較の baseline / fixture 条件はそろっているが、PoC 比較条件の記録を対象 Mac の host facts や preflight 結果として扱わない。
- Electron は preparation、clean migration / fixture read-back、production Next webpack build まで確認済みである。Electron package / runtime、GUI、lifecycle、メモリ、`.app` / DMG は未成立または BLOCKED である。
- Tauri + Node.js sidecar は shared baseline / fixture validation まで確認済みである。staging の npm / Cargo / Tauri CLI、native runtime、GUI、lifecycle、メモリ、`.app` / DMG は未成立または BLOCKED である。
- 両候補とも cold start、一覧・検索・詳細・編集・保存の操作反応、関連 process RSS 合計、native lifecycle、実在する `.app` / DMG の実測値は揃っていない。静的コードや unit test、runtime HTTP fallback は native GUI / lifecycle / artifact の PASS の代替にしない。
- shell は未選定であり、両候補の比較 summary に残る P0 blocker / unknown の確認と発注者の選定が終わるまで、Desktop Alpha の正式基盤、更新、migration、backup / restore の coding task を投入しない。
- 製品機能の実施順は、shell 選定後の現行 MVP の Desktop Alpha、その完成後の採用済み Phase 2 機能である。Desktop Alpha に Canvas PNG、検索サジェスト、大規模一覧、その他の Phase 2 機能を混ぜない。

## Desktop PoC comparison status

Desktop PoC の比較は完了している。詳細は [Desktop shell PoC 比較 summary](summary/20260812/1337-compare-desktop-shell-poc-evidence-20260812-summary.md) と [Electron / Tauri 比較 summary](summary/20260812/1342-compare-electron-tauri-poc-evidence-20260812-534d51a5-summary.md) を参照する。ここでの比較結果は shell 選定の入力であり、shell 選定済み、native runtime の受け入れ済み、または対象 Mac の preflight 済みを意味しない。

- 両候補は共通 baseline、deterministic な 10,000 note fixture、同じ測定契約で比較され、準備段階の証跡は整理済みである。
- Electron は準備・migration・fixture read-back・production build の範囲を確認できたが、依存解決、native runtime / GUI、lifecycle、メモリ、`.app` / DMG が未成立または BLOCKED である。
- Tauri + Node.js sidecar は shared baseline / fixture validation の範囲を確認できたが、npm / Cargo / native build、runtime / GUI、lifecycle、メモリ、`.app` / DMG が未成立または BLOCKED である。
- 両候補の stale lock / recovery、process cleanup、更新・署名・完全性検証、sidecar / native dependency の配布同梱も、静的設計と実測を分離したまま未確認である。
- したがって、比較 summary に残る追加 native P0 実測を完了し、同条件の結果を発注者が確認するまで、候補間の優劣を確定せず shell を選定しない。

## Approved but Unimplemented

### Desktop Alpha

Desktop Alpha の製品契約は承認済みだが、製品コード、packaging、Desktop UI は未実装である。現行 MVP を Apple Silicon Mac のアプリとして包み、次の契約を実装する。

| 領域 | 承認済み契約の要点 |
| --- | --- |
| Window lifecycle | single application instance / 1 primary window、二重起動時の既存 window 前面化、内部 process の許容、終了時の app-owned process cleanup、一覧からの起動、window size / position の復元、未保存終了時の「保存して終了」「保存せず終了」「戻る」を提供する。保存失敗時は終了せず、編集内容と dirty 状態を保持する |
| Settings | General、Updates、Data and Backup の 3 区分とする。Mac は Settings menu、開発用 Web は gear から開く。現行 /backup は代替機能の受け入れ確認まで維持する。app 管理 backup と外部 file の restore 入口を分ける |
| 更新 | 初期配布は DMG とする。起動後に最大 1 日 1 回の非同期確認と手動確認を提供し、確認 toggle は設けない。package を background download し、「再起動して更新」の明示操作でだけ適用する。確認、取得、署名・完全性検証に失敗した場合は現行版を維持する |
| 更新時の未保存内容 | 「保存して更新」「保存せず更新」「戻る」を通常終了と別の契約として提供する。保存失敗時は更新せず、編集内容と dirty 状態を保持する |
| Migration safety | pending migration がある更新だけ safety backup を作る。staging copy へ migration を古い順に適用し、schema、必須データ、Canvas、reopen の検証成功後だけ app と DB を切り替える。失敗時は現行 app と live DB を変更しない |
| Manual backup / restore | 手動 backup はユーザー選択先への平文 SQLite export とする。app 管理 backup と外部 file の restore を staging validation、明示確認、atomic switch、restart の同じ pipeline へ流す。restore 前 backup を作り、integrity、foreign key、schema / migration compatibility、必須データ、全 CanvasDocumentV1、reopen を検証する。migration 前と restore 前の safety backup は最新 3 世代を保持する |
| 完全なデータ削除 | 入力確認を伴う明示操作で live DB、app 管理 backup、設定を削除し、初回状態へ戻す。外部 SQLite export は削除せず、更新、reinstall、通常の uninstall から実行しない |
| 診断と privacy | 外部 telemetry と crash report の自動送信を行わない。local log は本文等を含めず、最大 14 日かつ合計 20 MB とする。診断 ZIP は allowlist で構成し、ノート内容、query、DB、token、user path、crash dump を含めない。ノート操作は offline とし、network は更新確認と取得だけに使う。Full Disk Access、app 独自 DB encryption、専用 password / Touch ID lock を必須にしない |
| Startup / failure UI | 通常起動は DB open と schema 状態を確認し、詳細 integrity check は異常終了後、migration 後、restore 後等に限定する。利用記録があるのに DB がない場合や DB が破損している場合は通常 UI より recovery UI を優先し、自動修復しない。save 失敗時は編集内容と dirty 状態を保持する。異常終了後に DB を開けた場合は一覧と一度だけの非 blocking 通知を表示する |

定期、日次、通常起動時、データ変更時の自動 backup と、異常終了用 draft / autosave は Desktop Alpha に含めない。

### Desktop Alpha 後の採用済み機能

次の3領域は採用済みだが未実装であり、Desktop Alpha の完成後に別 task で実装する。

#### Canvas PNG

- 保存済み CanvasDocumentV1 の page 全体を page.width × page.height と同じ寸法で PNG 化し、現在の paper 背景を含める。
- header、sidebar、toolbar、Settings 等の UI、Cue、Summary、legacy Markdown 本文を除外する。
- page 外の要素部分を page 境界で切り取り、画像を page 外へ広げない。
- 初期ファイル名は [タイトル]_[学習日].png とし、その文字列を画像内へ描画しない。
- PNG は編集用 backup、復元用正本、SQLite との双方向同期に使わない。

#### 検索サジェスト

- 検索対象は単一選択の selector とし、既定値をタイトルにする。基本候補はタイトル、学習元、本文、Cue、すべてとする。
- tag は既存の専用 filter を使い、検索対象 selector とサジェストから除外する。
- 候補は選択範囲の local data に存在する語句に限る。入力 1 文字目から最大 5 件を返し、前方一致を優先する。外部辞書 API、外部推論、telemetry を使わない。

#### 大規模一覧

- 5,000 件を長期利用の最低目標とし、利用上限にしない。deterministic な 10,000 note fixture で性能を確認する。
- 全件を一度に取得、描画せず、下端付近で次のまとまりを読む追加読み込み型の無限スクロールを採用する。
- virtualization または同等の windowing で DOM 要素数とメモリ増加を制限する。
- 現行 MVP の検索 API と 1 ページ 50 件の契約は、後続仕様 task の承認まで変更しない。

## Implemented Support Tooling

10,000 note の比較、性能確認を支援する isolated fixture tooling は実装済みである。

```sh
npm run fixture:generate -- [options]
npm run dev:fixture -- [options]
```

- fixture generator は既定 10,000 件、固定 seed cornell-method-fixture-v1 を使い、既存 migration を適用して Canvas と relation を read-back 検証する。実装は [generate-sqlite-fixture.js](scripts/generate-sqlite-fixture.js) にある。
- fixture 用 dev 起動経路は OS の一時 directory に fixture を生成し、その DB 専用の Next.js 開発 server を起動する。終了時は生成した一時 file だけを削除する。実装は [dev-sqlite-fixture.js](scripts/dev-sqlite-fixture.js) にある。
- command の定義は [package.json](package.json) にある。
- この tooling は製品 DB の seed ではない。存在することだけでは Desktop shell、Desktop PoC、Canvas PNG、検索対象 selector、サジェスト、無限スクロール、virtualization の実装または性能確認済みを意味しない。

## Open / Unknown

- Desktop shell は未選定である。両候補の比較 summary は存在するが、native runtime、GUI、lifecycle、メモリ、`.app` / DMG 等の required evidence が未成立または BLOCKED のため、どちらも採用済みとして扱わない。
- 対象の別物理 Mac 上で read-only preflight は未実施である。対象 Mac の machine、OS / build、RAM、storage、FileVault、Firewall、MDM の facts はすべて UNKNOWN とする。
- `summary/20260813/2130-vm-host-preflight-69726e2c-summary.md` の preflight は現在の作業 Mac 上で実行されたものに限る。そこにある host facts、空き容量、Firewall、FileVault 等を対象 Mac の結果や VM の GO 判定として引き継がない。
- VM の具体構成、UTM の採否・導入、guest OS / image、保存先、network / sharing、VM 作成、toolchain / dependency install は、対象 Mac の preflight、発注者の GO、操作ごとの個別承認が済むまで未決・未実施である。VM 計画 summary は実施順と承認境界を示す計画であり、この handoff で判断済み・承認済みとはしない。
- 対象 Mac の容量整理、ファイル削除、設定変更、UTM 導入、network / sharing 変更、OS image や package の取得を次 task として自動承認しない。
- bundle ID、user data / backup / 設定 / log の具体的な path、更新 provider、manifest / package の配置先と取得方式、署名・完全性検証方式、minimum deployment target は未決定である。
- Developer ID、notarization、Apple Developer Program、Intel artifact、一般公開用配布サイトは Public Mac Release の判断事項である。
- Settings の正確な文言と項目配置、完全なデータ削除の確認文言は Desktop Alpha の実装 task で決める。
- Canvas PNG の使用不可文字、同名 file、保存先、失敗時 UI、色管理は未決定である。
- 検索の Summary 分類、部分一致、tokenization、同順位、API / index、query schema、keyboard 操作と、一覧の取得単位、cursor / offset、仮想化方式、事前読み込み距離は未決定である。
- PDF export は現在未採用であり、次の外部出力候補として扱わない。将来再検討するかも未決定で、再検討時は Canvas PNG と分けた仕様、PoC、採用判断を必要とする。
- autosave、draft、version・競合処理、Undo / soft delete / purge、専用復習タスク、NoteCard / D&D、タグ管理 mutation、定期 backup、暗号化 backup は未採用である。発注者が個別に採用するまで仕様・実装 task を開始しない。
- Browser runtime、mobile、wheel / trackpad、実 DB read-back、E2E、外部 Postgres、追加の build / Prisma runtime は必要に応じて扱う任意 QA であり、現時点の最新結果はこの handoff で再確認していない。
- GitHub の PR / Issue、checks、merge 状態は live 確認していない。local queue の done / failed を GitHub の状態へ読み替えない。

## Current Repository State

### Git

2026-08-13 22:03:58 JST に read-only で確認した。

| 項目 | 確認結果 |
| --- | --- |
| branch | develop |
| HEAD | 1482e2e78c239298541dbda222aba54cd3a0d9b4 |

この handoff task の作業開始時の `git status --short` は空だった。今回の作業で変更する対象は `HANDOFF_2026-08-12.md` のみであり、他のファイルは変更しない。作業後の status は検証節で再確認する。

### Queue

2026-08-13 22:03:58 JST の `worker-status.sh` で確認した時点の snapshot である。queue status は作業単位で変動するため、再開時は live status を再確認する。

| Queue | 処理済み | 完了 | 失敗 | 実行中 | 待機 |
| --- | ---: | ---: | ---: | ---: | ---: |
| Common | 334 / 335 | 295 | 39 | 1 | 0 |
| UI | 266 / 266 | 251 | 15 | 0 | 0 |
| API | 32 / 32 | 31 | 1 | 0 | 0 |
| Overall | 632 / 633 | 577 | 55 | 1 | 0 |

Common の実行中 1 件は、この handoff task である。runner による task 移動と自動 summary 作成は、この task の終了後に反映される。

## Next Read

raw log、.next 系 artifact、古い大量 summary は読まない。次の文書と該当節だけを読む。

1. [HANDOFF_2026-08-12.md](HANDOFF_2026-08-12.md)
2. [Desktop shell PoC 比較 summary](summary/20260812/1337-compare-desktop-shell-poc-evidence-20260812-summary.md) と [Electron / Tauri 比較 summary](summary/20260812/1342-compare-electron-tauri-poc-evidence-20260812-534d51a5-summary.md)
3. [VM 検証環境の設計 summary](summary/20260812/1948-design-isolated-macos-vm-validation-environment-20260812-f29b19cd-summary.md) と [別ホスト VM 計画 summary](summary/20260812/2059-plan-macos-vm-on-separate-host-20260812-c46f7537-summary.md)
4. [vm-host-preflight summary](summary/20260813/2130-vm-host-preflight-69726e2c-summary.md) は、現在の作業 Mac の preflight であり、対象 Mac の結果ではないことを確認するために読む。host facts や容量を対象 Mac の GO 判定へ流用しない。
5. [PRODUCT_SPEC.md](doc/requirements/PRODUCT_SPEC.md) の Desktop / local-first / Phase 2 境界、[POST_MVP_IMPLEMENTATION_PLAN.md](doc/implementation/POST_MVP_IMPLEMENTATION_PLAN.md) の §5〜§7・§9・§13・§14、[MVP_CONTRACT.md](doc/implementation/MVP_CONTRACT.md) の §1・§9、[TARGET_ARCHITECTURE.md](doc/technical/TARGET_ARCHITECTURE.md) の Desktop shell / local runtime / storage boundary と Desktop Alpha 後の拡張境界

## Next Action

1. 発注者と Manager は、対象の別物理 Mac 上で read-only preflight を実行できる運用経路と担当を確認する。対象 Macへ接続したり、現在の作業 Macの facts を対象 Macへ読み替えたりしない。
2. 承認された経路で対象 Mac の read-only preflight を実行し、machine、OS / build、RAM、storage、FileVault、Firewall、MDM を記録する。結果が揃うまで VM 作成、容量整理、設定変更、UTM 導入を開始しない。
3. preflight 後に発注者が GO と個別操作を承認した場合だけ、VM の保存先・guest OS・resource・UTM / backend・network / sharing の具体構成を対象 Mac 上で決める。容量整理、削除、Firewall / FileVault 等の設定変更、購入、UTM 導入、OS image 取得、VM 作成は自動承認しない。
4. VM が承認・準備された後に、toolchain / dependency の取得と lockfile 確認、Electron / Tauri の native runtime、GUI、lifecycle、SQLite read-back、RSS、`.app` / DMG の残る P0 実測を同条件で行う。不成立項目は BLOCKED / UNVERIFIED のまま残す。
5. 両候補の required evidence と比較結果を発注者へ提示し、発注者が shell を選定するまで Desktop Alpha の正式基盤、coding task、Phase 2 coding task を投入しない。
6. shell 選定後は [POST_MVP_IMPLEMENTATION_PLAN.md](doc/implementation/POST_MVP_IMPLEMENTATION_PLAN.md) §7 の順で Desktop Alpha を実装し、packaged Desktop Alpha の結合 QA を完了する。Canvas PNG、検索サジェスト、大規模一覧は Desktop Alpha 完了後まで着手しない。
7. Desktop Alpha 完了後に、採用済み Phase 2 の Canvas PNG と検索・一覧改善を別 task で扱う。autosave、Undo / soft delete、専用復習 task、NoteCard / D&D 等は発注者の採用判断前に仕様・coding task を投入しない。任意 QA は Desktop PoC と分け、未確認の任意 QA を Gate 0 の blocker に戻さない。
